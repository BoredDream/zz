# 第二问 论文图表 · 交付说明

> 给**论文手**与**编程手**。本目录自包含：数据 + 生成脚本 + 成图（SVG/PDF/PNG）三份同源。
> 所有数字均来自赛题真实附件与仓库 `zz` 的求解产物，**无虚构、无随机数、无平滑插值**。
>
> ⚠️ **已全部升级为优秀论文级科研图表**：**9 张图**均已按统一视觉规范重设计，
> 绘图脚本为 **`figures/figs_q2_paper.py`**（一个脚本产出全部 9 张，**推荐使用**）。
> 九张图的纯图集预览：**`图集_第二问_论文版.pdf`**（706 KB，位于 `handoff/q2/`）。
> 本分支只保留这一个绘图脚本，早期版本未随库分发。

---

## 零、优秀论文级视觉规范（9 张图统一执行）

| 项目 | 规范 |
|---|---|
| 背景 | 纯白；简洁二维，**无渐变 / 发光 / 3D / 阴影** |
| 边框 | **去掉顶部与右侧**；左/下边框 0.7 pt、色 #B4BBC2 |
| 网格 | 仅水平，浅灰 #DFE3E7、lw 0.5、alpha 0.30（弱化，不抢数据） |
| 配色总原则 | **全篇低饱和蓝灰系**；同义同色（同一含义在 9 张图中颜色一致） |
| 主色 | **深蓝灰 #4C6E91**（计划量、模型预测等主数据）；深版 #3A5470 用于中位数线 |
| 强调色 | **去饱和赤陶 #A9705A**（紧急购电、最大偏差时点、核心结论）—— 全篇唯一暖色 |
| 实测值 | 灰 #5B6470（真实观测、实际净负荷） |
| 对照 / 次要 | 灰 #9AA4AE、浅灰 #C9D0D6 |
| 储电量 | 低饱和绿 #7E9B84（**仅图3a、图3b 使用**，全篇不重复用色） |
| 多重区分 | 颜色 **+** 线型 **+** marker：实线/●=主数据；虚线/■=对照或实测；点划线/▲=次要 |
| 黑白可辨识 | 每张图任意两条曲线至少在线型或 marker 上不同 |
| 双轴 | **全部取消 twinx**（图2a 已改为上下分栏单主题图，图3a 已改为单面板四日对比） |
| 字号 | 刻度 8.5 pt、轴标题 9.5 pt、图例 8.5 pt、面板小标题 9 pt；文字统一深灰 #333333 |
| 版面 | A4 双栏 figsize 7.1 × (3.3–4.7) 英寸 |
| 输出 | SVG（矢量、文字可编辑）+ PDF（矢量）+ PNG（**600 dpi**） |

**本轮相对上一版的具体改动**：

| 图 | 改了什么 | 为什么 |
|---|---|---|
| 全部 9 张 | 配色由「科研蓝 + 橙色」改为**低饱和蓝灰 + 去饱和赤陶**；橙色降饱和为 #A9705A | 符合学术插图风格要求：低饱和、克制、适合论文排版 |
| **图1** | **图注口径修正**：原写"情景全距覆盖 68.40%"，但图内只画 10–90 分位带 → 覆盖率统一为**分位带口径 54.51%** | 图内没有"全距带"，写全距覆盖率属于图文不符 |
| 图3a | 上下限标注由右侧移至左侧；改用手工轴布局 | 原标注与曲线重叠，且 `tight_layout` 遇到轴外图例导致绘图区被挤到右侧、左留大片空白 |
| 图1b | **数据审计 + 全面重设计**：① 查出并修正**时间标签生成缺陷**（附件1.xlsx 时间列为混合类型，后 12 行字符串被旧函数误判为 `24:00`，导致 24:00 出现假垂直线、末端曲线失真；数值本身无误）；② 删除整块绿色 `axvspan` 矩形背景，改为 `fill_between(where=net<0)` 只填净负荷负值区；③ 删除电价曲线下方的棕色大面积填充；④ 删除全部 marker；⑤ 光伏改用灰虚线（赤陶色归还给"紧急购电 / 误差 / 风险"语义）；⑥ 图内数值标注由 6 处减到 3 处（只留"光伏富余区 / 谷价区 / 尖峰价区"）；⑦ 图例改顶部单行、名称缩短为"小区负荷 / 光伏出力 / 净负荷"；⑧ x 轴标签改"时刻 / h"、刻度改 0/4/8/12/16/20/24 | 先修逻辑再改视觉：旧图的"光伏 10:00 后长期 6400 kW""24:00 垂直下降"均为标签缺陷造成的伪影；画面去除大面积色块、密集 marker 与文字噪声 |
| 图2a | **信息结构重构（单主题图）**：删除"实际净负荷""模型预测净负荷"两条曲线；删除大面积蓝灰填充；改为「上栏仅电价（35%）+ 下栏仅计划购电量窄柱（65%）」；删除大白框与长弯箭头；标题改为"单日计划购电与分时电价"；**并修正旧图注口径错误**（旧写模板行 42,769.8742 kWh / 26,344.09 元，图内实为自然日 42,605.0967 kWh / 26,273.6272 元） | 一图只回答一个问题（电价响应），预测准确性交由图1；消除视觉噪声与图文不符 |
| 图4a | **噪声清理重构**：长标题拆解为"(a) 预测误差的日内分布""(b) 各月平均绝对误差"；删除图内"正/负（缺电风险侧/剩余电量侧）"文字说明（改由 y=0 基准线承担）；全期 MAE 由出现两次减为**只在下面板出现一次**；6 月柱顶只留数值"84.1"（删"最高"）；**平均偏置 −9.45 完全移出图内**；月份标签去前导零（02月→2月）；x 轴刻度由 24 个减为 9 个；网格 alpha 0.30→0.22；版式比例改 `height_ratios=[1.8, 1.0]`、`hspace=0.42`；**并更正旧说明中两处事实错误与一处颜色描述** | 减少重复信息、文字堆叠与视觉噪声；让主次分明（6 月为唯一强调月） |
| 图3b / 图4b | 同步换为蓝灰配色；图3b 放电改赤陶、充电保持低饱和绿 | 保持全篇一致 |
**上一版的改动（保留记录）**：

| 图 | 改了什么 | 为什么 |
|---|---|---|
| 图1 | 情景包络由 2 层减为 1 层 10–90 分位；橙色点只在最大偏差面板出现 | 全距与 10–90 分位几乎重合；四面板都画点会误读为"每格都有极值点" |
| 图2a（上一版） | 删除 twinx 双轴，电价改独立上栏 | 双轴两线交点无物理意义，是竞赛图常见扣分点 |
| 图2b | 计划量降蓝、紧急购电升为强调色 | 突出 1.27% 的小量是这张图的核心任务 |
| 图3a | 图型更换：2×2 四单日面板 → 单面板四日对比 | 原版双轴+四面板 = 8 条信息线，缩到论文宽度不可读 |
| 图4a | 箱体改灰蓝、中位数用主色突出；最差月染强调色 | 原绿色填充与全篇色系冲突；最差月需直接点出 |
| 图5 | 右栏分组柱改折线+marker，主模型档用强调色实心 | 折线斜率比柱高更易看出"2 倍终端价值时费用上升" |

---

## 一、目录内容

```
handoff/q2/
├── README.md                            ← 交付总索引（先读这个）
├── 第二问_论文素材包.md                   ← ★ §A–§J 全套论文素材 + 附录 §K 代码索引
├── 第二问_题目表格_表1表2表3.csv           ← 表1/表2/表3 可复制版（带口径列）
├── 第二问_核心指标汇总.csv                 ← §D1 全部指标（带口径与来源）
├── 第二问_图表数据需求清单.csv             ← 图表数据需求
├── 图集_第二问_论文版.pdf                 ← ★ 九张图纯图集（706 KB）
├── figures/
│   ├── README_图表说明.md                ← 本文件（图集总览 + 每张图的用途与图注）
│   ├── figs_q2_paper.py                  ← ★ 唯一绘图脚本：一次产出全部 9 张论文级图
│   ├── make_album.py                     ← 生成《图集_第二问_论文版.pdf》
│   ├── fig1_netload_forecast.svg/.pdf/.png
│   ├── fig1b_typical_day.svg/.pdf/.png
│   ├── fig2a_single_day_dispatch.svg/.pdf/.png
│   ├── fig2b_monthly_stack.svg/.pdf/.png
│   ├── fig3a_soc_trajectory.svg/.pdf/.png
│   ├── fig3b_soc_hist.svg/.pdf/.png
│   ├── fig4a_forecast_error.svg/.pdf/.png
│   ├── fig4b_emergency_cost_band.svg/.pdf/.png
│   └── fig5_strategy_compare.svg/.pdf/.png
├── scripts/
│   ├── gen_plot_data.py                  ← 生成 _figdata/ 全部绘图数据
│   └── gen_scenarios.py                  ← 重算净负荷情景集（图1 的数据源）
└── _figdata/                             ← 11 个绘图数据文件（CSV，均带表头）
```

**共 9 张图 × 3 种格式 = 27 个图片文件**，另加 1 份图集 PDF。

| 格式 | 用途 | 特点 |
|---|---|---|
| **SVG** | Word / WPS / Inkscape 排版 | 矢量；文字为可编辑 `<text>` 节点，**0 个嵌入位图**，改字号/措辞不必回 Python |
| **PDF** | LaTeX / 专业排版 | 矢量，缩放不失真 |
| **PNG** | 快速预览 / 投稿系统 | 600 dpi |

---

## 二、图集总览（一张表看清每张图干什么）

| 图号 | 文件 | **这张图是干嘛的**（回答什么问题） | 规范 |
|---|---|---|---|
| 图1 | `fig1_netload_forecast` | 模型预测的净负荷与真实值在四个指定日期的吻合程度如何？预测的不确定性范围有多大？ | ★ 论文级 |
| 图1b | `fig1b_typical_day` | 典型日的负载、光伏、净负荷与分时电价长什么样？光伏富余出现在何时、有多少？ | ★ 论文级 |
| 图2a | `fig2a_single_day_dispatch` | 模型是否实现了"低价多购、高价少购"的电价响应策略？（**不承担预测精度展示，该任务见图1**） | ★ 论文级 |
| 图2b | `fig2b_monthly_stack` | 全期 334 天的购电量按月怎么分布？紧急购电集中在哪个月？ | ★ 论文级 |
| 图3a | `fig3a_soc_trajectory` | 四个指定日期的储电量轨迹是否始终在 1200–10800 内？是否与电价形成反向关系？ | ★ 论文级（已换图型） |
| 图3b | `fig3b_soc_hist` | 全期储电量与充放电量的分布形态如何？是否长期贴边界、是否经常打满功率上限？ | ★ 论文级 |
| 图4a | `fig4a_forecast_error` | 预测误差在 24 小时内如何分布、有无系统性偏差？各月 MAE 哪个月最大？ | ★ 论文级 |
| 图4b | `fig4b_emergency_cost_band` | 紧急购电发生在什么电价档、什么时刻？为什么它不影响"模型省钱"的结论？ | ★ 论文级 |
| 图5 | `fig5_strategy_compare` | 储能与日内补救各值多少钱？结果对终端储电价值取值是否敏感？ | ★ 论文级 |
| ~~图6~~ | —— | 第一问 vs 第二问对比。**未生成**：第一问尚未计算，无数据可用，不补造 | —— |

---

## 三、逐图说明（用途 + 读法 + 关键数字 + 可抄图注）

### 图1 `fig1_netload_forecast`｜净负荷预测与情景包络（2×2）

**用途**：这是第二问唯一的"预测 vs 观测"配对图。它要回答两件事——模型在题目指定的四个日期上
预测得准不准；以及模型内部那组历史残差情景张开多大的不确定性范围。图注承担"全期为留出集回测"的声明。

**读法**：深灰虚线=真实观测；蓝实线=模型中心预测；蓝色色带=历史残差情景包络（**仅画 10–90 分位带**）。

**关键数字**：四日 MAE 48.43 / 78.28 / 51.92 / 39.17 kWh per 10 min；全期 MAE 61.29、RMSE 85.82。
情景区间 n=14 / 14 / 14 / 7。

**图注**：
> **图 1　2025 年四个指定日期净负荷的模型中心预测与真实观测对比。**
> 数据为 2025-02-01 至 2025-12-31 交付期（334 天 × 144 个 10 分钟时段）的净负荷（小区负载 − 光伏出力，
> 单位 kWh/10 分钟）。本图为**留出集回测**结果：模型在每天 0:00 仅使用此前已完整观测的历史轮廓生成预测，
> 图中所绘预测值与真实观测值均为同期实际数据，**不含任何未来外推区间**，亦无训练期拟合曲线
> （该预测器为滚动中位数加权，无拟合值序列）。深灰实线为真实观测值；蓝实线为本文模型中心预测；
> 蓝色由深至浅的色带为**历史残差情景包络**（n=14 / n=7），**不是置信区间，也不是预测区间**。
> 图内仅作一处标注，规则为"本图所示日期范围内绝对预测偏差最大的时点"（2025-06-21 11:30 前后，
> 311 kWh/10 分钟）。全期预测精度 MAE 61.29、RMSE 85.82 kWh/10 分钟。
> 需说明的是，情景包络偏窄：四日合计仅 **54.51%** 的真实观测落在图中所示的 10–90 分位带之内
> （若按包络全距计算为 68.40%，但**图内未绘制全距带**，引用时请与图一致，用分位带口径）。

---

### 图1b `fig1b_typical_day`｜典型日负荷/光伏/净负荷与分时电价

> **重大重构说明（本轮）**：图1b 已**重新设计**，并且**在改图之前做了一次数据审计，查出并修正了一处时间标签生成缺陷**。
> **旧版图内的大面积绿色背景、棕色电价填充、多处数值标注、密集 marker 均已删除**；
> 旧描述中"绿色底纹标出光伏富余时段""红/蓝底纹=峰/谷价区"的说法已作废。
>
> ⚠️ **审计发现的真实缺陷（已修复）**：附件1.xlsx 的时间列是**混合类型**，且切换点恰好落在 **10:00** ——
> idx 0–59 为 `datetime.time`（00:10 … 10:00），**idx 60–143 为纯字符串**（`'10:10'` … `'0:00+1'`）。
> 旧版数据生成函数用 `"24:00" if not hasattr(v, "hour") else ...` 判断，
> 使**整个下午的 84 行（10:10–24:00）标签全部被写成 `24:00`**。
> 后果：`slot_of()` 对这 84 行返回同一个 x=24，折线在 x=24 处往返——
> **这就是旧图中"光伏从 10:00 起长期维持约 6400 kW"与"24:00 处垂直下降到 0"的确切成因**，
> 两者都是绘图伪影，不是数据特征。
> **数值本身始终无误**（144 行数值与附件1.xlsx 原始单元格逐行逐列比对，**0 项不一致**），缺陷仅在标签生成。
> 已在 `scripts/gen_plot_data.py` 中改为「显式类型判断 + 字符串解析」双路径，并加入断言：
> 144 行标签必须互不相同、首末必须为 `00:10` 与 `24:00`（修复前唯一标签仅 61 个，修复后 144 个）。
> 修复后逐时点复核：pv 峰值 7612.3 kW 出现在 **12:10**；pv 于 **19:20** 归零；
> net<0 仅出现在 **09:40–14:30**；**24:00 不再有垂直线**。

**核心任务（只回答这一个问题）**：典型日中，小区负荷、光伏出力、净负荷与分时电价在日内如何变化？

**信息结构（上下两面板，共享 x 轴；上 65% / 下 35%）**：

| 面板 | 画什么 |
|---|---|
| 上：功率 | 三条**干净折线**（无 marker）：小区负荷（深蓝灰实线 1.6）、光伏出力（灰虚线 1.5）、净负荷（青灰绿点划线 1.8，作为核心派生量略突出）；`y=0` 浅灰基准线；**仅把净负荷低于 0 的实际区域填充**（`fill_between(where=net<0)`，alpha 0.15） |
| 下：电价 | **仅一条赤陶阶梯线**（lw 1.5），**无曲线下方填充**；用 alpha 0.06 的极浅水平色带区分谷价区 / 尖峰价区 |

**图内文字只保留 3 处**：「光伏富余区」（净负荷负值区附近，无边框无箭头）、「谷价区」「尖峰价区」。
以下数值**全部移入图注与正文，不进图**：光伏峰值、光伏富余总量、最低价、最高价、峰谷价比。

**读法**：上栏深蓝灰实线=小区负荷、灰虚线=光伏出力、青灰绿点划线=净负荷（左轴，kW）；
浅绿填充带=净负荷为负的时段，即光伏富余；下栏赤陶阶梯线=分时电价（右轴，元/kWh）。

**关键数字**（来自附件1 的 144 个 10 分钟时段，修正标签后重算）：

| 项 | 数值 |
|---|---:|
| 小区负荷范围 | 3,309.4 – 5,959.0 kW |
| 光伏峰值 | **7,612.3 kW @ 12:10** |
| 光伏非零区间 | 04:40 – **19:20**（19:20 后严格为 0，共 55 个时段为 0） |
| 净负荷为负（光伏富余）区间 | **09:40 – 14:30**，共 30 个时段 |
| 光伏富余电量 | 6,248.0 kWh |
| 净负荷最低值 | −2,117.5 kW |
| 电价范围 | 0.3713 – 1.3952 元/kWh |
| **峰谷价比** | **3.76**（1.3952 / 0.3713） |

**图注**（可直接抄进论文）：

> **图 1b　典型日（附件1）的负荷、光伏、净负荷与分时电价。**
> 上栏为小区负荷（实线）、光伏出力（虚线）与净负荷（点划线，定义为小区负荷 − 光伏出力）的日内变化，
> 单位为 kW；浅绿色填充区为净负荷为负的时段，即光伏出力超过小区负荷的"光伏富余区"，
> 出现在 09:40–14:30，合计富余电量 6,248.0 kWh。下栏为分时电价（题面规定每日相同），
> 单位为元/kWh，电价区间为 0.3713–1.3952 元/kWh，峰谷价比约 3.76；浅色横带分别标示谷价区与尖峰价区。
> 光伏出力峰值 7,612.3 kW 出现在 12:10，并于 19:20 后为 0。由图可见：光伏出力在白日上升使净负荷随之下降
> 并转负，而电价在夜间与正午偏低、在傍晚 18:00–21:00 达到尖峰，二者的时间错配为储能的低价充电、
> 高价放电提供了套利空间。图中数据为附件1 给出的 144 个 10 分钟时段的统计值，
> 未作任何平滑、插值或修改。

---

### 图2a `fig2a_single_day_dispatch`｜单日计划购电对分时电价的响应（单主题图）

> **重大重构说明（本轮）**：图2a 已**重新组织信息结构**，不再是旧版的"购电量 + 实际净负荷 + 预测净负荷 + 电价"四变量混排图。
> **本图现在只回答一个问题：模型是否实现了"低价多购、高价少购"的电价响应策略？**
> **它不再承担"预测 vs 实际"的展示任务** —— 预测准确性一律由图1 负责。
> **旧描述中"图2a 为双轴图""图内有实际/预测净负荷曲线"的说法已作废，请勿再引用。**

**用途**：向评委直观呈现调度策略的**经济合理性**。视觉顺序被刻意设计为
「电价变化 → 计划购电量响应 → 低价多购、高价少购」，一眼可读，不需要读图例或说明框。

**信息结构（上下两面板，共享 x 轴；上 35% / 下 65%）**：

| 面板 | 画什么 | 不画什么 |
|---|---|---|
| 上：分时电价 | 阶梯线 + **极浅**同色填充（alpha 0.08）；尖峰价时段（≥1.25 元/kWh）用 alpha 0.10 底纹并标注"尖峰价时段" | 无右 y 轴、不叠加任何其他变量 |
| 下：计划购电量 | **窄柱**（宽 0.84×10 分钟），深蓝灰 **#4C6E91** 单一主色；本日无紧急购电故无叠加 | **无面积填充**；**不画实际净负荷**；**不画模型预测净负荷** |

**读法**：上栏赤陶阶梯线=分时电价（左轴，元/kWh）；下栏深蓝柱=计划购电量（kWh/10 分钟）。
两栏共享 x 轴（0–24 时，主刻度 0/4/8/12/16/20/24），上栏不重复 x 轴标签。

**关键数字**（2025-06-21，自然日口径，**与图内 144 个柱值完全对应**）：

| 项 | 数值 |
|---|---:|
| 计划购电量合计 | 42,605.0967 kWh |
| 计划购电费合计 | 26,273.6272 元 |
| 紧急购电量 | **0** kWh（本日无紧急购电） |
| 单时段最大计划购电量 | 1,411.6828 kWh/10 分钟（00:20） |
| 电价范围 | 0.3713 – 1.3952 元/kWh |
| 尖峰价时段（≥1.25） | 15 个时段，其计划购电合计仅 **2,139.0842 kWh** |
| 计划购电为 0 的时段数 | **65** 个 |

**电价响应的直接证据**（论文可引用）：

- **低价多购**：00:00–06:00 电价 0.4264–0.4482 元/kWh，计划购电合计 22,699.6 kWh；
  21:00–24:00 电价回落到 0.4177–0.9129，计划购电合计 11,412.0 kWh。
- **高价少购**：19:00–20:00 电价 1.3480 元/kWh，**计划购电为 0**。
- **65 个零购电时段中，有 21 个时段的净负荷为正**，即并非因为光伏富余，而是**纯粹因价格高而不购电**；
  另外 44 个同时处于光伏富余区间（07:30–15:50）。

**图注**（可直接抄进论文）：

> **图 2a　2025-06-21 单日计划购电与分时电价。**
> 上栏为当日分时电价（144 个 10 分钟时段，0.3713–1.3952 元/kWh），橙色底纹区间为尖峰价时段
> （电价 ≥1.25 元/kWh，共 15 个时段）；下栏为当日 0:00 发布的计划购电量（144 个 10 分钟时段，
> 单位 kWh/10 分钟）。当日计划购电量合计 42,605.0967 kWh、计划购电费 26,273.6272 元，
> 无紧急购电。由图可见：电价处于 0.42–0.45 元/kWh 的夜间时段计划购电量最高
> （00:00–06:00 合计 22,699.6 kWh），电价为 1.3480 元/kWh 的 19:00–20:00 时段计划购电量降为 0；
> 全天 65 个时段的计划购电量为 0，其中 21 个时段并非光伏富余所致，而是对高电价的直接响应。
> 尖峰价时段的计划购电量合计仅 2,139.0842 kWh，占全天计划购电量的 5.02%，
> 表明模型实现了"低价多购、高价少购"的电价响应策略。
> 本图仅展示计划购电量对电价的响应，净负荷的预测精度与偏差分析见图1。

---

### 图2b `fig2b_monthly_stack`｜月度购电量构成与紧急购电占比（上下双面板）

**用途**：把 334 天压缩成 11 根柱子，回答"全期费用花在哪、紧急购电是否可控"。
下面板用占比而不是绝对值，因为紧急购电量相对计划量极小（1.27%），绝对值在图上看不见。

**读法**：上面板堆叠柱（蓝=计划、红=紧急）；下面板仅画占比，虚线=全期均值。

**关键数字**：计划购电量 20,755,750.91 kWh、紧急 267,693.20 kWh；
紧急占比最高 2025-06（3.0991%）、最低 2025-11（0.4129%）；全期均值 1.2808%；
单月购电量最大 2025-12（2,627,683 kWh）。

**图注**：
> **图 2b　2025 年 2—12 月计划购电量与紧急购电量的月度构成。**
> 上图蓝色柱为计划购电量，红色柱为紧急购电量（按 5 倍电价购入），二者均为自然日口径
> （00:00—24:00）的月度合计；下图为紧急购电量占当月总购电量的比例（%），虚线为交付期
> （334 天）均值 1.2808%。紧急购电量占比在 2025 年 6 月最高（3.0991%），
> 2025 年 11 月最低（0.4129%）。

---

### 图3a `fig3a_soc_trajectory`｜四日储电量轨迹（单面板四日对比）

> **结构说明**：图3a 已在上一轮改为**单面板四日对比**（原 2×2 四单日面板 + 右轴电价已作废）。
> 四条曲线以**颜色 + 线型 + marker** 区分，**不再叠加电价**（避免双轴与信息线过多）。

**用途**：证明储能**始终运行在题面要求的 1200–10800 kWh 区间内**（对应素材包 §F1 约束校验的图形化证据），
并横向对比四个指定日期的储电量轨迹形态。

**读法**：四条线=四个指定日期的储电量（左轴）；赤陶虚线=运行上下限 1200 / 10800 kWh。

**关键数字**：SOC 起末值 2029→1398、4943→4940、1205→1200、3732→1347 kWh；
全期实测 min=1200.00、max=10800.00，四日全程未越界；2025-06-21 与 09-23 在白天长时间贴上限。

**图注**：
> **图 3a　四个指定日期的储电量轨迹。**
> 四条曲线分别为 2025-03-20（2029→1398 kWh）、2025-06-21（4943→4940 kWh）、
> 2025-09-23（1205→1200 kWh）、2025-12-21（3732→1347 kWh）的逐 10 分钟储电量（单位 kWh）；
> 赤陶色虚线为储能运行上下限 1200 kWh 与 10800 kWh。全部时段储电量均位于运行区间内，未出现越界。

---

### 图3b `fig3b_soc_hist`｜储电量分布与充放电量分布（左右双面板）

**用途**：**这是全图集里唯一能暴露模型结构性特征的一张**。它回答两个没被文字说清的问题：
储电量是否长期贴边界？充放电是否经常打满 833.33 kWh 的功率上限？
实测结果对"储能容量与功率是否够用"这个改进方向极有说服力。

**读法**：左面板 SOC 分布直方图（红线=上下限）；右面板充放电量直方图（红线=功率上限）。

**关键数字**：贴上限（≥10750 kWh）占 **14.75%**、贴下限（≤1250 kWh）占 **4.21%**；
充放电打满功率上限的比例为充电 **4.1%**、放电仅 **0.2%**；
放电分布明显比充电更集中于低值。

**图注**：
> **图 3b　全期储电量与单时段充放电量的分布。**
> 左图为 48,096 个时段起始储电量的分布，红色虚线为运行上下限 1200 / 10800 kWh，
> 其中贴上限（≥10750 kWh）的时段占 14.75%、贴下限（≤1250 kWh）的占 4.21%；
> 右图为单时段充电量与放电量的分布，红色虚线为功率上限 833.33 kWh
> （由题面附录1 给出的最大充放电功率 5000 kW 与 10 分钟时段长度换算得到），
> 打满该上限的时段占比为充电 4.1%、放电 0.2%。

---

### 图4a `fig4a_forecast_error`｜预测误差的日内分布与各月平均绝对误差（上下双面板）

> **重构说明（本轮）**：图4a 已按"减少重复信息、文字堆叠与视觉噪声"重做。
> **图内已删除**：长标题中的 MAE/偏置、图内的"正/负（缺电风险侧/剩余电量侧）"文字说明、
> 上面板重复出现的"全期 MAE 61.29"、6 月柱顶的"最高"字样、以及**平均偏置 −9.45（完全移出图内）**。
> 面板标题简化为"(a) 预测误差的日内分布""(b) 各月平均绝对误差"。
> **同时修正了旧说明中的两处事实错误**（见下方"旧描述勘误"）。

**用途**：模型的**误差诊断**，只回答两个问题：
(a) 预测误差在一天 24 小时内如何分布、是否存在系统性偏差；
(b) 各月平均绝对误差差异如何、哪个月最大。

**信息结构**（共享 x 轴，上 64% / 下 36%，`height_ratios=[1.8, 1.0]`）：

| 面板 | 画什么 |
|---|---|
| 上：日内分布 | 24 组箱线图（0–23 时）：箱体=四分位距（浅灰蓝）、中线=中位数（深蓝灰）、须=非离群极值；不显示离群点；`y=0` 基准线代替正/负文字说明；x 轴刻度仅 9 个（0/3/6/9/12/15/18/21/23） |
| 下：各月 MAE | 11 根柱：普通月份低饱和灰蓝，**只有 6 月染赤陶**（全图唯一被强调的月份）；全期 MAE 用水平虚线表示，其标注**全图只出现一次**；月份标签为"2月…12月"（去前导零） |

**全图文字只保留 2 处数值**：「全期 MAE 61.29」（虚线右上方，只出现一次）与 6 月柱顶的「84.1」。

**关键数字**（由 48,096 条明细逐时段现算，未平滑未插值）：

| 项 | 数值 |
|---|---:|
| 全期 MAE | **61.29** kWh/10 分钟 |
| 全期 RMSE | 85.82 kWh/10 分钟 |
| 平均偏置 | **−9.45** kWh/10 分钟（**仅在图注与正文出现**） |
| 误差最大月 | **2025-06，84.14** |
| 误差最小月 | 2025-05，49.78 |
| 日内波动最大 | **12 时**：MAE 114.33、四分位距 201.64 |
| 白天 vs 夜间 | 7–16 时 MAE 均值 **95.62**；0–6 时与 21–23 时均值 **34.45** |

**口径要点**：全部 24 小时的中位数均为负（即各时段中位情形下实际略低于预测），
而相对**平均水平**（−9.45）却是负偏——两端同时成立，说明少数白天时段存在较大的正向误差；
因此不能把"箱体偏正/偏负"作为单一方向结论，论文中应以中位数与 MAE 两个口径分述。

**图注**（可直接抄进论文）：

> **图 4a　预测误差的日内分布与各月平均绝对误差。**
> 上图给出各小时预测误差（实际值 − 预测值）的箱线分布，箱体为四分位距、中线为中位数、
> 须为非离群极值，深灰水平线为零误差基准；下图给出各月平均绝对误差，虚线为全期均值。
> 全期 48,096 个时段的 MAE 为 61.29 kWh/10 分钟，平均偏置为 −9.45 kWh/10 分钟；
> 误差在白天明显大于夜间（7—16 时 MAE 均值 95.62、其余时段 34.45 kWh/10 分钟），
> 以 12 时波动最大；各月中 6 月误差最高（84.14 kWh/10 分钟），5 月最低（49.78 kWh/10 分钟）。

**旧描述勘误**（旧 README 与本轮实测不符，已更正）：

| 旧表述 | 实测 | 处理 |
|---|---|---|
| "逐月 MAE 6 月最高**约 81**、11 月最低约 49" | 6 月 **84.14**；最低为 **5 月 49.78**（11 月为 50.38） | 已改为精确值与正确月份 |
| "误差在 **09:00—17:00 呈正向**（箱体偏正）" | **全部 24 小时的中位数均为负**，无一小时为正 | 已改为按中位数与 MAE 两个口径分述 |
| "红色虚线为全期均值" | 虚线为全期 MAE，颜色为深灰（`#5B6470`），不是红色 | 已更正 |

---

### 图4b `fig4b_emergency_cost_band`｜紧急购电的电价档与时刻分布（左右双面板）

**用途**：**解释一个乍看矛盾的现象**——66% 的紧急购电量发生在尖峰价档（≥1.25 元/kWh），
而模型本身是为了省钱。左面板量化"钱花在哪个价档"，右面板显示"哪些时刻被迫补购"。

**读法**：左=按电价四档的水平条形（数值与时段数直接标注）；右=按小时的时段数柱状。

**关键数字**：尖峰价档（≥1.25）176,682 kWh 占 **66.0%**（371 时段）；
峰档（0.9–1.25）70,795 kWh 占 26.4%（711 时段）；
谷档（<0.5）9,894 kWh 占 3.7%（236 时段）。
时刻上双峰：**19:00–21:00**（最高 19 时约 353 个时段）与 **08:00–09:00**（约 186 个时段）。

**图注**：
> **图 4b　紧急购电量按电价档与按时段的分布。**
> 左图为交付期（334 天）紧急购电量在四个电价档上的分布，括号内为该档的购电量占比与出现时段数：
> 尖峰价档（≥1.25 元/kWh）176,682 kWh（66.0%，371 个时段）、峰档（0.9—1.25 元/kWh）
> 70,795 kWh（26.4%，711 个时段）、平档（0.5—0.9 元/kWh）10,323 kWh（3.9%，142 个时段）、
> 谷档（<0.5 元/kWh）9,894 kWh（3.7%，236 个时段）。
> 右图为出现紧急购电的时段在一天 24 小时上的分布（合计 1,460 个时段），
> 呈明显的双峰形态，主要集中于 19:00—21:00 与 08:00—09:00。
> 需注意：紧急购电量仅占总购电量的 1.27%，其绝对量集中于高价档反映的是"预测误差落在高价时段"
> 这一事实，不表示策略在高价时段主动多购电。

---

### 图5 `fig5_strategy_compare`｜策略费用对比与终端价值敏感性（左右双面板）

**用途**：这是**结果章的核心图**。左面板把"储能值多少钱、日内补救值多少钱"一次说清
（堆叠柱把计划费与紧急费拆开）；右面板做参数敏感性，回答"结论稳不稳"。

**读法**：左=三策略堆叠柱（蓝=计划购电费、红=紧急购电费），柱顶标总费用；
右=四个日期 × 三档终端价值系数的分组柱。

**关键数字**：主策略 1,445.5 万元、固定参考储能（局部反事实）1,691.2 万元、无储能 1,870.4 万元；
主策略相对无储能节省 424.84 万元；终端价值取 2 倍时四日费用普遍上升约 6%–12%。

**图注**：
> **图 5　三种策略的费用构成与终端储电价值的敏感性。**
> 左图为交付期（334 天）自然日口径总购电费的构成：蓝色为计划购电费、红色为紧急购电费。
> 主策略（情景优化 + 因果储能补救）1,445.5 万元；固定参考储能的逐日局部反事实 1,691.2 万元
> （该反事实每日沿用主策略的储电量起点、不响应实际偏差，仅用于度量日内补救的局部价值，
> **不是独立连续全年策略**）；无储能基线 1,870.4 万元。主策略相对无储能基线节省 424.84 万元。
> 右图为终端储电价值系数取 0、1、2 倍时的四个指定日期当日费用，其中 1 倍为主模型取值
> （0.9 × 最低电价）。

---

## 四、图内三个必须说明的数据事实（写正文时别漏）

1. **情景包络偏窄**（图1）：**54.51%** 真实值落在图中所示的 10–90 分位带内
   （包络全距口径为 68.40%，但图内未画全距带，**引用时请用与图一致的分位带口径**）。
   建议主动在误差分析一节说明，不要等评阅人指出。
2. **储电量贴上限相当频繁**（图3b）：14.75% 的时段储电量 ≥10750 kWh。
   这支持一个合理的改进方向——储能容量相对光伏富余偏小。
3. **紧急购电集中在尖峰价档**（图4b）：66% 落在 ≥1.25 元/kWh 档。
   这**不是**策略缺陷，而是"预测误差恰好落在高价时段"的结果；表述时务必区分因果。

---

## 五、如何改图 / 重出

在**仓库根目录**（即本仓库）执行：

```powershell
# ★ 全部 9 张论文级图（唯一推荐命令）
python handoff/q2/figures/figs_q2_paper.py

# 九张图的纯图集 PDF（输出到 handoff/q2/）
python handoff/q2/figures/make_album.py

# 需要重新生成绘图数据时才跑（不重跑求解器）
python handoff/q2/scripts/gen_plot_data.py
python handoff/q2/scripts/gen_scenarios.py
```

脚本路径全部相对自身解析，**在仓库内任意位置调用都可以**。所有命令都会同时覆盖 SVG / PDF / PNG 三份。
**不需要重跑求解器**，绘图数据已固化在 `handoff/q2/_figdata/`，且与 `outputs/q2/` 的已提交结果一致。
所有命令都会同时覆盖 SVG / PDF / PNG 三份。**不需要重跑求解器**，绘图数据已固化在 `_figdata/`。

**常用改动位置**（新版脚本 `figs_q2_paper.py`）：

| 想改什么 | 改哪里 |
|---|---|
| 输出格式（只要 SVG） | `SAVE_FORMATS = ["svg"]` |
| PNG 分辨率 | `PNG_DPI = 600`（如需 300 dpi 投稿限制可改 300） |
| 主色 / 强调色 | 顶部 `C_MAIN`（科研蓝）、`C_ACCENT`（橙） |
| 对照色 | `C_GREY` / `C_GREYBLUE` |
| 储电量色 | `C_SOC` |
| 线型与 marker 约定 | `LS_MAIN / LS_ALT / LS_ALT2`、`MK_MAIN / MK_ALT / MK_ALT2` |
| 网格深浅 | `RC["axes.grid"]` 与 `style()` 内的 `alpha=0.35` |
| 字号 | `RC` 字典中的 `font.size / axes.labelsize / legend.fontsize` |
| 图2a 的日期 | `fig2a()` 里的 `target = "2025-06-21"` |
| 图3a 的日期 | `fig3a()` 里的 `targets = [...]` |
| 电价档划分阈值 | 数据层：`gen_plot_data.py` 中的 `band` 划分（0.5 / 0.9 / 1.25） |
| 版面尺寸 | 各函数内的 `figsize=(...)`（A4 单栏 `(3.35, 2.5)`；双栏 `(7.1, 4.2~4.5)`） |

**要重新生成数据**（例如换了统计范围）才需要跑：

```powershell
python gen_scenarios.py     # 重算净负荷情景集（依赖 ../zz/src/q2_solver.py）
python gen_plot_data.py     # 重算其余绘图数据
```

> 两个数据脚本**不修改仓库任何文件**，只读取 `../zz/` 的数据与函数。

---

## 六、字体注意事项（直接影响出图质量）

本机实测：**只有 `Microsoft YaHei` 同时支持中文与 U+2212 减号**。
`SimHei`、`SimSun`、`KaiTi`、`FangSong`、`DengXian`、`Microsoft JhengHei`
**均缺少 U+2212 字形**——若用它们出图，图1、图1b、图4a 的负值坐标
会渲染成**空白方框**。

两脚本已做双保险：① 字体优先级把 `Microsoft YaHei` 排首位；
② 设置 `axes.unicode_minus = False`，用 ASCII 连字符替代 U+2212。

**换机器出图时**，若目标机器没有 Microsoft YaHei，请改用开源的
`Noto Sans CJK SC`（Google Fonts 可下载），并保持 `axes.unicode_minus = False`。

---

## 七、验收结果

| 验收项 | 结果 |
|---|---|
| 代码实际运行 | ✅ **9/9 张**全部成功生成，无异常退出 |
| 数据校验（图1 逻辑） | ✅ 列齐备 / 0 重复时间戳 / 0 非有限值 / 4 日 × 144 时段完整 |
| 图1 中心预测与已落盘值一致性 | ✅ 最大绝对差 **0.000e+00** |
| **数值不变性（本轮重设计）** | ✅ **57 项逐项对照，0 项超差**（图1 四日 MAE、图2a 自然日/紧急/错位对齐、图2b 11 月 × 2 项、图3a 四日首末 SOC、图5 三策略 + 12 组敏感性） |
| 原始附件与求解产物是否被改动 | ✅ 未改动（SHA256 留档：附件1 `66b87134…`、附件2 `2e95fd44…`、`interval_detail.csv` `f28c6da3…`） |
| 未使用随机数 / 未虚构数据 | ✅ 情景由确定性代码重算 |
| 未平滑 / 未插值 / 未补缺 / 未聚合 | ✅ 无任何此类操作 |
| 明文约束校验的图形证据 | ✅ 图3a 证明 SOC 全程在 1200–10800 内；图3b 标出 833.33 kWh 功率上限及打满比例 |
| 图例遮挡数据 | ✅ 全部置于图顶、面板上方或轴外，无遮挡 |
| **双轴（twinx）** | ✅ **已全部消除**（图2a 改为上下分栏单主题图；图3a 改为单面板四日对比，均不再叠加电价右轴） |
| 颜色数量 | ✅ 主色 1 + 强调色 1 + 灰阶 2 + 储能绿 1（绿仅储能图使用） |
| 黑白打印可辨识 | ✅ 任意两条曲线至少在线型或 marker 上不同 |
| SVG 可编辑性 | ✅ 文字为 `<text>` 节点，0 个嵌入位图 |
| 输出分辨率 | ✅ PNG 均为 **600 dpi**（3,751×2,641 ~ 5,605×2,046 px） |
| 图表数量 | ✅ 按素材包 §E1 登记册，第二问 10 张中完成 9 张；图6 依赖第一问，未生成且不补造 |

---

## 八、尚未交付的部分

- **图6（第一问 vs 第二问对比）**：第一问尚未计算，无数据可用。**不会补造**。
  待第一问完成后即可生成，无需改动本目录其余内容。
- **第三问图表**：按用户要求搁置。
- 数据层已就绪但暂未成图的：`附件1_net_load.csv`、`emergency_scatter.csv`
  （含每个紧急购电时段的日期与电价，可作更细的散点图）、`soc_bound_check.csv`
  （逐时段上下限触及标记）。
