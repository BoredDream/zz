# 1、2、3、4 符号统一对照表（论文唯一定稿口径）

> **本文件的定位**：这是**四问符号的唯一权威口径**。论文里凡出现符号，一律以本表「统一写法」列为准；
> 与四份来源文件不一致处，**以本表为准**，来源文件保留其原名只为便于对照代码与产物。
>
> **四份来源文件**：
>
> | 问 | 来源文件 |
> |---|---|
> | 一 | `E:\onedrive\Desktop\建模\model\analysis\handoffs\01-model-q1-model.md`（v7） |
> | 二 | `第二问_论文素材包.md` |
> | 三 | `第三问_论文素材包.md` |
> | 四 | `第四问_论文素材包.md` |
>
> **为什么需要这份文件**：四份文件各自内部自洽，但跨问共存在 **9 处硬冲突**
> （同名不同义 / 同义不同名），直接合并会违反交付包规格
> （`提示词_模型转论文交付包.md` 第 468 行：「第二问与第三问**共用一套符号**，
> 同一个量在两问里必须用同一个字母，不许同义不同名、同名不同义」；
> 验收清单第 761、768 行同款要求）。第一问的 v7 标题本身就写着
> 「**团队统一符号**」——但**它那一套与第二、三、四问并不是同一套**，见下表。
>
> **适用范围**：问题一至问题四（含第四问的 4-2 / 4-3 两个变体）。
> **本文不含任何新的数字结论**，只统一记号；数字仍从四份来源文件取。
>
> **配套分册**：`1，2，3，4敏感度分析符号统一表.md`。
> 本表统一的是**模型里的量**；分册统一的是**敏感度分析里的量**（被扰动参数、实验设计、结果指标、结论强度判定），
> 对标 `zz` 仓库 `q3-hybrid-mpc@5611746` 的 Q1–Q4 全部敏感性/鲁棒性实验。
> 分册**只新增符号、不改动本表**；两表冲突时**以本表为准**。

---

## 〇、三分钟速览：9 处硬冲突

写论文前先记住这 9 条，它们是最容易让评阅人认为「模型前后不一致」的地方。

| # | 冲突 | 第一问原写法 | 第二问原写法 | 第三 / 四问原写法 | 统一后 |
|---:|---|---|---|---|---|
| 1 | **$G$ vs $g$** | $G_t$ = 计划购电量 | $G_{d,k}$ = 计划购电量 | $g^k_t$ = **放电量** | 计划购电 → $x$；放电 → $g$ |
| 2 | **$U$ 含义相反** | — | $U^\omega_h$ = 剩余电量 | $U^k_t$ = **超计划量** | 剩余/弃电 → $y$；超计划量 → $U$ |
| 3 | **$S$ 含义相反** | — | $S$ = 情景数 | $S^k_h$ = 储电量 | 情景数 → $K$ / $\lvert\Omega\rvert$；储电量 → $S$ |
| 4 | **$\lambda$ 取值 5 vs 0.478** | — | 紧急购电价倍数 $=5$ | 终端水价 $=0.478$ | 倍数 → $\alpha=5$；终端价值 → $\lambda$ |
| 5 | **$\varepsilon$ 第三问不存在** | — | 吞吐惩罚 $10^{-7}$ | 第三问**明文不使用** | $\varepsilon$ 保留，但**必须逐问声明有无** |
| 6 | **$\pi$ 撞名** | — | $\pi_\omega$ = 情景概率 | 第三问 $\pi_h$ = 自然日电价向量 | 概率 → $\pi_k$；价格 → $p^{\text{nat}}_t$ |
| 7 | **$E$ 撞名** | $E_{\min},E_{\max},E_0$ = **储电量**上下界与初值 | $E^\omega_h$ = **紧急购电量** | $E^k_h$（四问） | 储电量界 → $S_{\min},S_{\max},S_0$；紧急购电 → $z$ |
| 8 | **$W$ / $P$ 撞名** | $W_t$ = **弃电量**；$P$ = **全天购电量** | $W_d$ = 星期样本集；$P_{\max}$ = 功率 | $w^k_t$ = 弃电量 | 弃电 → $y$；全天购电量 → $Q_d$；全天购电费 → $F$ |
| 9 | **紧急电价倍数写法** | — | 符号是 $\lambda$、公式里写**裸数字 5** | $\alpha=5$ | 一律写 $\alpha\,p$，**公式里不出现裸 5** |

> **一句话给论文手**：第一、二问的
> $G,W,U,E,v,\lambda,dt,PV$ 与第一问的 $E_{\min}/E_{\max}/E_0,\bar P,P,F$
> **在合并稿里全部要改**；
> 第三、四问的 $x,q,a,z,c,g,S,\alpha,\lambda,\Delta t,C_{\max}$ 是基准，不动。
> 基准里**只有一处还要动**：第三、四问的弃电量 $w$ 统一改为 $y$
> （因为第一问弃电也写作 $W$、第二问已有一个 $w_d$ 表示星期权重，且 $U$ 要留给超计划量）。

### ⚠️ 但符号统一**不等于**公式可以合并——先读 §3.10–§3.13

上表解决的是"一个字母两个意思"。**下面四条是更深的坑**：
符号改完之后，公式**看着一样、含义其实不同**。照抄会写出与代码不符的公式。

| 坑 | 一句话 | 详见 |
|---|---|---|
| **A** | 第二问同一个下标 $h$ 被用来表示两种东西（价格索引 vs 递推索引），差的正好一格 | §3.10 |
| **B** | 双框重组里，$x$ 与 $S,c,g,z,y$ 的相位**不一样**，不能用同一个 $\tilde u$ 一刀切 | §3.10 |
| **C** | SOC 相位：第一、二、三问是「时段开始」，**第四问是「时段末」** | §3.10 |
| **D** | 平衡式松弛量 $y$：第一问「只许弃光」、第二问「剩余电量（已购未用）」、第三、四问「弃电量无上界」——**三种语义** | §3.11 |
| **E** | $x_t$ 的地位：第一、二问是**实际购电量**，第三、四问只是**0:00 参考计划**（进平衡式的是 $q_t/Q^k_t$） | §3.12 |
| **F** | $L_t,\mathrm{PV}_t$：第二问是 **kW 功率**，第三、四问是 **kWh/时段 电量** | §3.13 |

---

## 一、统一写法（按五类分组，**带单位**）

### 1.1 索引与集合

| 统一写法 | 含义 | 单位 | 适用问题 | 备注 |
|---|---|---|---|---|
| $d$ | 计划日（自然日）编号 | — | 一~四 | 交付统计期 2025-02-01 至 2025-12-31 共 **334** 天；第四问回测含 1 月预热，$d=0,\dots,364$ |
| $t$ | **时段编号**（唯一编号，四问共用） | — | 一~四 | $\boldsymbol{t=1,\dots,144}$；时段 $t$ 覆盖 $\big[(t-1)\cdot10,\ t\cdot10\big)$ 分钟 |
| $\tau$ | 时段 $t$ 的**起点时刻标签** | 时:分 | 一~四 | $\tau_t=(t-1)\times10$ min；$\tau_1=0{:}00$、$\tau_2=0{:}10$、$\tau_{61}=10{:}00$、$\tau_{144}=23{:}50$（**本表无"终点标签"概念**，附件行标/模板列标均为起点） |
| $h$ | 0:00 的 145 段优化窗口编号 | — | 二、三、四 | $h=0,\dots,144$；$h=0$ 为已锁定午夜段。**专指该窗口，不兼作一般时段** |
| $k$ | **情景**编号 | — | 二、三、四 | $k=1,\dots,K$，等权 |
| $K$ | 情景数 | 个 | 二、三、四 | 三问：$K=30$；二问：$K=\lvert\Omega\rvert\in\{7,14\}$ 逐日校准；4-2 沿用二问的 $\{7,14\}$ |
| $m$ | 决策阶段编号 | — | 三、四 | $m=0,1,2,3$ ↔ 0:00 / 6:00 / 12:00 / 18:00 |
| $j$ | 历史日回溯序号 | — | 二、三、四 | $j$ 遍历情景取样的历史日 |
| $\ell$ | 情景历史滞后 | 天 | 三、四 | $m=0$ 时 $\ell=2$；$m\ge1$ 时 $\ell=1$ |
| $\mathcal R$ | 预报/价格发布时刻集合 | 时 | 三、四 | $\{0,6,12,18\}$ |
| $T^{\text{stg}}$ | 阶段起始时段向量 | — | 三、四 | $[0,35,71,107,144]$ |
| $\mathcal M_{\text{on}}$ | 实际启用的发布时刻集合 | — | 三、四 | 4-2：$\{0\}$；4-3：$\{0,1,2,3\}$ |
| $\Omega$ | 二问情景集合 | — | 二 | $\lvert\Omega\rvert\in\{7,14\}$ |
| $\mathcal W$ | 权重候选集 | — | 二 | $\{0,0.25,0.5,0.75,1\}$ |
| $W_d,R_d,V,V'$ | 二问预测校准样本集 | — | 二 | 同星期 4 天 / 近 7 天 / 验证 28 天与 7 天 |
| $\mathcal W^{\text{in}}_d$ | 第四问电价形态/水平滚动窗口 | 天 | 四 | 宽 20 天 |
| $\mathcal T$ | 时段的集合记号 | — | 一~四 | 如表2 的分块 $\mathcal T=\{144\}\cup\{1,\dots,23\}$ |

#### 1.1.1 时段编号统一：**四问的物理含义本来就相同，只是编号基数不同**

> 🚩 **2026-09-12 修订记录**：本节原结论为「数据行的标签是区间**终点**」，**方向反了**，已按交付产物实测
> 改为「**行标 = 区间起点**」。四条依据：
> ① `src/q1_solver.py:62` `natural_price = np.r_[raw_price[-1], raw_price[:-1]]`
> （自然日 00:00–00:10 取附件1 **末行** `0:00+1`，区间 $t$ 取原始行 $t-1$）；
> ② `src/q1_solver.py:185` 把「10:00-10:10」绑定到 `grid[60]`（60×10 = 10:00）；
> ③ `scripts/build_result1.mjs:25` 把 `grid[60]` 写进 **B61**，而 B61 的模板列标即 `10:00`；
> ④ 题面明文「表中的 `0:00+1` 表示第二天凌晨 0:00，即当天 24:00」，而该格装的是 **00:00–00:10** 那一段的量。
> 另：`outputs/q1/summary.json` 自述 `time_convention = "区间起点；原始行行标为区间起点"`，并把
> "行标解释为区间终点"的结果单独记为 `alternative_interval_end_reading`（**非交付口径**，35,126.9486 元）。
> ⇒ **交付值 35,126.8486 元属于「行标 = 起点」口径**；⚠️ 两支数字不得写混：
> **35,126.8486 = 交付；35,126.9486 = 反事实**。

这是本次核对中**最重要的一条发现**，也是四问合并时最容易"以为要平移、结果平移错了"的地方。

四份文件用了三套编号，但**指向的是同一批物理区间、同一批模板单元格**。
**附件1 的行标、以及模板行的 144 个列标，标记的都是该区间的「起点时刻」**
（第 1 列列标 `0:10`、第 60 列列标 `10:00`、第 144 列列标 `0:00+1`）：

| 物理区间 | 时段（统一 $t$） | 一（原式） | 二（原式） | 三 / 四（原式） | 模板数据列 | 模板单元格 |
|---|---|---|---|---|---|---|
| 0:00–0:10（午夜段） | 1 | $t=1$ | $t=1$ | $t=0$（模板行，即 $h=0$） | 第 144 列（**前一模板行**的末列） | B145（前一行末格） |
| 0:10–0:20 | 2 | $t=2$ | $t=2$ | $t=1$ | 第 1 列 | B2 |
| 10:00–10:10 | **61** | $t=61$ | $t=61$ | $t=60$ | 第 60 列（0 基 59） | B61 |
| 12:00–12:10 | **73** | $t=73$ | $t=73$ | $t=72$ | 第 72 列（0 基 71） | B73 |
| 23:40–23:50 | 143 | $t=143$ | $t=143$ | $t=142$ | 第 142 列 | B143 |
| 23:50–24:00 | **144** | $t=144$ | $t=144$ | $t=143$ | 第 143 列 | B144 |

**三处独立证据表明四问指向同一格：**

1. 表1 六个时段：一写明「10:00–10:10 ↔ 时段 $t=60$」、模板格 `B61`；
   二、三、四写明「六时段 ↔ 模板数据列第 60、72、84、96、108、120 **列**」（0 基 59…119），
   同为 `B61/B73/B85/B97/B109/B121`。**列号（数据列 1 基）比一、二问的 $t$ 小 1，与三、四问的 $t$ 相等。**
2. 表2 首块：一写「0:00–4:00 ↔ 时段集合 $\{144\}\cup\{1,\dots,23\}$」，
   即**当日 0:00–0:10 由前一模板行的第 144 列（列标 `0:00+1`）供给**，其余 23 段取本行第 1–23 列。
3. 数据本身：附件1 首行标签 `00:10`、末行 `0:00+1`。**标签标记的是该段的起点**，
   所以标签 `00:10` 的那一行是 **0:10–0:20** 这一段的数值；0:00–0:10 那一段由行标 `0:00+1` 的那一行供给
   （题面：「`0:00+1` 表示第二天凌晨 0:00，即当天 24:00」——24:00 正是末段的起始时刻）。

⇒ **统一结论**：采用 $t=1,\dots,144$（与一、二问一致、与"第几个时段"的自然语言一致），
时段 $t$ 覆盖 $[(t-1)\cdot10,\ t\cdot10)$ 分钟；**附件1 的行标 / 模板的列标 = 该时段的起点时刻**，
即行标 $s$ 的那一格对应区间 $[s,\ s+10)$。
若仍用 $\tau_t$，则 $\tau_t=(t-1)\times10$ 是**起点标签**（$\tau_1=0{:}00$、$\tau_{61}=10{:}00$、$\tau_{144}=23{:}50$）。
**第一、二问的编号原样保留；第三、四问的 $t$ 加 1 即为统一 $t$（$t_{\text{新}}=t_{\text{原}}+1$）。**

> **必须写进论文的一句口径话**：
> 「本文以**区间起点**标记每一段：时段 $t$ 覆盖 $[(t-1)\cdot10,\ t\cdot10)$ 分钟；
> 附件1 的行标与模板的列标**均为该段的起点时刻**（首列标 `0:10`、末列标 `0:00+1`），
> 自然日 0:00–0:10 段取**前一模板行的末列**。」
>
> ⚠️ 第一问 v7 文件的「**起点口径**」命名与交付产物**一致，论文可以直接引用这个词**；
> 需要补上它没写清的一句：统一时段 $t=1$（0:00–0:10）不由本行首列供给，而由**前一模板行末列**供给——
> 这正是表2 把 0:00–4:00 写成 $\{144\}\cup\{1,\dots,23\}$ 的原因。
> （本节早前的版本曾判 v7「物理上不自洽」，经 2026-09-12 复核，**该判断有误**：
> v7 的"起点"说法再加上跨行重组即完全自洽。）
> 本条属于**命名问题而非数值问题**：只改说法，不动任何结果。

### 1.2 参数（题面给定或模型标定）

| 统一写法 | 含义 | 取值 | 单位 | 适用问题 |
|---|---|---|---|---|
| $\Delta t$ | 时段长度 | $1/6$（10 分钟） | h | 一~四 |
| $T$ | 一天时段总数 | 144 | 个 | 一~四 |
| $H$ | 0:00 优化窗口长度 | 145 | 段 | 二、三、四 |
| $S^{\text{cap}}$ | 储能物理最大容量（额定） | 12000 | kWh | 一~四（**仅作容量标称说明，不进约束**） |
| $S_{\min},S_{\max}$ | 储电量运行下/上界 | 1200 / 10800 | kWh | 一~四 |
| $S_0$ | 日循环锚点 / 初始储电量 | 6000 | kWh | 一~四（**第一问含义见 §3.8**） |
| $P_{\max}$ | 储能最大充放电功率 | 5000 | kW | 一~四 |
| $C_{\max}$ | 单时段最大充/放电量 | $P_{\max}\Delta t=833.3333$ | kWh | 一~四 |
| $\eta$ | 充、放电效率（**各** 0.9，往返 81%） | 0.9 | — | 一~四 |
| $\alpha$ | **紧急购电电价倍数** | 5 | — | 二、三、四（**第一问无紧急购电**） |
| $\lambda$ | **终端储电量价值（水价）** | 三问与 4-3：0.478；二问与 4-2：0.33417 | 元/kWh（三、4-3）／ 元/**内部** kWh（二、4-2） | 二、三、四（**取值与量纲分歧见 §3.4**；第一问用硬锚点，不用 $\lambda$） |
| $\varepsilon$ | 吞吐惩罚系数（去 LP 多解退化） | $1\times10^{-7}$ | 元/kWh | **二、4-2 有；一、三问无** |
| $p_t$ | 第 $t$ 时段电价 | 附件1；0.3713–1.3952 | 元/kWh | 一、二、三 |
| $p^{\text{nat}}_t$ | 附件1 分时电价（自然日框） | 同上 | 元/kWh | 四（**替代原第三问的 $\pi_h$**） |
| $p^{\text{act}}_{d,t}$ | **结算**电价（附件4 实际价） | 0.0076–1.7936 | 元/kWh | 四 |
| $\hat p_{d,t}$ | **预测**电价（只进决策） | 式(10)–(13) | 元/kWh | 四 |
| $p^k_t$ | 情景 $k$ 的电价实现 | 式(14) | 元/kWh | 四 |
| $L_t$ | 负载（第二问**功率** kW；第三、四问**电量** kWh/时段） | 二：附件1/2；三、四：附件2 | kW 或 kWh | 二、三、四（**第二问与其他问单位不同，见 §3.13**） |
| $\mathrm{PV}_t$ | 光伏出力（第二问**功率** kW；第三、四问**电量** kWh/时段） | 附件1 / 附件2 | kW 或 kWh | 二、三、四 |
| $P_{\text{load},t},P_{\text{pv},t}$ | **功率**形式的负载 / 光伏 | 附件1 | kW | 一（**第一问额外用一套功率记号**） |
| $\widehat{\mathrm{PV}}^3_m$ | 附件3 未来第 $m$ 个整点光伏预报 | 附件3 | kW | 三 |
| $N_t$ | 净负荷电量 | $(L_t-\mathrm{PV}_t)\Delta t$ | kWh | 二（**非负约定见 §3.7**） |
| $\pi_k$ | 情景等概率权重 | $1/K$ | — | 二、三、四 |
| $\kappa_d$ | 二问残差窗口 | $\{7,14\}$ | 天 | 二 |
| $w_d$ | 二问星期权重 | $\{0,0.25,0.5,0.75,1\}$ | — | 二 |
| $\omega_j$ | 三问衰减均值权重 | $0.8^{\,j}/\sum 0.8^{\,j'}$ | — | 三（**原写作 $w_j$，与弃电 $w$ 撞名**） |
| $W^{\text{comb}}$ | 三问光伏两源组合权重 | $[0.4,0.6,0.9,0.4]$ | — | 三 |
| $\phi$ | 第四问电价残差 AR(1) 衰减 | 0.85 | — | 四 |
| $W^{\text{in}}$ | 第四问电价滚动窗口 | 20 | 天 | 四 |
| $X^{\text{fix}},X^{\text{nat}},X^{\text{raw}}$ | 一~四共用的**数据重排记号** | — | — | 一~四（见 §3.9） |

### 1.3 决策变量

| 统一写法 | 含义 | 单位 | 适用问题 | 原写法 |
|---|---|---|---|---|
| $x_t$ | 0:00 发布的**计划购电量** | kWh | 一、二、三、四 | 一作 $G_t$；二作 $G_{d,k}$ |
| $a_t$ | 阶段 $m\ge1$ 锁定的合约量 | kWh | 三、四 | 同 |
| $q_t$ | **最终生效合约量** | kWh | 三、四（一、二问 $q\equiv x$） | 同 |
| $q^{\text{cmt}}$ | 午夜 00:00–00:10 已锁定购电量（外生） | kWh | 三、四 | 二问作 $g^-_d$；一、二问无此内生化概念 |
| $Q^k_t$ | recourse 段仍可调整的合约量（逐情景） | kWh | 三、四 | 同 |
| $z^k_t$ | **紧急购电量**（逐情景） | kWh | 二、三、四（**一无需**） | 二作 $E^\omega_h$；四作 $z^k_h$ |
| $c^k_t$ | **充电量**（逐情景，交流母线侧） | kWh | 一、二、三、四 | 一作 $C_t$；二作 $C^\omega_h$；四作 $C^k_h$ |
| $g^k_t$ | **放电量**（逐情景，交流母线侧） | kWh | 一、二、三、四 | 一作 $D_t$；二作 $D^\omega_h$；四作 $D^k_h$ |
| $y^k_t$ | **剩余 / 弃电量**（不倒送） | kWh | 一、二、三、四 | 一作 $W_t$；二作 $U^\omega_h$；三作 $w^k_t$；四作 $\tilde w^k_h$ |
| $U^k_t$ | **超计划量** $(Q^k_t-x_t)^{+}$（线性化辅助） | kWh | 三、四 | 同（**保留 $U$，因为一、二问的 $U$ 已改名**） |
| $U^{0}_t$ | 阶段 $m\ge1$ 相对 0:00 原计划的超计划量 | kWh | 四 | 同 |
| $\bar c_h,\bar g_h$ | 参考充/放电轨迹（情景解等概率平均） | kWh | 二 | 原 $\bar C_h,\bar D_h$ |

> **情景上标的有无**：第一问是确定性 LP（无情景），其 $x_t,c_t,g_t,y_t$ 就是统一写法去掉上标 $k$；
> 第二问的 $t$ 是自然日编号、$h$ 是 145 段窗口编号，两套下标都在用，故其原符号带 $\omega,h$。

### 1.4 状态量

| 统一写法 | 含义 | 单位 | 适用问题 | 原写法 |
|---|---|---|---|---|
| $S^k_t$ | 储电量（SOC） | kWh | 一、二、三、四 | 一作 $S_t$；二作 $S^\omega_h$ |
| $S^{\text{cur}}$ | 阶段开始时的储电量 | kWh | 三 | 同 |
| $N^k_t$ | 情景 $k$ 的净负荷电量 | kWh | 二、三、四 | 三作 $L^k_t$ |
| $\widehat N_t$ | 净负荷中心预测 | kWh | 二 | 同 |
| $\mathrm{net}_t$ | 实时层缺电/富余判定量 $\mathrm{net}_t=N_t-q_t$ | kWh | 三、四 | 同 |
| $\varepsilon^{\text{res},k}_t$ | **历史残差轮廓分量** | kWh | 二、三 | **二问原写作 $\varepsilon^\omega_h$，与吞吐惩罚 $\varepsilon$ 撞名** |
| $\tilde u$ | 任一物理量在自然日框上的重组取值 | — | 三、四 | 同（$u\in\{x,q,c,g,z,y\}$） |

> ⚠️ **SOC 相位须知**（完整核对见 §3.10 陷阱 C）：
> 第一、二、三问的 $S$ 是「时段**开始**时」的储电量，**第四问是「时段末」**。
> 本表建议统一取**「时段末」口径**（$S_t$ = 时段 $t$ 末的储电量，$S_0$ = 0:00 初值），
> 则第一、二、三问的递推下标整体加一、第四问不动。
> 第一问的锚点原文写 $S_{145}:=S_1$（链条首尾相接），统一写法即 **$S_{144}=S_0=6000$**。
> **这只是改说法，不改任何数值。**

### 1.5 派生量（不设独立符号，直接给定义式）

| 派生量 | 定义式（统一记号） | 单位 |
|---|---|---|
| 计划购电量合计 | $\sum_d\sum_t x_{d,t}$ | kWh |
| 紧急购电量合计 | $\sum_{d,t}z_{d,t}$ | kWh |
| 紧急购电量占比 | $\dfrac{\sum z}{\sum x+\sum z}$ | % |
| 剩余/弃电量合计 | $\sum y$ | kWh |
| 光伏富余电量 | $\sum_{d,t}\max(-N_{d,t},0)$ | kWh |
| 净上调量 / 下调量 | $\sum(Q-x)^{+}$ / $\sum(x-Q)^{+}$（第三、四问） | kWh |
| **全天购电量** | $Q_d=\sum_t x_{d,t}$ | kWh |
| **全天购电费** | $F_d=\sum_t p_t x_{d,t}$ | 元 |
| 储能等效循环次数 | $\dfrac{\sum g}{S_{\max}-S_{\min}}$（**分母口径未统一，见 §3.5**） | 次 |
| 预测误差 | $\mathrm{MAE}=\frac1T\sum_t\lvert N^{\text{act}}_t-\widehat N_t\rvert$ | kWh/时段 |
| **双框重组（自然日 $t=1$）** | $\tilde u_{1}=u_{144}^{\text{prev}};\quad \tilde u_{t}=u_{t-1},\ t=2,\dots,144$ | — |

> ⚠️ **双框重组不是"取前一计划行的末列"这么简单**——两问的实现里藏着一个**错位**，
> 见 §3.10。上表是按"计划量"口径写的正确形式；**不能**把第三、四问原文的
> 「$\tilde u_{d,0}=u_{d-1,143}$；$\tilde u_{d,\tau}=u_{d,\tau-1}$」照抄过来，
> 那样在"购电量"这一个量上会**错一格**。

---

## 二、新旧符号对照表（论文手逐条替换用）

**读法**：把每一栏的「原写法」换成最右栏的「论文统一写法」。
**第一、二问改动最多**；第三、四问基本不动（只有弃电量 $w\to y$）。

### 2.1 计划/合同类

| 量 | 第一问原写法 | 第二问原写法 | 第三问原写法 | 第四问原写法 | **论文统一写法** |
|---|---|---|---|---|---|
| 计划购电量 | $G_t$ | $G_{d,k}$ | $x_t$ | $x_t$ | $\boldsymbol{x_t}$ |
| 午夜已锁定购电量 | — | $g^-_d$ | $q^{\text{cmt}}$ | $q^{\text{cmt}}_d$ | $\boldsymbol{q^{\text{cmt}}}$ |
| 最终合约量 | 无（$q\equiv x$） | 无（$q\equiv G$） | $q_t$ | $q_t$ | $\boldsymbol{q_t}$ |
| 阶段合约量 | — | — | $a_t$ | $a_t$ | $\boldsymbol{a_t}$ |
| 情景合约量 | — | — | $Q^k_t$ | $Q^k_t$ | $\boldsymbol{Q^k_t}$ |
| 超计划量 | — | — | $U^k_t$ | $U^k_t,U^0_t$ | $\boldsymbol{U^k_t,U^0_t}$ |

### 2.2 功率/电量/状态类 —— **第一、二问必须全改**

| 量 | 第一问原写法 | 第二问原写法 | 第三问原写法 | 第四问原写法 | **论文统一写法** |
|---|---|---|---|---|---|
| 紧急购电量 | —（无） | $E^\omega_h$ | $z^k_t$ | $z^k_h$ | $\boldsymbol{z^k_t}$ |
| 充电量 | $C_t$ | $C^\omega_h$ | $c^k_t$ | $C^k_h$ | $\boldsymbol{c^k_t}$ |
| 放电量 | $D_t$ | $D^\omega_h$ | $g^k_t$ | $D^k_h$ | $\boldsymbol{g^k_t}$ |
| 剩余/弃电量 | $W_t$ | $U^\omega_h$ | $w^k_t$ | $\tilde w^k_h$ | $\boldsymbol{y^k_t}$ |
| 参考充/放电轨迹 | — | $\bar C_h,\bar D_h$ | — | — | $\boldsymbol{\bar c_h,\bar g_h}$ |
| 储电量 | $S_t$ | $S^\omega_h$ | $S^k_t$ | $S^k_h$ | $\boldsymbol{S^k_t}$（**相位见 §3.10 陷阱 C**） |

> ⚠️ **$G/g$ 是最高危的一处**：第一、二问 $G$ 是计划购电、第三问 $g$ 是放电。
> 统一后 $x$ 管计划、$g$ 管放电，这两个字母在全篇不再兼职。
>
> ⚠️ **平衡式"看着一样、含义不同"**：四问统一后可写成
> $x_t+g_t+z_t-c_t-y_t=L_t-\mathrm{PV}_t$（第一问无 $z_t$），
> 但**各问的松弛变量 $y$ 语义不同、$x_t$ 的地位也不同**——见 §3.11 与 §3.12。
> **不要把这条式子当成"同一个模型"。**
>
> ⚠️ **$W$ / $w$ 有三个含义**：第一问弃电量 $W_t$ → $y_t$；
> 第三、四问情景弃电量 $w^k_t$ → $y^k_t$；
> 第三问参数衰减权重 $w_j$ → $\omega_j$；第二问星期权重 $w_d$ 保留。
> 统一后：**$y$ = 弃电量，$\omega_j$ = 衰减权重，$w_d$ = 星期权重，$W$ 只用于 $W^{\text{comb}},W^{\text{in}}$ 两个参数**。
>
> ⚠️ **$E$ 全线让给第一问的储能界**：第一问 $E_{\min},E_{\max},E_0$ 是储电量界与初值。
> 统一后一律改 $S_{\min},S_{\max},S_0$，$E$ 不再表示任何量。

### 2.3 参数/效率/目标类

| 量 | 第一问原写法 | 第二问原写法 | 第三问原写法 | 第四问原写法 | **论文统一写法** |
|---|---|---|---|---|---|
| 时段长度 | $\Delta t$ | $dt$ | $\Delta t$ | $\Delta t$ | $\boldsymbol{\Delta t}$ |
| 充、放电效率 | $\eta$（$\eta_c=\eta_d$） | $\eta_c,\eta_d$ | $\eta$ | $\eta$ | $\boldsymbol{\eta}$（同一常数 0.9，**必须注明"各"与母线侧口径**） |
| 储电量下/上限 | $E_{\min},E_{\max}$ | $S_{\min},S_{\max}$ | $S_{\min},S_{\max}$ | $S_{\min},S_{\max}$ | $\boldsymbol{S_{\min},S_{\max}}$ |
| 储能初值 / 日循环锚点 | $E_0$ | $S^{\text{init}}$ | $S_0$ | $S_0$ | $\boldsymbol{S_0}$（**第一问含义见 §3.8**） |
| 最大充放电功率 | $\bar P$ | $P_{\max}$ | —（用 $C_{\max}$） | —（用 $C_{\max}$） | $\boldsymbol{P_{\max}}$ |
| 单时段充放上限 | $\bar P\Delta t=5000/6$ | $E_{\max}$ | $C_{\max}$ | $C_{\max}$ | $\boldsymbol{C_{\max}}$ |
| **紧急购电电价倍数** | —（无） | $\lambda$ | $\alpha$ | $\alpha$ | $\boldsymbol{\alpha=5}$ |
| **终端储电量价值** | —（用硬锚点） | $v$ | $\lambda$ | $\lambda$ / $\bar v$ | $\boldsymbol{\lambda}$（见 §3.4） |
| 吞吐惩罚系数 | —（无） | $\varepsilon$ | —（无此项） | $\varepsilon$ | $\boldsymbol{\varepsilon}$（见 §3.3） |
| 自然日电价向量 | — | — | $\pi_h$ | — | $\boldsymbol{p^{\text{nat}}_t}$ |
| 情景概率权重 | —（无情景） | $\pi_\omega$ | $\pi_k$ | $\pi_k$ | $\boldsymbol{\pi_k=1/K}$ |
| 历史残差分量 | — | $\varepsilon^\omega_h$ | （式(21) 内联） | — | $\boldsymbol{\varepsilon^{\text{res},k}_t}$ |
| 光伏电量 | $PV_t$ | $V_t$ | $G_t$ | $G^k_h$ | $\boldsymbol{\mathrm{PV}_t}$（**不占用 $G$**） |
| 净负荷 | —（用 $L_t-PV_t$） | $N_t$ | $L_t-G_t$ | $L^k_h-G^k_h$ | $\boldsymbol{N_t=(L_t-\mathrm{PV}_t)\Delta t}$ |
| **全天购电量** | $P$ | —（见 §1.5） | —（见 §1.5） | —（见 §1.5） | $\boldsymbol{Q_d}$ |
| **全天购电费** | $F$ | $C^{\text{day}}_d$ | 式(8) | 式(8) | $\boldsymbol{F_d}$ |

---

## 三、必须写进论文的护栏（不写就会被质疑）

### 3.1 四问共用符号，逐问不必重述

交付包规格要求共用一套符号。**论文符号表只列一次**，
「来源」列标注「一~四问共用」或「仅第四问」。

### 3.2 双时间框：凡出现「全天」必须表态

- **第一、二问**：只有**自然日框**（统一时段号 $t=1,\dots,144$）。
- **第三、四问**：**双框并用**——计划/调整在**模板行框**（原文 $t=0,\dots,143$），
  物理量、费用、SOC 在**自然日框**，重组式见 §1.5。
- ⚠️ **两个框的合计不可相加**。凡写「计划购电量合计」「弃电量合计」，
  必须注明是模板行口径还是自然日口径。
- **两个框的精确对应（已核对一致，见 §1.1.1）**：模板行 $t_{\text{原}}$ 与统一时段号
  $t_{\text{新}}=t_{\text{原}}+1$ 同格；**模板数据第 $m$ 列 ↔ 统一时段 $m+1$（$m=1,\dots,143$），
  覆盖从 $m\times10$ 到 $(m+1)\times10$ 分钟**（第 1 列 = 0:10–0:20、第 60 列 = 10:00–10:10、第 143 列 = 23:50–24:00）；
  第 144 列（列标 `0:00+1`）覆盖 24:00–24:10，对应**次日的统一时段 1**，即当日自然日 0:00–0:10 段由**前一模板行**的第 144 列供给。
- ⚠️ **重组的下标相位见 §3.10 陷阱 B**：$x$ 与 $S,c,g,z,y$ 在双框下**相差一格**，
  不能用同一个 $\tilde u$ 一刀切。

### 3.3 $\varepsilon$ 的有无必须逐问声明

- **第二问、4-2 变体**：目标函数含 $\varepsilon\sum(c+g)$，$\varepsilon=10^{-7}$，
  作用是消除 LP 多解退化，**对费用数值无实质影响**，不计入对外报告的费用。
- **第三问**：明文**不使用**该惩罚项（素材包 §A1.1 原话）。
- **第一问**：不含该惩罚项（第一问用「最小化 $\sum(c+g)$ 作次级目标」处理多解，见 §3.8）。
- ⚠️ 论文若把四问写成「统一的滚动随机规划框架」，**必须在此处补一句
  "第三问不含吞吐惩罚项，因为……"**，否则公式与代码不符。

### 3.4 两个终端价值口径 —— 合并成同一符号后必须解释

| 问题 | 原写法 | 定义 | 取值 | 单位 |
|---|---|---|---|---|
| 第一问 | —（不用价值函数） | **硬锚点** $S_{144}=S_0=6000$ | 6000 | kWh |
| 第二问 | $v$ | $\eta\min_t p_t$ | 0.33417 | 元/**内部** kWh |
| 第三问 | $\lambda$ | 常数水价（$\approx$ 谷价 $0.43\div\eta$） | 0.478 | 元/kWh |
| 4-2 | $\bar v$ | 同第二问 $v$ | 0.33417 | 元/内部 kWh |
| 4-3 | $\lambda$ | 同第三问 $\lambda$ | 0.478 | 元/kWh |

- 统一为 $\lambda$ 后，**符号表必须加一行**：「$\lambda$ 为终端储电量价值；
  第二问与 4-2 取 $\lambda=\eta\min_tp_t=0.33417$ 元/内部 kWh，
  第三问与 4-3 取常数 $0.478$ 元/kWh」。
- 若嫌一节之内两值易混，可写 $\lambda^{\text{Q2}}$ 与 $\lambda^{\text{Q3}}$，
  但**正文必须显式声明二者是同一概念、取值口径不同**。
- ⚠️ 元/内部 kWh 与 元/kWh 是**两个口径**（差一个 $\eta$），不可混写。
- ⚠️ **$v$ 不再使用**：$v$ 与 $V_t$ / $\mathrm{PV}_t$ 在视觉上极易混，统一并入 $\lambda$。
- ⚠️ **第一问没有终端价值项**，它是**硬约束锚点**：题面只对问题 1 要求
  "0:00 与 24:00 储电量相同"，第一问就把它写成了等式约束。
  论文不可把第一问的锚点与第二、三问的 $\lambda$ 说成"同一种处理"。
  第一问实测：若改以 12000 为上限、去掉锚点，费用会变——**锚点是实质约束，不是软价值**。

### 3.5 等效循环次数的分母口径未统一

- 第二问：$\sum g/(S_{\max}-S_{\min})$，分母 **4800** kWh ⇒ 1041.5669 次。
- 第三问：$\sum g/(S_{\max}-S_{\min})$，分母 **9600** kWh ⇒ 584.0660 次；
  若分母取 4800 则为 1,168.1320 次。
- 第四问：素材包自报「分母口径未统一」。
- 第一问：全天充电 $\sum c=20740.6661$ kWh、放电 $\sum g=16799.9396$ kWh（**单日**，不可与全期数字并列）。
- ⇒ **论文里该指标要么全用 4800（可用容量），要么全用 9600，并注明所用分母**。
  这是四问之间唯一一处**数值口径**不统一（其余都是记号问题），不要漏。

### 3.6 紧急电价倍数：公式里不要出现裸数字 5

- 统一写法：目标函数与结算式一律写 $\alpha\,p$，并**先定义 $\alpha=5$**。
- ⚠️ 第四问素材包 §A3 假设 18 指出：**LP 内系数 $\alpha$ 与结算硬编码的 5
  是两个独立的量**，当前恰好都等于 5，故不影响结果；论文必须表态这一点。
- ⚠️ 第三问题面**未写倍数**，沿用第二问的 5 倍是**假设**，论文里要写成假设并给出灵敏度方向。
- 第一问**没有紧急购电**（确定性、无缺电惩罚），$\alpha$ 不出现。

### 3.7 净负荷 $N$ 的非负约定

- 第二问把 $N$ 定义为可正可负（负值 = 光伏富余），并从功率平衡中扣出 $y$（剩余电量）。
- 第三、四问把情景净负荷用 $\max(\cdot,0)$ **截断为非负**，富余另行由 $\max(-\cdot,0)$ 处理。
- 第一问**不定义 $N$**，直接用 $L_t,\mathrm{PV}_t$ 两个非负量；其弃电上界写成
  $0\le y_t\le \mathrm{PV}_t$（等价 $x_t+g_t\le L_t+c_t$，即"只许弃光"）。
- ⇒ 符号统一为 $N$，但**必须加脚注说明：第二问 $N$ 可负，第三、四问情景净负荷已截断为非负**。
  否则读者会以为 $N$ 的定义前后矛盾。
- 二问未截断口径下的富余电量：3,058,159.9162 kWh；三问弃电量：1,258,785.6073 kWh；
  一问全天弃电：0（主解中不弃电）。
  **三者口径不同，不可直接比大小**。

### 3.8 第一问的「全天购电量 $P$」与锚点 $S_0$ —— 必须一次说清

第一问有两个符号是全篇唯一的，合并时必须改掉：

| 第一问原符号 | 含义 | 冲突 | 统一写法 |
|---|---|---|---|
| $P$ | 全天购电量（派生输出量） | 与 $P_{\max}$（功率）视觉冲突 | $\boldsymbol{Q_d=\sum_t x_{d,t}}$ |
| $F$ | 全天购电费（派生输出量） | 与第四问电价形态因子 $\phi$、与费用 $C$ 命名体系冲突 | $\boldsymbol{F_d=\sum_t p_t x_{d,t}}$ |
| $E_{\min},E_{\max}$ | 储电量下/上限 | 与第二问 $E$（紧急购电）撞名 | $\boldsymbol{S_{\min},S_{\max}}$ |
| $E_0$ | 0:00 储电量 | 同上 | $\boldsymbol{S_0}$ |
| $\bar P$ | 最大充放电功率 | 与 $P$、$P_{\max}$ 冲突 | $\boldsymbol{P_{\max}}$ |
| $W_t$ | 弃电量 | 与 $w_d,W^{\text{comb}}$ 冲突 | $\boldsymbol{y_t}$ |

- **$S_0$ 在第一问是"日循环锚点"**（$S_{144}=S_0=6000$，即 0:00 与 24:00 储电量相同），
  在第二、三、四问是"初始储电量"。**同一个符号、两种地位**：
  第一问是**硬等式约束**，第二、三、四问是**外生初值 + 终端价值 $\lambda$**。
  论文符号表加一行脚注即可，但**不能在正文里混着说**。
- 第一问的多解处理：**不得**用事后"同减 $\min(c_t,g_t)$"修补（实测会破坏锚点、
  使 $S_{144}$ 由 6000 变 6175.93、并可越上界）。
  正确做法是**在最优面上以次级目标 $\min\sum_t(c_t+g_t)$ 重新求解并重跑断言**。
  这条要写进论文的"模型求解"小节，否则会被质疑"报告的解不满足约束"。

### 3.9 数据重排记号：$X^{\text{raw}}/X^{\text{nat}}/X^{\text{fix}}$ 四问同源

第一问 v7 与第三、四问规格共用同一套重排约定，**论文里应当只讲一次**：

- **附件1 是"重复日型"**：允许且**必须显式重排一次**——
  $X^{\text{fix}}[0]=X^{\text{raw}}[EO]$、$X^{\text{fix}}[j]=X^{\text{raw}}[j]$（$j\ge1$）。
  重排后时段 $t$ 就用附件第 $t$ 行，$t=144$ 即 $EO$（循环日下的当日 0:00–0:10）。
- **附件2/附件4 是"年度矩阵"**：自然日数组必须按绝对时间**跨行拼接**——
  $X^{\text{nat}}[d,0]=X^{\text{raw}}[d-1,EO]$、$X^{\text{nat}}[d,t]=X^{\text{raw}}[d,t-1]$。
- ⚠️ **禁止对附件2/4 的同一行做循环移位**：那会把本行的 $EO$
  （它代表**次日** 0:00–0:10）偷放到当天 0:00，造成整日错位与信息泄漏。
- ⚠️ 第二问素材包把这条写成 "$N=(\text{负载}-\text{光伏})\Delta t$" 并只提"自然日顺序"，
  **没有显式交代跨行拼接**。四问合稿时**以本节的显式公式为准**。

### 3.10 ⚠️ 双框对齐的三个陷阱（**最容易把公式写错的地方**）

这一节是本文**最重要**的一节。符号统一之后，公式看着一样了，
但**下标相位不一致**，照抄就会错。以下三条都已按代码与公式逐条核对过。

#### 陷阱 A · 第二问的 $h$ 与 $t$ 差一个偏移

第二问素材包对同一个下标 $h$ 用了**两种含义**，这是它自身的笔误：

| 位置 | 原文写法 | 实际含义 |
|---|---|---|
| §A1 式 (1) | $\sum_{h=0}^{144}5\,p_h\,E^\omega_h$ | $h$ = **自然日时段号**（0…144，但只有 1…144 有实际值） |
| §A1 式 (2)(3) | 平衡 / 递推里的 $h$ | $h$ = **145 段窗口的递推号**（0…144） |

代码（`q2_solver.py:180`，本分支 `5611746` 行号，逐字）是：
`objective[:planned_count] = np.r_[price_natural[1:], price_natural[0]]`
——**发布的 144 个计划量 $x_k$ 对应的价格是 $p_{k+1}$**（$k=0$ 配 $p_1$、…、$k=143$ 配 $p_{144}$）。

> ⚠️ 更要紧的是：**$p$ 是"每天相同"的循环电价**，所以 $p_{143}\leftrightarrow p_{144}$ 的差别
> **在费用数值上恰好不可观测**（这一点是运气，不是设计）。
> 论文必须按代码的正确相位写，不能因为"数值看不出"就写错下标。

⇒ **统一写法**：计划量一律记 $x_t$（$t=1,\dots,144$，自然日时段号）；
145 段递推一律记 $S_h,c_h,g_h,z_h,y_h$（$h=0,\dots,144$），二者关系

$$h = t-1,\qquad\text{即}\quad x_k\ \text{作用在}\ h=k-1\ \text{这一段的平衡里}$$

#### 陷阱 B · 双框重组：$x$ 与 $(S,c,g,z,y)$ 的相位**不一样**

第三、四问原文的重组式「$\tilde u_{d,0}=u_{d-1,143}$；$\tilde u_{d,\tau}=u_{d,\tau-1}$」
**只对物理量成立，对计划量 $x$ 不成立**。原因是代码里 $x$ 多了（或少了）一格偏移：

```python
# 第二问 q2_solver.py:312（本分支 5611746 行号，逐字）
natural_grid = np.r_[committed, published[:143]]
# 第四问 4-2 q4_q2_solver.py:98（逐字）
published=sol["grid"]; natural_grid=np.r_[committed,published[:143]]
# 第三问 q3_multistage.py:528 ／ 第四问 4-3 q4_solver.py:398（逐字）
natural_x = np.r_[committed_x, x[:T-1]]
```
> ⚠️ **行号与引文已按交付分支 `5611746` 核准（2026-09-12 复核）**：原引用的
> `q2_solver.py:1429`、`q2_solver.py:1561`、`q4_solver.py:187` 与
> `x_nat = np.concatenate([[committed], x[:143]])` **在该分支（及 master / q3、q4-handoff / f22c091 各版本）
> 均不存在**——`q2_solver.py` 全部版本只有 367–368 行，`src/` 中也查不到 `x_nat` 这个标识符。
> 陷阱的**结论（相位差一格）经复核仍然成立**，仅引文与行号有误，已换成本分支的真实位置。

⇒ 两侧都是「自然日第 1 段 = 前一日末列，**其余各段 = 本日已发布量整体右移一格**」：

| 统一自然日时段 | $x$（购电量） | 物理量 $c,g,z,y$、SOC $S$ |
|---|---|---|
| $t=1$（0:00–0:10） | 前一日已锁定 $q^{\text{cmt}}$ | 前一日末段值 |
| $t=2,\dots,144$ | $x_{t-1}$（本日已发布量 $x_1,\dots,x_{143}$） | 本段自身值 |

> **论文写法建议**：不要用「$\tilde u$ 一刀切」的写法。
> 直接写清"**计划量 $x$ 与物理量在双框下相差一格**"，
> 并给出上表。含混地用同一个 $\tilde u$ 会让 $x$ 的整体合计**错掉前一日末列、多算本日末列**。

#### 陷阱 C · SOC 的相位：第一、二、三问与第四问不同

| 问 | 原文递推 | 原文下标含义 | 统一写法的正确相位 |
|---|---|---|---|
| 一 | $S_{t+1}=S_t+\eta c_t-g_t/\eta$，$S_{145}:=S_1$ | $S_t$ = 时段 $t$ **开始时** | $S_0$ = 0:00 初值，$S_{144}$ = 24:00。**锚点写 $S_{144}=S_0$** |
| 二 | $S_{h+1}=S_h+\eta c_h-g_h/\eta$，$h=0,\dots,144$ | 代码 `soc[0]=initial_soc`、`soc[t]` 用于第 $t$ 段 ⇒ **时段开始** | $S_0=6000$，$S_{144}$ 传给次日。**一致** |
| 三 | $S^k_t-S^k_{t-1}-\eta c^k_t+g^k_t/\eta=0$，$S^k_{-1}=S^{\text{cur}}$ | 时段**开始** | $S^k_0$ = 阶段开始值。**一致** |
| 四 | $S_h=S_{h-1}+\eta C_h-D_h/\eta$ | **时段 $h$ 末** | ⚠️ **差一格**，见下 |

第四问按统一相位改写时，**必须整体平移一格**：

$$S_h=S_{h-1}+\eta\,C_h-\frac{D_h}{\eta},\quad h=1,\dots,144\ \Longrightarrow\ S_h=S^0_h\ (\text{统一"时段 }h\text{ 末"}),\quad S^0_0=\text{阶段开始值}$$

即第四问原文的 $S_0$ 是**统一写法的 $S_1$**、原文的 $S_{143}$ 是**统一的 $S_{144}$**。
**同一个字母 $S_0$ 在第四问指"第 1 段末"、在一二三问指"0:00 初值"，正好差一段。**

> ⇒ **论文里请统一采用「时段末」还是「时段开始」，然后四问全部按它写**。
> 本表建议：**取"时段末"**（$S_t$ = 时段 $t$ 末的储电量，$S_0$ = 0:00 初值），
> 这样 $S_0=6000$ 与 $S_{144}=6000$（第一问锚点）读起来最自然。
> 代价是**第一、二、三问的递推下标要整体加一**，第四问不动。

### 3.11 ⚠️ 平衡式"长得一样、语义不同"：盈余处置规则三问三样

符号统一后四个问题都能写成同一形状

$$x_t+g_t+z_t-c_t-y_t=L_t-\mathrm{PV}_t\qquad(\text{第一问无 }z_t)$$

**看着完全一样，但那只是巧合。** 逐条核对原始模型后，$y_t$ 的语义与边界条件**三问三样**：

| 问 | 原文平衡式 | $y$（松弛/剩余）的含义 | $y$ 有上界吗 | 物理后果 |
|---|---|---|---|---|
| 一 | $G_t+PV_t+D_t=L_t+C_t+W_t$（**等式**） | $W_t$ 弃电量，**只许弃光** | **有**：$0\le W_t\le PV_t$ | 买来的电和放出来的电**不许弃**；盈余卖不掉就必须不买 |
| 二 | $G+D+E-C-U=N$（**等式**） | $U$ 剩余电量，"供给超出需求且储能无法继续吸收的部分" | **无**：仅 $U\ge0$ | 可以**买了电再闲置**（已购未用）；光富余与多购的电**混在同一个 $U$ 里** |
| 三、四 | $\tilde q^{\text{eff},k}_t+z^k_t+g^k_t-c^k_t-w^k_t=L_t-G_t$（**等式**） | $w$ 弃电量，**不倒送** | **无**：仅 $w\ge0$ | 松弛变量吸收任何盈余，**不区分**"多购的"与"弃光的" |

> **⇒ 论文必须表态（三选一，建议第 2 条）**：
> 1. 承认四问用了**三种不同的盈余处置规则**，并说明各自理由与数值影响；
> 2. 统一成"**$y$ = 剩余电量，$y\ge0$ 无上界，且不区分来源**"——这是第二、三、四问的写法，
>    第一问只需**额外加**一条 $y_t\le\mathrm{PV}_t$ 并说明"本数据下该上界不紧"；
> 3. 统一成"只许弃光"——这会让第二、三、四问的模型**变了**，不可取。
>
> ⚠️ 特别提醒第一问：它的 $0\le W_t\le PV_t$ **在本数据上不紧**
> （单段最大盈余 352.92 kWh < 单段充电上限 833.33 kWh，且 $W\equiv0$ 重解费用不变），
> 所以"加了这条上界"与"没加"**费用完全相同**——但这**不代表**它和另外三问的模型等价。
> 两边的差别在于**多购的电能不能被"扔掉"**，第一问说不能、第二问说能。

### 3.12 ⚠️ $x_t$ 的**地位**不同：第一问是"实际购电"，第三、四问只是"0:00 的参考计划"

这一条决定了你**能不能把四问的目标函数写成同一个式子**。

| 问 | 进平衡式的是什么 | $x_t$ 的角色 | 调整罚项 |
|---|---|---|---|
| 一 | $x_t$ 本身 | **就是实际购电量**，无调整机制 | 无 |
| 二 | $x_t$ 本身（＝$G_{d,k}$） | **就是实际购电量**（第一阶段决策，日内不调整量、只补救） | 无 |
| 三、四 | $q_t$ 或 $Q^k_t$ | ⚠️ **只是"0:00 发布的参考计划"**，进平衡式的是**最终合约量** $q_t$（或情景合约量 $Q^k_t$） | **有**：$1.5p(q-x)^{+}+0.5p(x-q)^{+}$ |

第三问式(10) 逐字写作 $\tilde q^{\text{eff},k}_t+z^k_t+g^k_t-c^k_t-w^k_t=L_t-G_t$；
第四问式(20b)(20c) 写作 $x_t+\cdots$ 与 $Q^k_t+\cdots$。

⇒ **论文表述建议**：不要写"四问共用同一条平衡式"，
改写成「**第一阶段承诺量** $u_t$ 进平衡式」，并说明 $u_t$ 在各问的取值：

$$u_t=\begin{cases}x_t,&\text{第一、二问（无调整机制）}\\ q_t\ \text{或}\ Q^k_t,&\text{第三、四问（有调整机制）}\end{cases}$$

⚠️ 如果照搬我上一版写的"$x+g+z-c-y=L-\mathrm{PV}$ 四问通用"，
评阅人对照第三、四问的代码会立刻发现**进平衡式的不是 $x$**——这正是你担心的那类错误。

### 3.13 ⚠️ $L_t$ / $\mathrm{PV}_t$ 在第二问是**功率**，在第三、四问是**电量**

这是唯一一处**会直接造成量纲错误**的符号问题。

| 问 | 原文定义 | 单位 | 数值来源 |
|---|---|---|---|
| 二 | $L_t$ = 小区负载**功率**；$N_t=(L_t-V_t)\cdot dt$ | **kW** | 附件2（**实际**） |
| 三、四 | $L_t$ = 负载**电量**（kW × $\Delta t$） | **kWh/时段** | 附件2（**实际**） |
| 一 | 同时用两套：$P_{\text{load},t}$（功率，kW，附件1）与 $L_t$（电量，kWh） | kW 与 kWh | 附件1（**预测**） |

⇒ **统一写法**：把 $L_t,\mathrm{PV}_t$ 一律定义为**电量**（kWh/时段），
功率用 $P_{\text{load},t},P_{\text{pv},t}$ 表示。这样：
- 第三、四问不动；
- 第二问的 $L_t,V_t$ 改称 $P_{\text{load},t},P_{\text{pv},t}$，再乘 $\Delta t$ 得电量；
- 第一问 $L_t,\mathrm{PV}_t$ 保持电量定义。

⚠️ **另有两处要一并注意**：
1. **数据来源不同**：第一问用附件1（**预测**负载/光伏），第二、三、四问用附件2（**实际**）。
   符号统一了，来源没统一，论文必须逐问注明。
2. **第一问源文件自身有一处单位不一致**：$P_{\text{load},t}$ 定义为 kW 取自附件1，
   但正文又写 $P_{\text{load}}=L_t/\Delta t=3{,}555{,}643.93/6=592{,}607.32$ kW，
   与附件1 负载的 kW 量级（3309.39–5958.97 kW）对不上。
   论文**不要引用那个推导值**，直接写 $L_t=P_{\text{load},t}\Delta t$ 并给出附件1 的实测范围。

---

## 四、论文符号表排版建议

1. **单表 + 分组**：用本表 §1 的五组（索引与集合 / 参数 / 决策变量 / 状态量 / 派生量），
   每组一个小表，**四列：符号 / 含义 / 单位 / 取值或来源**（交付包规格第 487 行）。
2. **"来源"列**写附件号或"推导"或"建模假设"，不要写代码行号（那是素材包的事）。
3. **同名不同取值**（$\lambda$）与**有无之项**（$\varepsilon$）各加一行脚注。
4. **第四问的 $p^{\text{act}}/\hat p/p^k$ 要单独一行说明**：这是第四问唯一新增的符号区分，
   即「结算价 / 决策用预测价 / 情景价」。同时提醒：第四问式(6)(8) 里的 $p_t$
   **指结算价 $p^{\text{act}}_t$**，不要读成附件1 的分时价。
5. **式号**：四问各自独立编号（**二问 17 条**、**三问 24 条**、**四问 26 条**；
   第一问的公式分散在 §一 A1–A6 与 §三，**条数由你与论文手最终敲定**，本表不预设）。
   若论文连续编号且只含二、三、四问：二问 +0、三问 +17、四问 **+41**。
   ⚠️ **第四问素材包此处写错了**（`第四问_论文素材包.md:727`）：
   原文写「第二问 16 条 + 第三问 22 条 ⇒ 本问式号整体 +38」，
   但二问素材包自检行（`第二问_论文素材包.md:1749`）写的是“§A1 全部 **17** 个公式”，
   三问素材包 §A1 末行（`第三问_论文素材包.md:359`）写的是“式 (1)–(**24**)”。
   ⇒ 正确偏移是 **+17（三问）/ +41（四问）**。**以本条为准。**
6. **第一问的 $E_{\min},E_{\max},E_0,\bar P,P,F,W$ 六个符号必须改**
   （见 §3.8），它们与第二、三、四问的既有字母直接冲突。

---

## 五、我做过与没做过的事（自检）

- **做过**：逐条比对四份来源文件的符号表 / 全部编号公式 / 假设清单及其跨问对照表，
  提取每个符号的定义、单位、取值与出现位置，交叉核对冲突；
  并**逐条回读了原始代码**（`q2_solver.py` 载荷与自然日重组两处、`q3_multistage.py` 式(10)–(15)、
  `q4_solver.py` 式(20)–(21)）
  来核定平衡式、SOC 递推与双框重组的**下标相位**，结论见 §3.10–§3.13。
  > 🚩 2026-09-12 复核：原写的行号「`q2_solver.py` 第 1429–1455、1546–1584 行」不存在
  > （该文件全部版本仅 367–368 行），已改为按**函数/代码片段**定位；实测位置见 §3.10 的修订注。
- **修正了自己上一版的 4 处错误**（这些都是"符号统一之后公式逻辑不成立"的实例）：
  1. §2.2 原写"四问可统一写成 $x+g+z-c-y=L-\mathrm{PV}$"——
     **错在把 $x_t$ 当成进平衡式的量**，第三、四问进的是 $q_t/Q^k_t$（§3.12）；
  2. §1.2 原写"$L_t$ 在一、二问是功率"——**错**，第一问的 $L_t$ 是**电量**，
     功率另记 $P_{\text{load},t}$（§3.13）；
  3. §1.5 原照抄第三、四问的"$\tilde u$ 一刀切"重组式——
     **对 $x$ 错一格**，$x$ 与物理量的相位不同（§3.10 陷阱 B）；
  4. §1.4 原写"第一、二、三问是时段末、第四问是时段开始"——**写反了**，
     实际是一、二、三问为**时段开始**、第四问为**时段末**（§3.10 陷阱 C）。
- **没做过**：我没有改动四份来源文件里的任何符号，也没有重算任何数字；
  本文不含任何新的数值结论，所有取值均照抄自来源文件。
- **发现的两处来源文件自身的问题**（都不是数值问题，但论文不能照抄）：
  1. 第二问素材包对 $h$ 同时用作价格索引与递推索引（§3.10 陷阱 A）；
  2. 第一问 v7 里 $P_{\text{load}}=L_t/\Delta t=592{,}607.32$ kW 与附件1 的 kW 量级
     （3309.39–5958.97）对不上，建议不要引用该推导值（§3.13）。
  > 🚩 2026-09-12 复核更正：原第 1 条"第一问 v7 的「起点口径」命名与它自己的 B2/B145 映射、
  > 表2 分块不自洽"**判断有误、已删除**——v7 的"起点"命名与交付产物一致，
  > 0:00–0:10 段由前一模板行末列供给，与表2 的 $\{144\}\cup\{1,\dots,23\}$ 完全自洽（§1.1.1）。
- **待你拍板**：
  1. §1.5 等效循环次数的分母取 4800 还是 9600；
  2. 式号连续编号的偏移量；
  3. 是否接受把第一问的 $E_{\min},E_{\max},E_0,\bar P,P,F,W$ 与第二问的
     $G,C,D,E,U,v,\lambda,dt,E_{\max},V$ 全量改名
     （改名后四份来源文件的符号表与正文需要同步更新）；
  4. **已决**：「起点口径」名称保留（2026-09-12：经交付产物复核，"行标 = 区间起点"成立，§1.1.1 已按此改写）；
  5. **§3.10 陷阱 C 的 SOC 相位取"时段末"还是"时段开始"**（本表建议"时段末"）；
  6. **§3.11 的 $y$ 语义三选一**（本表建议"统一为无上界的剩余电量 + 第一问额外加 $y\le\mathrm{PV}$"）。
