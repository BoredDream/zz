# 微网数学建模项目后续任务参考文档

更新时间：2026-09-12（第四问完成并封印阶段C；陈旧报告归档与封存）  
项目根目录：`C:\Users\admin\Desktop\zz`  
当前阶段：**阶段A、B、C 均已完成；第三问、第四问结果已验收并生成官方工作簿**。  
下一步为**阶段D（论文与图表）**，其后是阶段E（最终交付审计）。

## ⚠️ 引用须知（2026-09-12 更新）

**现行第三问模型是 `src/q3_multistage.py`**（模板行框、0:00/6:00/12:00/18:00 四阶段、
题面结算口径），自然日交付期总费用 **13,162,682.89 元**。

**已失效、不得再引用**：旧模型 `src/q3_solver.py` 及其全部数字与结论
（年度总费用 14,743,406.7685 元、`6+12+18` 策略推荐、退款口径敏感性、
退款口径复算与唯一性检验），对应文件已移入 **`reports/_archive/`** 并加了封存横幅。

| 用途 | 现行权威来源 |
|---|---|
| 第三问数字（引用契约） | `reports/q3_paper_handoff.md` |
| 第三问结果表 | `reports/q3_report.md` |
| 第三问模型定义 | `docs/q3_multistage_model.md` |
| 第四问数字 | `reports/q4_report.md` |
| 旧模型（仅审计轨迹） | `reports/_archive/` |

**三项已知证据缺口，现已全部关闭**（详见 `reports/q3_paper_handoff.md` 第 5 节）：
① 题目要求的「预报时刻价值」对照（八套组合）**已完成**（2026-09-12，见下方 C″3）：
   全启用相对 `0-only` 节省 622,378.8759 元，三个时刻的条件边际全为正；
② 求解器退化与配置敏感性检验**已完成**（2026-09-12，见下方 C″6）：
   换算法最优值一致到 6.17e-14、端到端最坏差 28.21 元（0.0002%），
   主答案 13,162,682.89 元不是求解器伪影；但 LP 普遍退化（46.9% 解向量不同）；
③ 独立复算**已完成**（2026-09-12，见下方 C″7）：`scripts/verify_q3_multistage.py`
   不 import 模型，从原始附件重算结算、储能轨迹与八组合，偏差 0.000e+00。
   **三个缺口已全部关闭**；仍须保留的只是假设层面的限定（C″7 末列 5 条）。

此外，原先列在「其余未决项」里的**终端储能价值 λ 全年敏感性扫描**也**已完成**
（2026-09-12，见下方 C″8）：λ 取 0—3 倍时交付期费用极差 97,158.16 元（0.74%），
但费用对 λ **非单调** —— λ ≈ 0.36—0.48 之间有储能运行机制切换，交付所用的 λ = 0.478
正好在切换点右侧，**不能说某个 λ「更优」**。

本文档同时是任务台账：阶段A—C 的验收项已逐条填写实测值，阶段D—E 为待办路线。

---

## 0. 总体目标与执行顺序

后续工作严格按以下顺序推进：

1. ~~完成第三问断点续跑功能~~（已完成）；
2. ~~用真实附件数据完成第三问10套策略回测~~（已完成）；
3. ~~验收第三问结果并生成官方工作簿~~（已完成）；
4. ~~确定第三问论文主口径与敏感性口径~~（阶段B 已完成，但结论已被阶段B 之后的模型更换取代，见上方警告）；
5. ~~将第二、三问模型迁移到附件4波动电价，完成第四问~~（**阶段C 已完成并封印**，见下文）；
6. 汇总四问结果，完成论文、图表和最终PDF；
7. 做全项目一致性审计和最终交付。

任何后续阶段都不得绕过前一阶段的验收门槛。

---

## 0.1 第三问已完成结果快照（2026-09-12）

### ✅ 现行快照 —— 模型 `src/q3_multistage.py`

多阶段随机规划（0:00 制定计划，6:00/12:00/18:00 三次滚动调整未交付区间），
计划/调整采用**模板行框**，实际执行、SOC、紧急购电和费用跨行重组为自然日0:00–24:00；
交付期 2025-02-01—2025-12-31，共334个自然日。

| 指标 | 数值 |
|---|---:|
| 计划购电费/元 | 12,114,099.7164 |
| 调整相关费用/元 | 833,624.8961 |
| 紧急购电费/元 | 214,958.2730 |
| **自然日总费用/元** | **13,162,682.8855** |
| 紧急购电量/kWh | 38,737.7478 |
| 弃电量/kWh | 1,258,785.6073 |

完整结果表见 `reports/q3_report.md` 第 2 节；**论文引用一律以
`reports/q3_paper_handoff.md` 为准**（其中第 5 节列出两个证据缺口）。

### 🔒 已失效快照 —— 模型 `src/q3_solver.py`（下方全部内容）

> 以下内容描述旧模型，**数字与结论全部失效，不得引用**。
> 旧报告本体在 `reports/_archive/`。保留于此仅为审计轨迹。

模型版本 `q3_causal_rolling_saa_common_storage_v3`，交付期 2025-02-01—2025-12-31，334 个自然日。

输入附件 SHA256（检查点签名的一部分，重新运行必须一致）：

| 文件 | SHA256 |
|---|---|
| 附件1.xlsx | `66b87134f5ecccd68184d3539bb1293ef039f9e0fdd955a589b9bfa7f227c377` |
| 附件2.xlsx | `2e95fd446bfafa0d8c59577b5c2e2ea8b3f1def20dde54a3062556f4da9b4c72` |
| 附件3.xlsx | `8a61b06c52bd0d639a1cc37c61a7d9f5b75edcbca718f64c1bd3498ec9f9d843` |

### 主策略 `6+12+18`（自然日口径）

| 指标 | 数值 |
|---|---:|
| 计划购电量/kWh | 22,301,486.4665 |
| 最终合同购电量/kWh | 22,593,339.5741 |
| 非紧急结算费用/元 | 13,940,094.9488 |
| 紧急购电量/kWh | 209,501.6576 |
| 紧急购电费用/元 | 803,311.8197 |
| **总费用/元** | **14,743,406.7685** |

### 10 套策略对照（基线 `none` = 14,932,806.3979 元）

| 策略 | 总费用/元 | 节省额/元 | 节省率 | 紧急购电量/kWh | 累计上调/kWh | 累计下调/kWh |
|---|---:|---:|---:|---:|---:|---:|
| none | 14,932,806.40 | 0.00 | 0.0000% | 335,575.62 | 0 | 0 |
| 6 | 14,787,207.65 | 145,598.75 | 0.9750% | 229,798.00 | 250,147.46 | 0 |
| 12 | 14,786,018.21 | 146,788.19 | 0.9830% | 265,883.53 | 116,456.22 | 0 |
| 18 | 14,919,737.35 | 13,069.04 | 0.0875% | 332,426.42 | 8,364.70 | 0 |
| 6+12 | 14,749,689.43 | 183,116.97 | 1.2263% | 211,119.98 | 288,974.20 | 0 |
| 6+18 | 14,775,668.22 | 157,138.18 | 1.0523% | 226,468.67 | 257,074.50 | 0 |
| 12+18 | 14,779,775.94 | 153,030.46 | 1.0248% | 264,211.02 | 119,532.44 | 0 |
| **6+12+18** | **14,743,406.77** | **189,399.63** | **1.2683%** | **209,501.66** | **291,853.11** | **0** |
| 6+12+18_frozen0 | 14,922,766.51 | 10,039.89 | 0.0672% | 328,612.71 | 22,951.67 | 0 |
| 6+12+18_refund | 14,479,572.00 | 453,234.40 | 3.0352% | 235,278.64 | 357,209.12 | 923,674.39 |

### 三个必须说清的结论

1. **各发布时刻的边际价值均为正，但 18:00 最小。** 6→6+12 得 +0.2512 百分点，12→6+12 得 +0.2433，6+12→6+12+18 得 +0.0421。没有出现负边际价值，因此"建议弃用某一时刻"缺乏数据支持；但 18:00 的贡献确实微弱。
2. **预报价值几乎全部来自光伏预报更新。** `frozen0`（冻结 0:00 光伏预报但仍在 6/12/18 重优化）仅省 0.0672 个百分点，而 6+12+18 相对 frozen0 多出的 +1.2011 个百分点才是新预报信息价值。原因是日内**负荷中心预测不随发布时刻更新**（见 A9 限制 3），6/12/18 相对 0:00 唯一的新信息就是光伏预报。该局限会**低估**多时点预报的真实价值，论文中必须写明。
3. **主口径下下调恒为 0，是规则的经济后果而非代码限制。** 8 套主口径策略累计下调均为 0，而退款口径下调 923,674.39 kWh。已证明 `Q⁻≡0` 在"原计划费保留、不退款"下恒被占优，并由此得到恒等式 `pA+0.5p|A−G| = pG+1.5pQ⁺`，即"主策略固定电量按退款口径重算"（14,743,406.7685 元）与原口径**逐位相同**，不是复制错误；而按退款口径**重新优化**可自由下调，费用低 263,834.77 元（1.7895%）。证明见 `docs/q3_derivation.md` 第 4.1 节。

### 特殊现象

主策略有 **2,526 个时段**同时出现紧急购电与充电，涉及紧急购电量 **63,675.7793 kWh**（占紧急购电总量 30.4%）。核查结论：这些时段的充电量逐时段等于发布时锁定的共同储能轨迹，相对锁定轨迹最大偏差 0.000e+00 kWh（`all_overlap_charges_equal_locked_trajectory=true`），不是执行器自相矛盾，也未用贪心规则覆盖优化结果。成因见结论 2。按要求未删除、未改为贪心动作。

> ⚠️ 注意：上句「未用贪心规则覆盖优化结果」是**旧模型**的结论。现行模型的实时层
> **就是**贪心规则（`dispatch()`），该表述不可套用到现行模型。

<!-- 🔒 旧模型快照结束 -->

---

## 0.2 第一问已完成结果快照（2026-09-12 补做）

第一问此前在本仓库**没有代码、没有报告、没有产物**（`git ls-files` 中唯一的 `result1.xlsx` 是空白官方模板），
是 E2 的真实缺口，本次补齐。

### 模型

单日确定性线性规划。决策变量为购电量 `G_t`、充电量 `C_t`、放电量 `D_t`、弃电量 `W_t` 与储电量 `S_t`；
目标为 `min Σ p_t G_t`；约束为能量平衡 `G+PV−W+D = L+C`（等价于“供给不低于负载”）、
储电量递推 `S_{t+1} = S_t + 0.9C_t − D_t/0.9`、功率上限 `5000/6 = 833.33333… kWh`、
储电量范围 `1200—10800 kWh`，以及**周期条件 `S_0 = S_144 = 6000 kWh`**。

时间口径：附件1原始行行标即区间起点（`0:10`…`23:50`、`0:00+1`），与模板计划购电量表逐行对应；
自然日 `00:00` 按题设“每天相同”的周期日假设取原始行 `0:00+1`，**未做同一行循环移位**。

### 产物

| 文件 | 内容 |
|---|---|
| `src/q1_solver.py` | 求解器（附加分析的起点自由敏感性与区间终点反事实口径同文件内完成） |
| `scripts/build_result1.mjs` | 用官方模板生成 `result1.xlsx` |
| `scripts/validate_q1.py` | 独立验收（**26 项全部通过**） |
| `outputs/q1/result1.xlsx` | 交付工作簿（计划购电量 144 行 + 充放电量 6 段） |
| `outputs/q1/summary.json`、`solver_payload.json`、`interval_detail.csv` | 结果与明细 |
| `reports/q1_report.md` | 计算报告（9 节） |

### 关键数字（自然日口径）

| 指标 | 数值 |
|---|---:|
| 全天购电量 | 59,482.6990 kWh |
| **全天购电费** | **35,126.8486 元** |
| 总充电量 | 20,740.6661 kWh |
| 总放电量 | 16,799.9396 kWh |
| 0:00 / 24:00 储电量 | 6,000.0000 / 6,000.0000 kWh |
| 全天弃光量 | 0 kWh |
| 不配置储能对照费用 | 48,052.0466 元 |
| 储能节省 | 12,925.1980 元（26.90%） |

### 独立复核

1. **另一套编码 + `highs-ipm` 内点法重解**：把 `G = 净负荷 + C − D + W` 代入目标函数（`G≥0` 变成不等式约束），
   目标值与交付值相差 **+0.0000e+00 元**；
2. **明细级独立复算**：从 `interval_detail.csv` 重算购电量与购电费，与 `summary.json` 逐位一致；
3. **工作簿逐格核对**：`result1.xlsx` 与载荷一致，模板已有的 `D2='0:00'`、`D3='24:00'` 标签保留；
4. **与一份独立完成的对照标准逐项对照，22 项全部一致**（见 `reports/q1_report.md` 第 8 节）。
   唯一数值差异是最大电量平衡残差（对照 2.27e-13 / 本次 1.14e-13），已查明为**同一条约束的两种求和次序**，
   按 `购电+光伏+放电−负载−充电` 求和即得 2.2737367544323206e-13，属浮点舍入，不影响任何报送数字。
   建模口径的唯一差异是对照标准用二元变量显式写出充放电互斥，本次用线性规划；本解无同时充放电区间，
   两者最优解与最优值相同。

### 第一问尚存限制

1. 目标函数含 `1e-7` 元/kWh 吞吐量平局项，对购电费的影响为 **0.0038 元**（已在 `summary.json` 单列）；
2. 起点储电量固定为 6000 kWh 是“附录1给定初值 + 首末相等”的组合口径；若只保留首末相等而让起点自由，
   起点取 8550 kWh、费用 35,118.5986 元，**低 8.25 元**；
3. 区间终点反事实口径的全天费用为 35,126.9486 元（与交付口径差 0.10 元），
   但指定时段会整体错移 10 分钟，论文必须声明采用区间起点口径；
4. 储能不计折旧（题目未提供参数），不允许向外网售电。

---

# 阶段A：第三问（✅ 已完成，2026-09-12）

> 🔒 **本节描述的是已被取代的旧模型 `src/q3_solver.py`，其数字与结论全部失效。**
>
> 阶段A 完成时第三问用的是旧模型；之后阶段B 末尾整体更换为 `src/q3_multistage.py`。
> 本节保留原样是为了留下审计轨迹（谁在什么时候做了什么、踩过哪些坑），
> **但其中的费用数字、策略结论、产物路径均不得再被引用**。
> 特别是 A5/A6/A7/A8 里指向 `outputs/q3/`、`src/q3_solver.py`、
> 旧 `reports/q3_report.md`（9 节版）的内容，现行对应物分别是
> `outputs/q3_multistage/`、`src/q3_multistage.py`、`reports/q3_report.md`（已重写）。
>
> 现行权威来源见文首「引用须知」。旧报告本体在 `reports/_archive/`。

## A1. 阅读与接管 ✅

### 必读文件（全部已读）

1. `problem/statement.md`：赛题原文；
2. `reports/q3_review.md`：第三问独立审查；
3. `reports/q3_session_handoff.md`：上一会话交接；
4. `docs/q3_model.md`：修正版模型；
5. `docs/q3_derivation.md`：修正版推导；
6. `src/q3_solver.py`：当前求解器；
7. `scripts/build_result3.mjs`：工作簿生成程序；
8. `scripts/validate_q3.py`：验收程序。

### 必须确认

- [x] 当前 `outputs/q3/` 是旧模型产物，不作为新结果引用（旧产物已全部清除后重算）；
- [x] `reports/q3_review.md` 不得修改（本次未改动，`git diff` 已核对）；
- [x] 第二问文件不在本阶段修改范围（`outputs/q2/`、`src/q2_solver.py` 未改动）；
- [x] 工作树存在用户已有修改，不得执行破坏性恢复；
- [x] 不得把滚动SAA/MPC称为严格多阶段随机最优（`summary.json` 中 `strict_multistage_optimal=false`，验收脚本第 0 节强制校验）。

---

## A2. 增加检查点与断点续跑 ✅

### 实现

1. 每完成一套策略立即写入 `outputs/q3/checkpoints/<策略>.json`；
2. 检查点保存：策略名、365 天内部回测结果、334 天交付期结果、每次发布日志、SOC 与跨日合同状态、输入文件 SHA256、模型版本、参数签名、完成时间；
3. 启动时校验签名（`build_signature`）；
4. 只有输入、模型、参数完全一致时才复用；
5. 签名不一致时打印 `[checkpoint] 签名不一致，忽略 <策略>.json` 并重算；
6. 全部策略完成后统一汇总。

### 验收条件（逐条实测）

- [x] 任意完成一套策略后强制中止程序 → 再次启动能够跳过已完成策略：**实测**移走 `18` 的检查点后重跑，输出 `8 次复用 + 重算 12、18`，其余策略全部跳过；
- [x] 修改一个模型参数后，旧检查点被拒绝：**实测**篡改 `12.json` 签名为 `deadbeef…` 后重跑，打印"签名不一致，忽略 12.json"并重算；
- [x] 检查点加载结果与连续运行结果一致：**实测**10 个检查点齐备时重跑，10 次复用、0 次重算，`summary.json`/`solver_payload.json`/`policy_comparison_daily.csv`/`interval_detail.csv`/`daily_metrics.csv`/`release_log.json` 六个产物 **SHA256 逐字节一致**；重算得到的 `12.json`、`18.json` 与原检查点同样 SHA256 一致；
- [x] 检查点不能与旧版 `outputs/q3` 混用：旧 `outputs/q3` 已整体删除，检查点目录随本次重算新建。

### ⚠ 运维约束（后续每次改动必须遵守）

检查点签名包含 `src/q3_solver.py` 的 SHA256，**回测运行期间禁止编辑该文件**；任何编辑都会使 10 套策略的检查点全部失效并触发全量重算。本次实测全量重算耗时约 **9 分钟**（远低于原估的 25—35 分钟），因此"改代码→重算→重验收"是可接受的常规流程。

---

## A3. 第三问模型代码复核 ✅

### 核心模型要求（已满足）

同一发布时刻的所有情景共享购电量、充电量、放电量、SOC 轨迹；只有紧急购电量与弃电量随情景变化。由 `scripts/test_q3_units.py::test_non_anticipativity` 断言：两情景补救净额 (E−U) 不同（−460.0000 与 0.0000 kWh），而购电与储能动作只有一份。

### 必查公式（已核对）

能量平衡 `A_h + D_h + E_h^ω − C_h − U_h^ω = N_h^ω`；SOC 递推 `S_{h+1} = S_h + 0.9C_h − D_h/0.9`；物理边界 `1200 ≤ S_h ≤ 10800`、`0 ≤ C_h,D_h ≤ 5000·(1/6) = 833.3333… kWh`；调整恒等式 `A_h^k − Q_{h,k}^+ + Q_{h,k}^- = A_h^{k−1}`；调整费 `1.5p_hQ_{h,k}^+ + 0.5p_hQ_{h,k}^-`。

⚠ 注意：**833.3333 是四位小数的显示值，不是精确上界**。精确限值为 `5000 kW × 1/6 h = 833.33333…kWh`。验收脚本原先用 `833.3333 + 1e-9` 卡容差，会把恰好落在约束边界上的合法解误判为越界（本次已修，见 A6）。

### 必查时间映射（已核对）

- [x] 自然日数组：00:00、00:10、……、23:50（144 个）；
- [x] 原始附件行：00:10、……、23:50、次日00:00（144 列）；
- [x] 自然日 00:00 必须取上一原始行末列（`feb1_midnight_source = 附件2 2025-01-31 行末列(0:00+1)`）；
- [x] 模板一行覆盖 00:10 至次日 00:00；
- [x] 禁止同一行循环移位；
- [x] 自然日调整费用与模板行调整费用分别映射；
- [x] 模板末列（次日 00:00）调整费不得遗漏（通过 `B^π_{d,0} = π^{(d−1)}_{143}` 结转到下一自然日）。

### 小规模测试（`scripts/test_q3_units.py`，7 项全通过）

- [x] `py_compile` 通过；
- [x] 单日、单情景测试通过；
- [x] 多情景当前储能动作完全相同（`test_non_anticipativity`）；
- [x] 两个未来不同但当前观测相同的情景不能选择不同当前动作；
- [x] 计划、调整和执行使用同一 SOC 定义；
- [x] 实际执行不再调用贪心规则覆盖优化结果（`test_locked_trajectory_and_soc`：最大偏差 0.000e+00 kWh）。

### 本次复核修正的两处模型问题

1. **残差轮廓与中心预测必须使用同一负荷预测器（同一权重）。** `ForecastCache.scenarios` 原先对每个历史日使用该历史日自身的权重构造残差，而中心预测用计划日权重，两者不一致会使情景分布不代表实际预测误差。已改为统一使用计划日权重，并同步更新 `docs/q3_model.md`。
2. **终端价值的施加时点。** 终端储能价值施加于 24:00 时刻的 SOC `S_{H−1}`（与回测真正结转的 SOC 为同一状态），发布向量末区间（次日 00:00—00:10）的储能动作约束为 `C=D=0`，交由次日 0:00 模型决定。

---

## A4. 第三问 10 套策略真实回测 ✅

### 数据与范围

`problem/data/附件1.xlsx`、`附件2.xlsx`、`附件3.xlsx`；交付期 2025-02-01 至 2025-12-31，共 334 天、48,096 个 10 分钟时段。各策略独立维护 SOC、上一日计划末列、上一日最终合同末列、调整费用与每次发布的合同状态；未从主策略结果中事后删除预报时刻伪造对照。

### 必跑策略（10 套全部完成）

| 编号 | 策略 | 用途 | 状态 |
|---:|---|---|---|
| 1 | `none` | 仅0:00预报基线 | ✅ |
| 2 | `6` | 单独检验6:00预报 | ✅ |
| 3 | `12` | 单独检验12:00预报 | ✅ |
| 4 | `18` | 单独检验18:00预报 | ✅ |
| 5 | `6+12` | 两时刻组合 | ✅ |
| 6 | `6+18` | 两时刻组合 | ✅ |
| 7 | `12+18` | 两时刻组合 | ✅ |
| 8 | `6+12+18` | 主策略 | ✅ |
| 9 | `6+12+18_frozen0` | 冻结0:00光伏预报但仍滚动重优化 | ✅ |
| 10 | `6+12+18_refund` | 退款/最终净额口径重新优化 | ✅ |

**实测耗时：全量 10 套约 9 分钟**（原估 25—35 分钟，因 `ForecastCache` 在策略间共享而显著加快）。

### 运行命令

```powershell
$env:PYTHONPATH="src"; .\.venv\Scripts\python.exe -u -X utf8 src\q3_solver.py --data-dir problem/data --output-dir outputs/q3 --report reports/q3_report.md
```

---

## A5. 第三问结果文件生成 ✅

| 文件 | 内容 | 状态 |
|---|---|---|
| `outputs/q3/summary.json` | 主结果、10套策略汇总、数值校验、指定日期、调整结构 | ✅ |
| `outputs/q3/solver_payload.json` | 官方工作簿写入载荷（334 天） | ✅ |
| `outputs/q3/daily_metrics.csv` | 主策略 334 天指标 | ✅ |
| `outputs/q3/interval_detail.csv` | 主策略 48,096 条自然日明细 | ✅ |
| `outputs/q3/policy_comparison_daily.csv` | 10 套策略共 3,340 条逐日记录 | ✅ |
| `outputs/q3/release_log.json` | 主策略 1,336 条发布记录 | ✅ |
| `outputs/q3/release_logs/<策略>.json` | 全部 10 套策略的发布日志 | ✅ |
| `outputs/q3/baseline_none_interval_detail.csv` | 仅0:00基线明细 | ✅ |
| `outputs/q3/baseline_frozen0_interval_detail.csv` | 冻结预报基线明细 | ✅ |
| `outputs/q3/result3.xlsx` | 按附件5官方模板填写的最终结果 | ✅ |
| `reports/q3_report.md` | 第三问最终计算报告（9 节） | ✅ |

### 发布日志必含字段（已逐条核对 1,336 条记录）

策略名、日期、发布时刻、信息截止时刻、使用的预报来源、历史残差窗口、情景数、调整前合同、调整后完整合同向量、共同充电向量、共同放电向量、上调量、下调量、本次调整费用 —— 均在 `release_logs/*.json` 中，且验收脚本第 5 节强制校验向量长度、求解状态、情景数与费用合法性。

### 本次新增字段

- `summary.json.adjustment_structure`：各策略累计上调/下调量（用于证明 `Q⁻≡0`）；
- `summary.json.fixed_quantity_refund_resettlement_equals_primary`：固定电量退款重算恒等于主口径的布尔标记。

---

## A6. 第三问数值验收 ✅

执行命令：

```powershell
$env:PYTHONPATH="src"; .\.venv\Scripts\python.exe -X utf8 scripts\validate_q3.py
```

**十节全部通过，退出码 0。** 实测值：

### 数据规模

- [x] 10 套策略各 334 天；
- [x] `policy_comparison_daily.csv` 共 3,340 条；
- [x] 主策略共 48,096 条 10 分钟记录；
- [x] 两个重点基线各 48,096 条明细。

### 时间轴

- [x] 2025-02-01 00:00 实际净负荷 = **423.9600 kWh**；
- [x] 364 个可核对午夜均来自上一原始行末列，跨行映射最大误差 **0.000e+00 kW**；
- [x] 模板行与自然日合计不混用（报告与明细分列两套口径）；
- [x] 指定日期结果索引正确。

### 物理约束

- [x] 最大能量平衡残差 **2.274e-13 kWh** < 1e-7；
- [x] 最大 SOC 递推残差 **9.095e-13 kWh** < 1e-7；
- [x] SOC 范围 **1200.000000—10800.000000 kWh**（浮点残差 2.5e-12，容差内不越界）；
- [x] 单时段充放电峰值 **833.3333333 kWh** = 精确限值 `5000/6`，裕度 −1.14e-13（即恰好落在约束边界，合法）；
- [x] 无同时充放电（**0 个时段**）；
- [x] 跨日 SOC 连续（`cross_day_soc_continuity = true`）。

### 费用

- [x] 主口径可由"初始计划费 + 逐次调整费 + 紧急购电费"复算（由 48,096 条明细独立复算一致）；
- [x] 每次提交均有记录，中间修改没有被最终净额覆盖（1,336 条发布记录）；
- [x] 退款口径与主口径完全分开（两值相差 263,834.77 元，验收脚本强制校验差值 > 1 元）；
- [x] 自然日费用与模板行费用各自一致；
- [x] 汇总值等于逐日值之和；逐日值等于逐时段值之和；
- [x] 新增：固定电量退款重算恒等式由明细独立复算通过。

### 执行端与特殊现象

- [x] 执行充放电与发布锁定共同轨迹最大偏差 **0.000e+00 kWh**；
- [x] 紧急购电同时充电 **2,526 个时段 / 63,675.7793 kWh**，已核查全部来自发布时锁定的共同轨迹（见 0.1 节"特殊现象"）；
- [x] 未擅自改回贪心补救。

### 本次发现并修正的验收脚本自身缺陷（三处，均为脚本问题而非模型问题）

1. **限值容差写错**：把四位小数 `833.3333` 当精确上界配 `1e-9` 容差，使恰好落在边界上的合法解被判越界。已改为按 `5000/6` 精确限值判定，并打印峰值与裕度。
2. **发布时刻标签不匹配**：`updates_of` 用 `f"{int(hour):02d}:00"` 生成 `"06:00"`，而 `ISSUE_LABELS` 中是 `"6:00"`，索引必然抛异常（该分支此前从未执行过）。已改为不补零。
3. **工作簿取值行偏移**：充放电表第 1 行是表头，脚本却从第 1 行取首日储电量，把表头字符串 `"储电量"` 送去 `float()`。已改为 `2 + 6*position` / `3 + 6*position`。

**教训：验收脚本本身未经真实数据运行前不能视为可信。** 后续任何验收脚本都必须先在真实产物上完整跑通一次，再用于判定。

---

## A7. `result3.xlsx` 工作簿验收 ✅

生成命令：`node scripts/build_result3.mjs`

- [x] 官方模板结构保留（四张工作表名称一致：`计划购电量`、`调整购电量`、`充放电量`、`紧急购电量`）；
- [x] 日期为 2025-02-01 至 2025-12-31（334 行）；
- [x] 计划表与载荷逐项一致（146 列 = 144 时段 + 合计 + 费用）；
- [x] 调整表与最终合同逐项一致；
- [x] 计划费用和调整相关费用没有混淆；
- [x] 充放电表共 334×6 = 2,004 个四小时段，0:00 与 24:00 储电量逐日核对；
- [x] 紧急购电表与主策略明细一致（4,068 行）；
- [x] 无 `#REF!`、`#VALUE!`、`#DIV/0!`、`#N/A` 等错误（正则扫描 matched 0 entries）；
- [x] 四张表均**实际渲染**为 PNG 并逐张目视检查（`outputs/q3/previews/`）；
- [x] 表头、日期格式（`yyyy-mm-dd`）、数字格式（`0.0000`）、冻结窗格已设置。

注意：旧模型的 `outputs/q3/template_previews/` 渲染已随旧产物删除，现统一输出到 `outputs/q3/previews/`。

---

## A8. 第三问报告定稿 ✅

`reports/q3_report.md` 共 9 节：模型定位与结算口径、8 种预报组合比较、结算敏感性、指定日期计划与最终购电量、指定日期 4 小时段充放电、指定日期紧急购电、紧急购电同时充电核查、数值校验、尚存限制。

### 核心数字（已全部落盘并有独立复算）

- [x] 主策略非紧急结算费用 13,940,094.9488 元；
- [x] 主策略紧急购电量 209,501.6576 kWh；
- [x] 主策略紧急购电费用 803,311.8197 元；
- [x] 主策略总费用 14,743,406.7685 元；
- [x] 8 种预报组合总费用（见 0.1 节表）；
- [x] 相对仅0:00策略的节省额与节省率；
- [x] frozen0 基线 14,922,766.51 元；
- [x] 退款口径重新优化 14,479,572.00 元；
- [x] 主策略固定电量按退款口径重结算 14,743,406.7685 元（= 主口径，恒等式，已解释）。

### 四个指定日期（2025-03-20、2025-06-21、2025-09-23、2025-12-21）

每个日期均给出：六个指定 10 分钟时段的计划购电量与最终调整购电量、六个 4 小时段充电量与放电量、0:00 与 24:00 储电量、紧急购电时段与电量。详见 `reports/q3_report.md` 第 4—6 节与 `summary.json` 的 `target_dates`。

---

## A9. 第三问尚存限制（论文必须保留）📌

1. 0:00 计划没有用完整情景树联合定价未来 6/12/18 时的信息到达与调整机会，属滚动 SAA/MPC 近似（`strict_multistage_optimal=false`）。
2. 终端储能价值固定为 `λ=0.478元/kWh`，Q3与Q4-3已统一以保证横向比较不混入终端规则变化；该系数仍未做全面敏感性验证。
3. **日内负荷中心预测不随 6/12/18 更新**，只用发布前的历史轮廓。这是"1.27% 节省几乎全部来自光伏预报更新（frozen0 仅 0.0672%）"的直接原因，也是"紧急购电同时充电"的成因。该局限会**低估**多时点预报的真实价值。
4. 附件3只给整点光伏预报，10分钟值采用以发布时刻为锚点的PCHIP保形插值；0:00用上一已完成区间，其他发布时刻用当时可测量值。
5. 午夜购电锁定为前一日最后一次提交的次日 00:00 量，属合同时间假设。
6. 结算口径（逐次提交不退款、退款/最终净额）均需人工确认。
7. 预报精度（下 6 小时，每 10 分钟区间）：0:00 MAE 31.31 / RMSE 43.37；6:00 43.88 / 57.92；12:00 44.15 / 58.91；18:00 38.84 / 54.37 kWh（各 12,024 观测）。

---

# 阶段B：确定第三问最终采用策略（✅ 已完成，2026-09-12）

> 🔒 **本节结论已被推翻，数字与策略推荐全部失效。**
>
> 阶段B 在旧模型上比较了 8 套预报组合并推荐 `6+12+18`，还做了两轮独立复核。
> 但第三问此后整体更换为 `src/q3_multistage.py`：**新模型的结构本身即
> 0:00/6:00/12:00/18:00 四阶段，不存在「选哪几个时刻」的组合择优问题**，
> 且新模型**没有**重做本节的任何一项复核。
>
> **⇒ B1—B5 的内容不得作为现行第三问的证据。** 保留原样仅为审计轨迹。
> 相关文件（决策表、引用契约、退款复算、唯一性检验）已移入 `reports/_archive/`。
> 现行模型的证据缺口清单见 `reports/q3_paper_handoff.md` 第 5 节。

产物：`reports/q3_strategy_recommendation.md`、`outputs/q3/strategy_decision.json`、`outputs/q3/strategy_decision.csv`；
生成脚本 `scripts/q3_strategy_decision.py`（附加分析，不改动任何已验收产物）。
复核产物见 B5，引用契约见 `reports/q3_paper_handoff.md`。

## B1. 决策表 ✅

| 策略 | 使用时刻 | 发布次数 | 调整次数 | 调整天数 | 主口径总费用/元 | 主口径排名 | 退款口径总费用/元 | 退款口径排名 | 是否推荐 |
|---|---|---:|---:|---:|---:|---:|---:|---:|---|
| `none` | 仅0:00 | 334 | 0 | 0 | 14,932,806.40 | 8 | 14,932,806.40 | 8 | 基线 |
| `6` | 6:00 | 668 | 197 | 197 | 14,787,207.65 | 6 | 14,671,533.11 | 6 | — |
| `12` | 12:00 | 668 | 170 | 170 | 14,786,018.21 | 5 | 14,648,418.69 | 4 | — |
| `18` | 18:00 | 668 | 88 | 88 | 14,919,737.35 | 7 | 14,918,901.63 | 7 | — |
| `6+12` | 6:00、12:00 | 1002 | 331 | 231 | 14,749,689.43 | 2 | 14,485,906.57 | 2 | ◻ 并列候选（差 6,282.66 元） |
| `6+18` | 6:00、18:00 | 1002 | 268 | 222 | 14,775,668.22 | 3 | 14,658,547.79 | 5 | — |
| `12+18` | 12:00、18:00 | 1002 | 227 | 185 | 14,779,775.94 | 4 | 14,641,957.71 | 3 | — |
| **`6+12+18`** | 6:00、12:00、18:00 | 1336 | 379 | 239 | **14,743,406.77** | 1 | **14,479,572.00** | 1 | ✅ 首选 |

“并列候选”指主口径总费用落在最优值 0.1% 以内。`6+12+18_frozen0` 对照基线 14,922,766.51 元，不参与排序。

## B2. 边际价值与推荐表态 ✅

| 新增时刻 | 组合变化 | 主口径边际价值 | 退款口径边际价值 | 方向一致 |
|---|---|---:|---:|---|
| 12:00 | `6` → `6+12` | +0.2512 pp | +1.2431 pp | 是 |
| 6:00 | `12` → `6+12` | +0.2433 pp | +1.0883 pp | 是 |
| 18:00 | `6` → `6+18` | +0.0773 pp | +0.0870 pp | 是 |
| 18:00 | `12` → `12+18` | +0.0418 pp | +0.0433 pp | 是 |
| 18:00 | `6+12` → `6+12+18` | +0.0421 pp | +0.0424 pp | 是 |

**结论：采用全部三个预报时刻 `6+12+18`。** 8 种组合的边际价值在两个结算口径下**全部为正**，没有出现负边际价值，因此"建议弃用某一时刻"缺乏数据支持。相对 `6+12` 多一次发布换来 6,282.66 元（约 18.81 元/天），该权衡已在决策表中显式给出。

## B3. 结算敏感性的准确结论 ⚠

退款口径 8 套组合**已全部重新优化**（原先只有 `6+12+18` 一个做过）。两个口径下：

- **最优与次优组合一致**，均为 `6+12+18` 与 `6+12`；`none` 与 `18` 仍居末两位 → 推荐结论稳健；
- **但中间名次确有交换**：`12+18` 第 4 → 第 3、`12` 第 5 → 第 4、`6+18` 第 3 → 第 5。
  原因是退款口径下可自由下调，各组合受益幅度不同（低 0.006%—1.79%）。
  **论文若引用完整费用排序，必须注明所用结算口径**，不能笼统写"排序一致"。

## B4. 尚待补做 ⏳

- 终端储能价值系数（当前固定 `0.9×最低电价`）的敏感性扫描，检验结论是否稳健；
- 在论文中明确区分"模型局限导致的低估"与"预报本身价值有限"；
- ~~附加分析的独立验收~~（已完成，见 B5：退款口径 8 组合与主口径唯一性均已独立复核）。

## B5. 两轮独立复核 ✅（2026-09-12 补做）

产物：`reports/q3_refund_verification.md` + `outputs/q3_refund_verification.log`（复算脚本 `scripts/verify_q3_refund.py`）、
`reports/q3_uniqueness_check.md` + `outputs/q3/uniqueness_check.json` + `outputs/q3_uniqueness_check.log`（复算脚本 `scripts/q3_uniqueness_check.py`）。
两个脚本都是**附加分析**，不改动 `src/q3_solver.py` 与任何已验收产物。

### B5.1 退款口径 8 组合的独立复算 ✅

两条彼此独立的路径，均不复用 `totals()`/`assemble()` 的结算代码：

| 路径 | 做法 | 结果 |
|---|---|---|
| 甲 明细级独立会计复算 | 直接读检查点原始数组重算结算 | 8 组合与决策表差均在 **1e-8 元**量级 |
| 乙 独立线性规划重解 | 消元形式（`A = G + I − D`）+ `highs-ipm` | 重解 **4,008** 个调整时刻，最大目标值偏差 **2.547e-11 元** |

能量平衡残差 ≤ 2.3e-13 kWh，SOC 残差 ≤ 9.1e-13 kWh，SOC 全程在 1200—10800 内，无同时充放电。

**证明**：决策表中退款口径的年度总费用数字是对的，LP 编码与结算会计没有算错。
**未证明**：预测模型本身（两条路径共用 `ForecastCache`，未重新实现预测器），也未证明退款口径这个**假设**是否成立。

### B5.2 主口径最优解唯一性检验 ✅（本轮最重要的新发现）

方法：**强制回放**记录轨迹，使每个发布时刻的 LP 输入（情景、价格、初始 SOC、参考合同）与已验收运行逐位一致，
再对同一子问题用不同算法（`highs-ipm`）／打破平局扰动重解，变体解代回**未扰动**的原始目标函数评价。
回放重建的年度数字与 `summary.json` **逐位相同**（差 `+0.000e+00`），确认对比的确实是同一个子问题。

| 结论 | 数值 |
|---|---|
| 退化的发布时刻 | **611 / 1460（41.9%）**，其中调整子问题 447、0:00 计划子问题 164 |
| 记录解相对独立算法最大轨迹差异 | **6,283.1283 kWh**（2025-08-16 6:00） |
| 退化时刻的最大目标值偏差 | **+1.699e-03 元**（2025-03-06 6:00） |

**绝对费用水平依赖求解器配置**：目标函数一字不改，只把算法换成 `highs-ipm` 重跑整年，
年度总费用从 14,743,406.7685 元变成 14,712,890.3190 元（**−30,516.4495 元**）。
5 种配置的极差约 **33,298 元（约 0.226%）**。因此交付的 14,743,406.7685 元是
**特定求解器配置下的一次实现**，不是"该模型的唯一最优费用"。

**但推荐结论稳定**：同样 5 种配置下 `6+12+18` 对 `6+12` 的领先为
**6,282.6614—6,894.0848 元（0.0426%—0.0469%）**，**5 次全部由 `6+12+18` 胜出**。

**未证明**：逐时刻检验只覆盖 `6+12+18`，整年级联重跑另覆盖 `6+12`；
其余 6 套策略（`none`、`6`、`12`、`18`、`6+18`、`12+18`）未做同样检验。检验对象是 LP 编码本身，不构成建模正确性的独立验证。

### B5.3 给论文手的汇总文件

新增 `reports/q3_paper_handoff.md`，作为第三问数字的**引用契约**：可写结论、必须同时写明的 6 条限制、
两轮复核证明了什么／没证明什么、9 条"论文里不能写"。论文引用第三问数字一律以该文件口径为准。

⚠ 同一次核对发现：审查文件 `reports/q3_review.md` 第 8 节的日界差额（累计 64.8587 元／单日最大 94.5352 元）
已随模型重跑失效；按当前 `solver_payload.json` 重算为**累计 69.3161 元／单日最大 110.7990 元（2025-06-21）**。
`q3_review.md` 保持原样不修改（用户审查文件），但**其同项数字不得再被引用**。

---

# 阶段C：完成第四问 🔒 已封印（2026-09-12）

> **本阶段已结束并封印。** 下文 C1—C5 的原始条款保留原样并逐条标注实际处置，
> 新增 C6—C9 记录实际交付物、结果与验收。**若与本节任何旧表述冲突，
> 一律以 `docs/q4_model.md` + `reports/q4_report.md` 为准。**

## C1. 任务目标 ✅

将已经验收的第二问和第三问模型迁移到附件4的波动电价环境，分别生成：

- `result4-2.xlsx` ✅ `outputs/q4/result4-2.xlsx`
- `result4-3.xlsx` ✅ `outputs/q4/result4-3.xlsx`

## C2. 前置条件 ✅（全部满足）

- [x] 第二问结果已验收；
- [x] 第三问结果已验收；
- [x] 第三问最终策略已确定（阶段B）；
      ⚠️ **修正**：阶段B 的 `6+12+18` 对照口径已随第三问换模型而失效。
      现行第三问模型（`src/q3_multistage.py`）的发布时刻是 **0:00 / 6:00 / 12:00 / 18:00**
      四阶段（`TSTAGE = [0, 35, 71, 107, 144]`），不是 `6+12+18` 的三阶段对照。
      第四问的 4-3 沿用四阶段结构。
- [x] 附件4价格时间标签和单位已核对：365×144，单位 元/kWh，
      范围 0.0076–1.7936，均值 0.7662；第 `t` 列对应 `[(t+1)·10, (t+2)·10)` 分钟；
- [x] 第四问不得继续使用附件1固定电价误算：结算一律按 `PMAT[d]` 实际电价。

## C3. 模型迁移原则（原条款 + 实际处置）

> ⚠️ **原始 C3 的"保持不变"清单有 3 条与最终实现不符**，系因第三问在阶段B 之后
> 整体更换了模型（`src/q3_solver.py` → `src/q3_multistage.py`）。逐条修正如下。

原定"保持不变" —— **实际成立的部分**：

- ✅ 电池容量、功率（5000 kW）和效率（0.9/0.9），SOC ∈ [1200, 10800]，初值 6000 kWh；
- ✅ 信息截止规则（同一发布时刻的全部情景共享决策，不存在情景提前预知未来）。

原定"保持不变" —— **实际已变更的部分**：

| 原条款 | 实际情况 |
|---|---|
| 时间口径＝区间起点 + **跨行映射**（自然日 00:00 取上一原始行末列） | ✅ 已恢复：计划/调整保留模板行，实际执行、SOC、紧急购电和费用跨行映射为自然日0:00–24:00 |
| 实际数据跨行映射 | ✅ 已实现；自然日六段按跨行后的实际轨迹汇总 |
| 因果执行方式＝**严格按发布锁定轨迹，不用贪心覆盖** | ❌ 实时层是**贪心规则**：给定 `q` 后唯一自由度是"放电 or 紧急购电"，`dispatch()` 按 `min(net, CMAX, (S−SMIN)·η)` 放电，余量走紧急购电。（实测"按计划 SOC 设预留下限"反而更差，因下限建立在偏乐观的光伏预报上） |
| 第三问结算规则及敏感性结构 | ❌ 结算改用**题面口径** `C = p·min(x,q) + 1.5p(q−x)⁺ + 0.5p(x−q)⁺ + 5p·z` |

原定"需要替换或重新评估" —— 实际处置：

| 原条款 | 实际处置 |
|---|---|
| 计划购电目标函数中的价格 | ✅ 新建两因子电价预测 `p̂ = 形态 × 水平 × 日内 AR(1) 修正`（`PHI=0.85`, `WIN=20`） |
| 紧急购电价格（5×当时电价） | ✅ 保持 `α=5`，但改按**场景内** `p^k` 计（与价格同场景配对） |
| 调整费用（1.5p / 0.5p） | ✅ 保持；因违约价/紧急价都定义为"交易时刻电价"的倍数，相对价格结构不变，**LP 无需改写** |
| 储能套利行为 | ✅ 已比较，见 C4 第 7 项（结论与直觉相反） |
| 终端储能价值（`0.9×min p`） | ✅ Q3与Q4-3统一为`λ=0.478元/kWh`；4-2沿用Q2，保证两组对应比较各自不混入终端规则变化 |
| 预报更新的经济价值 | ⚠️ **未做**，见 C4 第 6 项 |

**第四问特有的新建模点**（原始 C3 未预见）：目标里出现 `E[p·q]`，同一阶段的
here-and-now 决策不带场景下标、系数须取期望电价，而 recourse 变量与价格同场景配对。
⇒ 报童**概率**分位数规则推广为**价格加权**分位数规则 `E[p·1{q>x}] = 0.5·E[p]`。

## C4. 必须比较（逐条销账）

| # | 条款 | 状态 | 落点 |
|---|---|---|---|
| 1 | 固定电价与波动电价总费用 | ✅ | 13,162,682.8855 → 13,847,794.9401 元（**+5.20%**）；q4_report §3 |
| 2 | 计划购电量变化 | ✅ | 20,140,461.0054 kWh（4-3模板行）；q4_report §2 |
| 3 | 充放电时段变化 | ✅ | 六个 4 小时段逐段对照；q4_report §5 表2 |
| 4 | 紧急购电量变化 | ✅ | 38,737.7478 → 41,691.4524 kWh（**+7.62%**）；q4_report §3 |
| 5 | SOC 轨迹变化 | ✅ | 每日自然日「0:00储电量 / 24:00储电量」；q4_report §5 表2 |
| 6 | 第三问各预报时刻价值是否改变 | ⚠️ **本次重算范围内未做** | 第四问仍**未做**该对照（经用户决定移出本次重算范围）；**第三问侧的同类对照已在阶段C″补齐**（见 C″3）。注：第四问的日内更新价值另有 `docs/q4_model.md` §10 第 3 条单独消融（结论：18:00 更新按 MAE 是负收益、按费用几乎中性），但那是**更新本身**的价值，不是**各发布时刻组合**的对照 |
| 7 | 电价波动是否导致更多低价充电、高价放电 | ✅ | **否**。Q3与Q4-3充电量 6,921,852.8792 → 6,647,718.7293 kWh（**−3.96%**），放电量同幅下降。q4_report §3 |

## C5. 特别警告 ✅ 已遵守

- 统一比较期间：全部结论取**交付期 2025-02-01—12-31，334 天**，
  未将 365 天全年结果与 334 天结果混比；
- 每个"增加/下降"结论均在 `reports/q4_report.md` 注明时间范围、对照对象、
  电价口径、单位，以及是否含紧急购电费与调整费。

⚠️ 后续引用须注意：`src/q4_solver.py` 的 `backtest4()` 默认跑 **365 天**
（预热 30 天用），报告与工作簿一律只取 **334 天交付期**。二者不可混用。

## C6. 实际交付物

| 类别 | 文件 |
|---|---|
| 模型说明 | `docs/q4_model.md`（11 节） |
| 实现 | `src/q4_solver.py` |
| 全年回测 | `scripts/export_q4.py <2\|3> [K] [stages]` |
| 工作簿生成 | `scripts/build_result4.mjs <2\|3> [K]` |
| 独立验收 | `scripts/validate_q4.py <2\|3> [K]`（19+ 项检查） |
| 报告生成 | `scripts/report_q4.py` → `reports/q4_report.md` |
| 消融证据 | `docs/_ablate_q4_m3.py` |
| 产物 | `outputs/q4/result4-{2,3}.xlsx`、`payload_q4-{2,3}_K30.json`、`detail_q4-{2,3}_K30.npz`、`summary_q4-{2,3}_K30.json`、`previews/` |

## C7. 结果快照（交付期 334 天，K=30）

| 项目 | 4-2（对应问题2） | 4-3（对应问题3） |
|---|---:|---:|
| 计划购电费 | 13,489,775.86 元 | 12,740,081.27 元 |
| 调整相关费用 | 0 | 878,485.24 元 |
| 紧急购电费 | 1,712,339.27 元 | 229,228.43 元 |
| **自然日总费用** | **15,202,115.13 元** | **13,847,794.94 元** |
| 紧急购电量 | 272,838.0735 kWh | 41,691.4524 kWh |
| 含紧急购电的日期 | 170 / 334 | 146 / 334 |

**Q3型四阶段策略（4-3）相对Q2型一次决策（4-2）全年省1,354,320.19元（8.91%）**，
紧急购电量下降84.7%，紧急购电日期由170天降至146天。两者预测器也分别继承Q2和Q3，
因此不能把全部差额单独归因于日内滚动调整。

## C8. 验收证据

- `scripts/validate_q4.py` 两个变体各跑一次，全部通过：
  功率平衡残差 ≤ 2.274e-13 kWh、SOC 递推残差 0、充放电不越限、SOC 不越界、
  三项费用拆分与总费用之差为0；
- 逐格核对两个工作簿全部单元格（334 天 × 144 列 × 2 张价格表 + 全天合计列 +
  表2 六个段 + 紧急购电时段），**偏差 0**；
- 断言结算价等于附件4 实际电价；断言 4-2 的 `max|q − x| = 0`；
- `reports/q4_report.md` §7 由报告脚本独立重算一遍关键不变量，与验收脚本一致。

## C9. 遗留与不得越界的表述

- ⚠️ **与第三问的 +5.20% 不可整体归因于"价格波动"**：第三问把附件1 电价当**已知**量
  送入 LP，第四问必须在 0:00 **预测**。该差额同时含 (a) 波动风险成本 (b) 预测误差
  信息缺失成本。拆分需再做一次"完美预见电价"对照回测，**本次未做**。
- ⚠️ **四个指定日期中有1天（2025-03-20）4-3当日费用高于4-2**。
  滚动调整按**期望**降本，不保证逐日更优；一律按全周期合计表述。
- ⚠️ 电价预测为两因子 + 日内 AR(1)，未用任何外部信息；场景取自历史残差重采样、
  未做同月分层，尾部风险可能被低估；残差截断在 [0.3, 3.0] 倍。
- ⚠️ 名为多阶段实为"滚动两阶段"，`strict_multistage_optimal=false`，
  **不宣称严格多阶段随机最优**。

---

# 阶段C′：陈旧报告归档与封存 ✅（2026-09-12）

## C′1. 起因

第三问在阶段B 之后整体更换了模型（`src/q3_solver.py` → `src/q3_multistage.py`），
但 `reports/` 下**没有任何文件**记录现行数字，反而有 6 份报告仍以旧模型的口径
描述第三问。其中 `reports/q3_paper_handoff.md`（论文引用契约）第 1 节直接给出
已失效的 14,743,406.7685 元，`reports/q3_report.md` 则是最像"第三问正式报告"的文件。
若直接进入阶段D，论文会引用一整批作废数字。

## C′2. 处置

| 文件 | 处置 |
|---|---|
| `q3_report.md` | **重写**为现行版（由 `scripts/report_q3_multistage.py` 从 payload 机器生成） |
| `q3_paper_handoff.md` | **新建**现行引用契约（含第 5 节「证据缺口」） |
| `q3_report.md`（旧）、`q3_paper_handoff.md`（旧）、`q3_strategy_recommendation.md`、`q3_uniqueness_check.md`、`q3_refund_verification.md`、`q3_session_handoff.md` | **移入 `reports/_archive/`** 并加封存横幅 |
| `q3_review.md`（用户审查文件） | **原位加横幅，正文一字未改** |
| `q2_review.md` | **不动**（其「跨行映射」结论对第二问仍然成立） |

加横幅与校验由 `scripts/_seal_archive.py`、`scripts/_verify_seal.py` 完成；
后者逐字节比对，确认 **7/7 个文件的正文与归档前完全一致**（只多了横幅）。

## C′3. 新暴露的两个证据缺口 ⚠️

归档过程中确认，现行第三问模型有两处**从未补做**的工作（详见
`reports/q3_paper_handoff.md` 第 5 节）：

1. **题目要求的「预报时刻价值」对照（`none/6/12/18/…` 八套组合）现行模型未做** ——
   旧模型做过，但结论随模型更换失效。**这直接关系到第三问能否回答题面问题3 第二段末句。**
2. **现行模型未做独立复核** —— 旧模型的两轮复核（退款口径 8 组合复算、
   最优解唯一性检验）均未在新模型上重做；现有 `validate_q3_multistage.py`
   只证明「编码与已验收产物自洽」，不是独立复算。

**这两项是进入阶段D 的前置条件**，处理方案待定。
**后续：第 1 项已在阶段C″3 补齐、第 2 项已在阶段C″7 补齐（均 2026-09-12）；两项均已关闭。**

---

# 阶段C″：补齐预报发布时刻组合对照 ✅（2026-09-12）

## C″1. 起因

`reports/q3_report.md` 第 8 节原第 1 条限制白纸黑字写着「现行模型只跑过
`stages=(0,1,2,3)` 一种配置，因此**本报告不能回答该问题**」——
而「是否需要引入其他时刻的预报制定调整购电策略」是题面问题3 第二段末句，
属于必须回答的交付项，不是可选敏感性。这是当时最致命的缺口。

## C″2. 建模修正（先修口径，再跑对照）

`src/q3_multistage.py` 原实现里，禁用某个发布时刻后，上一次发布仍只调整固定的
下一个 6 小时块，`[t_next, 144)` 会**悄悄退回 0:00 计划** —— 既不许可调整、
又不延续上一版计划，会系统性低估被禁用预报的价值。修正为统一规则：

> 第 `m` 阶段锁定 `[TSTAGE[m], TSTAGE[m_next))`，`m_next` 为**下一次实际启用的**
> 发布时刻；后面没有已启用时刻则负责到当日结束。

改动对主答案 `stages=(0,1,2,3)` **逐位无影响**（该集合下 `m_next` 恒等于 `m+1`）。
已做回归确认：前 6 日的 `x/q/c/g/z/w/S` 与改动前产物 `detail_stages0123_K30.npz`
逐位相差 `0.000e+00`。另加 `stages` 合法性校验与承诺区间越界报错。

模型文档同步见 `docs/q3_multistage_model.md` 第 1.1 节。

## C″3. 交付物与结果

| 产物 | 说明 |
|---|---|
| `outputs/q3_multistage/summary_stages{0,01,02,03,012,013,023,0123}_K30.json` | 八组合各一份（334 天，K=30） |
| `outputs/q3_multistage/q3_stage_comparison.json` / `.csv` | 汇总、条件边际、Shapley 分摊 |
| `scripts/assemble_q3_stage_comparison.py` | 汇总脚本，**逐份校验**八组的情景数、储能口径与交付期起止同口径 |
| `reports/q3_report.md` 第 4 节 | 报告正文（由 `scripts/report_q3_multistage.py` 机器生成） |

| 策略 | 总费用/元 | 相对 `0-only` 节省/元 |
|---|---:|---:|
| `0+6+12+18`（主策略） | 13,162,682.8855 | 622,378.8759（4.515%） |
| `0+6+18` | 13,286,554.0877 | 498,507.6737 |
| `0+12+18` | 13,288,096.3270 | 496,965.4344 |
| `0+18` | 13,376,118.1166 | 408,943.6448 |
| `0+6+12` | 13,388,526.3278 | 396,535.4337 |
| `0+12` | 13,512,386.6663 | 272,675.0951 |
| `0+6` | 13,624,463.6825 | 160,598.0789 |
| `0-only` | 13,785,061.7614 | — |

Shapley 分摊：6:00 **130,907.90 元（21.0%）**、12:00 **186,175.29 元（29.9%）**、
18:00 **305,295.68 元（49.1%）**，三者之和恒等于 622,378.8759 元。

**结论**：全部 12 个条件边际均为**正**（最小值 88,021.79 元），
即**三个时刻都值得引入、没有哪个应当舍弃**；排序 **18:00 > 12:00 > 6:00**。
价值高度可替代，单时刻节省**不可相加**。

## C″4. 不得越界的表述

- 该节省度量的是「在该时刻**重新制定计划的能力**」的价值，不是预报精度本身的价值；
- 是**同一套 SAA 情景、同一 `λ`、同一初值**下的**模型内条件结论**，
  换预测器 / 情景数 / `λ` 都会改变数值；
- `0-only` 的 13,785,061.7614 元是**本模型**的「只发布一次」退化结果，
  **不是问题 2 的 14,455,427.6340 元** —— 两者预测器与情景生成方式不同，不可相减。

## C″5. 仍未完成

- ~~现行模型**独立复核**（C′3 第 2 项）仍未做~~ —— **已完成（2026-09-12）**，见下方 C″7：
  由不 import 模型的第二套实现从原始附件重算结算、储能轨迹与八组合；
- ~~求解器配置敏感性未在现行模型上重做~~ —— **已完成（2026-09-12）**，见下方 C″6。

⇒ 阶段C″ 的三项缺口已全部关闭；剩余未决项见文末「其余未决项」。

---

## C″6. 求解器退化与配置敏感性检验 ✅（2026-09-12）

脚本 `scripts/q3_solver_uniqueness_check.py`。**不修改 `src/`**：现行代码是
`linprog(c, ..., method='highs')`，`method` 为关键字参数，脚本替换模块命名空间里的
`linprog` 名字即可换算法，主答案路径一字未动。

**（1）整年端到端对照**（334 天交付期）：

| 算法 | 交付期总费用/元 | 与主答案之差/元 | 逐日最大偏差/元 | 偏差 > 1 元的天数 |
|---|---:|---:|---:|---:|
| `highs`（主答案） | 13,162,682.8855 | — | — | — |
| `highs-ds`（对偶单纯形） | 13,162,682.8855 | 0.0000（逐位相同） | 0.0000 | 0 |
| `highs-ipm`（内点法） | 13,162,654.6768 | −28.2087（−0.000214%） | 41.8873（2025-11-29） | 3 |

**（2）LP 级普查**（2025-02-01 起连续 329 天共 1316 个发布 LP，各用 3 种算法重解）：
最优**值**跨算法一致到 6.17e-14 相对量级，**无一个** LP 超过 1e-9；
最优**解向量**不同的 (LP, 算法) 对 1234 / 2632（46.9%），全部来自内点法
（`highs-ds` 与 `highs` 0 / 1316 完全一致）；目标系数 1e-7 相对扰动后 165 / 1316 解向量不同。

**结论**：现行模型**没有**旧模型那种「换算法费用差 0.226%」的配置敏感性，
主答案 13,162,682.8855 元不是求解器伪影；但 LP **普遍退化、最优解不唯一**，
故逐时段「最优策略」只是最优解之一。

**口径注意**：普查比较完整 LP 解向量，模型只取前缀 `r.x[:nD]` 作为决策，
故 46.9% 描述的是退化程度、**不是**决策层面的不唯一率；决策层面影响由端到端对照界定。
另：脚本 `census` 模式本意抽样，但 `backtest` 会求解区间内**每一天**，
实际覆盖 329 天全部 1316 个 LP（非 42 天抽样），比原计划更强，单次约 31 分钟。

**产物**：`outputs/q3_multistage/solver_sensitivity_highs-ds.json`、
`solver_sensitivity_highs-ipm.json`、`solver_degeneracy_census.json`；报告第 8.1 节。

---
## C″7. 独立复算 ✅（2026-09-12）

脚本 `scripts/verify_q3_multistage.py`。**不 import `src/q3_multistage.py`**：只读原始
附件 1/2 的 xlsx 与已落盘的明细 npz，按 README「固定口径」与题面公式从零重算。
它补上的是「最终状态定义」第 3 条要求的「每个结果均能由明细独立复算」——
此前第三问只有两个校验，都不是独立复算（`validate_q3_multistage.py` import 了模型；
`q3_solver_uniqueness_check.py` 复用同一份 LP 构造）。

| 路径 | 做法 | 结果 |
|---|---|---|
| 甲 结算复算 | 独立构造自然日价格向量（附件列右移一位）+ 独立实现题面公式，逐日重算三项费用与总费用 | 逐日与交付期合计同 `summary`/`payload` 偏差 **0.000e+00** 元 |
| 乙 实时层与储能轨迹复算 | 按文档描述的贪心规则**重新实现**实时层，从当日初始 SOC 重放 365 天的 c/g/z/w/S | 与明细数组偏差 **0.000e+00** kWh |
| 丙 八组合复算 | 八个发布组合各重跑甲，并独立重算条件边际与 Shapley 分摊 | 八组总费用偏差 **0.000e+00** 元，Shapley 偏差 ≤ 5.821e-11 元 |

独立重算出的交付期总费用 = 交付值 **13,162,682.8855 元**。物理不变量（SOC 递推、
跨日连续性、充放电上限、SOC 上下限、非负性、同时充放电）也全部由明细独立重算通过。

**仍未覆盖**（引用时必须保留）：

1. **不重解任何 LP** —— 「记录解是该 LP 的最优解」**没有被独立复核**。
   最优化层面的证据仍只有 C″6（最优值不依赖算法）+ 记录解可行性。
2. 预测子模型（PCHIP 插值、负荷预测）、SAA 情景生成、终端价值 `λ = 0.478`、
   锁定区间规则都是**假设**，未被复核。
3. 时间标签口径按 README 第 3 条复算并验证自洽，但口径本身是建模约定。
4. `result3.xlsx` 未被本脚本直接重算（工作簿 ↔ payload 由 validator 负责，
   payload ↔ 原始附件由本脚本负责，复合后可传递锚定）。
5. 路径乙的贪心规则按文档重新实现；若文档本身写错，两者会一起错。

**产物**：`outputs/q3_multistage/independent_verify_0123_K30.json`；报告第 8.2 节。

---

## C″8. 终端储能价值 λ 的全年敏感性扫描 ✅（2026-09-12）

脚本 `scripts/q3_lambda_sensitivity.py`。**不修改 `src/`**：λ 是 `stage_lp` 与
`stage0_lp_145` 的**关键字默认值**（`lam=LAM`），脚本替换模块命名空间里的这两个
函数名、把 `lam` 换成扫描值，`alpha` 与 `commit_end` 原样透传 —— 与 C″6 替换
`linprog` 是同一手法。λ 取主答案值的 0/0.5/0.75/1/1.5/2/3 倍共 7 点，各跑一次全年回测。

**补丁自检**：λ = 0.478 那次重跑与已交付 `summary_stages0123_K30.json` 相差
**0.000e+00 元**（逐位复现），证明换 λ 的机制本身没有改变模型。

| λ | 交付期总费用/元 | 与主答案之差/元 | 期初 SOC/kWh | 日均日末 SOC/kWh | 紧急购电量/kWh |
|---:|---:|---:|---:|---:|---:|
| 0.0000 (0×) | 13,179,760.3378 | +17,077.4523 | 1,528.7603 | 1,682.9718 | 60,200.0862 |
| 0.2390 (0.5×) | 13,169,514.2697 | +6,831.3842 | 1,565.5211 | 1,757.9847 | 52,942.8860 |
| 0.3585 (0.75×) | 13,154,552.9851 | −8,129.9004 | 1,594.3739 | 1,822.1708 | 47,381.9213 |
| **0.4780 (1×，主答案)** | **13,162,682.8855** | — | 9,284.9109 | 9,292.4941 | 38,737.7478 |
| 0.7170 (1.5×) | 13,243,341.6765 | +80,658.7910 | 10,251.9707 | 10,199.9485 | 42,433.0155 |
| 0.9560 (2×) | 13,247,496.7495 | +84,813.8640 | 10,278.3855 | 10,227.5777 | 42,593.0184 |
| 1.4340 (3×) | 13,251,711.1473 | +89,028.2618 | 10,284.7024 | 10,250.4845 | 42,003.7574 |

**结论**：

1. **量级上不敏感**：0—3 倍范围内极差 97,158.1623 元（**0.74%**），
   3 倍 λ 也只高 89,028.2618 元（0.676%）。主答案量级不是 λ 的伪影。
2. **但费用对 λ 非单调**：λ ≈ 0.36—0.48 之间存在**储能运行机制切换**——
   低 λ 侧电池全年空转在低位，高 λ 侧被顶在高位。**交付所用的 λ = 0.478 正好在切换点右侧**。
3. **不能说某个 λ「更优」**：全区间最小值在 λ = 0.3585（低 8,129.9004 元 = 0.0618%），
   但低机制进入交付期时电池少存 7,690.5370 kWh（属 2025-01 预热期，**不属于交付期**），
   按 λ = 0.478 计价约值 3,676.0767 元，故这点优势含**存量转移**成分。
4. **是 C″6 退化结论的另一个侧面**：切换点两侧费用只差 8,129.9004 元（0.0618%），
   物理计划却差很多（紧急购电 8,644.1735 kWh、弃电 38,607.9157—182,058.6363 kWh、
   充电 571,713.5350 kWh、放电 463,097.7726 kWh）——**费用面很平，策略面不平**。

**仍未覆盖**：① 八组合节省是否随 λ 变化（未重跑）；② λ 仍是外生常数，**未内生求解**；
③ 切换点只夹在 λ ∈ (0.3585, 0.478) 内，未细定位；④ 其余 λ 点未做独立复算。

**产物**：`outputs/q3_multistage/lambda_sensitivity_K30.json` 与 `lambda_scan/`（每点一份）；
报告第 8.3 节。

---

# 阶段D：论文与图表

## D1. 论文结构建议

1. 问题重述；
2. 模型假设；
3. 符号说明；
4. 数据预处理与时间轴；
5. 第一问确定性优化；
6. 第二问随机计划购电；
7. 第三问滚动预报与合同调整；
8. 第四问波动电价；
9. 模型检验与敏感性；
10. 优缺点与改进方向；
11. 结论。

## D2. 必须统一的符号

- 时间下标和日期下标；
- 计划购电量、最终合同量、实际执行购电量；
- 充电量、放电量和 SOC；
- 紧急购电与弃电；
- 上调量与下调量；
- 固定电价与波动电价；
- 自然日统计与模板行统计。

## D3. 推荐图表

只保留能回答论文问题的图：

1. 时间轴与信息到达示意图；
2. 典型日计划、调整、实际净负荷和储能联合曲线；
3. 8 种预报组合费用比较；
4. frozen0 与最新预报策略的费用拆分；
5. 两种结算口径敏感性；
6. 固定电价与波动电价的储能行为比较；
7. 紧急购电月份或时段分布。

不得为了增加图数量而绘制与结论无关的图。每张图必须注明数据期间、单位、策略和口径。

## D4. 论文表格

- 四个指定日期结果表；
- 各问题年度或交付期费用汇总；
- 8 种预报组合对照；
- 结算敏感性；
- 固定与波动电价对照；
- 模型约束与残差检验。

---

# 阶段E：最终交付审计

## E1. 数值一致性

- [ ] 论文数字与JSON一致；
- [ ] JSON与CSV逐时段汇总一致；
- [ ] CSV与Excel一致；
- [ ] 四个指定日期与全年明细一致；
- [ ] 图表数据与正文数字一致；
- [ ] 不同问题的统计期间明确。

## E2. 文件完整性

- [x] `result1.xlsx`（2026-09-12 补做，见 0.2 节；`outputs/q1/result1.xlsx`）
- [x] `result2.xlsx`
- [x] `result3.xlsx`
- [ ] `result4-2.xlsx`
- [ ] `result4-3.xlsx`
- [ ] 最终论文源文件
- [ ] 最终论文PDF
- [x] 模型代码
- [x] 运行说明（`README.md` 第一、二、三问运行一节）
- [x] 校验脚本与校验日志（`scripts/validate_q1.py`、`scripts/validate_q2.py`、`scripts/validate_q3.py`、`outputs/q3_run.log`）

## E3. 论文PDF检查

- [ ] 成功编译；
- [ ] 逐页渲染检查；
- [ ] 无表格越界；
- [ ] 无公式截断；
- [ ] 图注和表注完整；
- [ ] 页码、目录和参考文献正确；
- [ ] 附录代码没有破坏版面；
- [ ] 摘要中的数字与正文一致。

## E4. Git交付

- [x] 查看工作树状态；
- [x] 区分用户已有修改与本任务修改；
- [x] 只暂存本任务文件（`git diff --cached --name-only` 已核对，无 q2 / q3_review）；
- [x] 运行 `git diff --check`（仅 `reports/project_task_reference.md` 的 Markdown 硬换行尾随空格，属有意格式）；
- [x] 本地提交：`6125f94`（master），38 个文件，+151,309 / −156,986；
- [x] **当时**远程推送状态：未推送，`origin/master` 停在 `398270c`，推送需用户明确授权；
- [x] 未实际推送时未声称已推送。

后续提交与推送记录（同一 E4 口径，均由用户授权后执行）：

| 提交 | 内容 | 推送 |
|---|---|---|
| `eb53289` | 阶段B：第三问策略推荐决策表 | 已推送 |
| `630184e` | 第三问退款口径 8 组合独立复算 | 已推送 |
| `37957da` | 第三问主口径唯一性检验，并汇总给论文手的口径报告 | 已推送 |

---

# 禁止事项

1. 禁止引用旧版 `outputs/q3` 作为修正后结果（旧产物已删除，新结果全部由本次重算生成）；
2. 禁止使用当天未来实际值参与此前决策；
3. 禁止同一行循环移位修复午夜数据；
4. 禁止让不同未来情景选择不同当前储能动作；
5. 禁止用贪心执行覆盖求解器储能计划；
6. 禁止只计算最终净调整量而丢失中间提交；
7. 禁止把不明确的合同解释写成题面唯一结论；
8. 禁止把滚动近似宣称为严格多阶段最优；
9. 禁止混用 334 天交付期和 365 天全年指标；
10. 禁止伪造缺失数据、运行结果或验证结论；
11. 禁止修改 `reports/q3_review.md`；
12. 禁止覆盖用户已有第二问修改；
13. 禁止把交付的 **13,162,682.89 元**（现行模型）写成"**该问题**的最优费用"或"最小值"
    ——现行模型是滚动两阶段近似，且终端价值 `λ` 是外生常数，该值不是原问题的最优值。
    **可以**写成"**该模型**在既定 `λ`/情景集下的最优费用"：阶段C″6 已实测换算法
    只差 0.0002%，该数值本身不是求解器伪影
    （见 `reports/q3_paper_handoff.md` 第 5.2 节、报告第 8.1 节）。
    仍需保留的限定：LP 存在多个最优解，故**逐时段策略不唯一**，"储能轨迹唯一"
    /"理论最优调度"的说法仍被第 14 条禁止；
14. 禁止声称储能轨迹唯一，或称之为"理论最优调度"；
15. 禁止引用 `reports/q3_review.md` 的任何数字或结论 —— 其审查对象是被淘汰的
    `src/q3_solver.py`（已在文件顶部加封存横幅）；
16. 禁止引用 `reports/_archive/` 下的任何数字（含旧的 14,743,406.7685 元、
    `6+12+18` 策略推荐、退款口径敏感性、退款复算与唯一性检验的结论）；
17. 禁止把「模板行合计」与「自然日合计」相加或混用；
18. 「引入 6/12/18 预报带来 X 元节省」**现在可以写**（阶段C″已补齐对照），
    但数字只能取自 `outputs/q3_multistage/q3_stage_comparison.json` 或
    `reports/q3_paper_handoff.md` 第 5.1 节，且必须同时写明其适用条件
    （模型内条件结论、单时刻节省不可相加）；
19. 禁止声称现行第三问模型「已经两轮独立复核」（那是旧模型 B5 的措辞，见
    `reports/q3_paper_handoff.md` 第 5.4 节）；
    可以声称的是「已通过求解器配置敏感性检验：换算法年度费用偏差 0.0002%（5.2）」
    与「结算金额与储能轨迹已由原始附件独立复算，偏差 ≤ 1e-9（5.3/C″7）」。
    但**不得**由 C″7 推出「记录解已被独立验证为最优解」——复算不重解 LP（C″7 第 1 条）；
20. 禁止声称某个终端储能价值 λ 取值「更优」或「最优」—— λ 是外生常数，
    C″8 只说明其影响被限定在 0.74% 以内，且全区间最小值 λ = 0.3585 的优势含存量转移成分。
    **可以**写「λ 在 0—3 倍范围内变动时交付期费用极差 0.74%（C″8/报告第 8.3 节）」。

---

# 推荐总体时间表

| 阶段 | 工作 | 原估时间 | 实际/修正 |
|---|---|---:|---:|
| 0.2 | 第一问补齐（求解、工作簿、独立验收、对照复核） | — | **已完成**（26 项验收通过） |
| A1—A3 | 接管、检查点、代码复核 | 40—65 分钟 | 已完成 |
| A4 | 第三问 10 套策略回测 | 25—35 分钟 | **实测约 9 分钟** |
| A5—A7 | 产物生成、数值验收、工作簿检查 | 15—30 分钟 | 已完成 |
| A8—B | 第三问报告与策略决策 | 20—35 分钟 | 均已完成（退款口径 8 组合补算约 8 分钟） |
| B5 | 第三问两轮独立复核 + 论文引用契约 | — | 旧模型上**已完成**，但已随模型更换失效（产物在 `reports/_archive/`） |
| C′ | 陈旧报告归档与封存 | — | **已完成**（6 份旧报告移入 `reports/_archive/`，`q3_report.md` 重写，新建现行引用契约） |
| C″ | 补齐第三问「预报发布时刻组合」对照 | — | **已完成**（修正锁定区间口径 + 重跑八组合，报告第 4 节，见 C″1—C″5） |
| C″6 | 第三问求解器退化与配置敏感性检验 | — | **已完成**（端到端 3 算法整年对照 + 1316 个 LP 普查；结论：主答案非求解器伪影，但 LP 普遍退化，见 C″6） |
| C″7 | 第三问独立复算（第二套实现） | — | **已完成**（不 import 模型，从原始附件重算结算/储能轨迹/八组合，偏差 0.000e+00，见 C″7） |
| C″8 | 第三问终端储能价值 λ 全年敏感性扫描 | — | **已完成**（7 个 λ 点各跑一年；0—3 倍内费用极差 0.74%，但存在储能运行机制切换，见 C″8） |
| C | 第四问建模、回测与验收 | 1—2 小时 | **已完成并封印**（变体 4-2 回测 222 s、4-3 399 s） |
| D | 论文与图表 | 2—4 小时 | 待办 |
| E | 最终交付审计 | 30—60 分钟 | 待办 |

第三问已从接管到完全验收；第三问之后完成第四问及整篇论文，建议预留至少半天连续工作时间。

**给后续阶段的两条经验**：① 全量重算只需约 9 分钟，因此"改代码→重算→重验收"应作为常规闭环，不要为了省时间而跳过重算；② 验收脚本本身也会出错，必须先跑通再用于判定（本次三处缺陷均属此类）。

---

# 最终状态定义

只有同时满足以下条件，项目才可标记为"完成"：

1. 四问模型均有明确数学表达和信息边界；
2. 所有要求的Excel结果文件由真实数据生成；
3. 每个结果均能由明细独立复算；
4. 物理约束、时间映射和费用口径全部验收；
5. 论文数字、表格、图形和工作簿完全一致；
6. 最终PDF已逐页检查；
7. 所有不确定合同假设均明确标注"需人工确认"；
8. 不存在把旧结果、部分运行或未验证结果冒充最终结论的情况。

当前进度：第一问（2026-09-12 补做）、第三问、第四问满足第 2、3、4、7 条
（第 1、5、6 条须随阶段 D/E 一并复核），
第一问第 3 条已由消元编码重解 + 明细复算 + 对照标准逐项核对加强（见 0.2 节），
第四问第 3、4 条已由 `scripts/validate_q4.py` 独立验收 + 工作簿逐格核对加强（见 C8）；
**第三问第 3 条已由 `scripts/verify_q3_multistage.py` 独立复算加强**（不 import 模型，
从原始附件重算结算与储能轨迹，偏差 0.000e+00；见 C″7）；
第二问待并入最终审计。

> ⚠️ 第 3 条原文为「每个结果均能由明细独立复算」。第三问**现已满足**该条
> （C″7，2026-09-12）。但须注意 C″7 的边界：它**不重解任何 LP**，
> 故「记录解是该 LP 的最优解」仍只由 C″6（最优值不依赖算法）+ 可行性支撑。

**进入阶段D 前必须先处理的第三问缺口**（见 `reports/q3_paper_handoff.md` 第 5 节、C″5）：
**三项已全部关闭**（C″3、C″6、C″7）。

1. ~~「预报时刻价值」对照（八套组合）现行模型未做~~ —— **已在阶段C″3 补齐（2026-09-12）**：
   全启用相对 `0-only` 节省 622,378.8759 元，12 个条件边际全为正，
   Shapley 排序 18:00 > 12:00 > 6:00。题面问题3 第二段末句现可回答。
2. ~~求解器配置敏感性未在现行模型上重做~~ —— **已在阶段C″6 补齐（2026-09-12）**：
   换算法最优值一致到 6.17e-14，端到端最坏差 28.21 元（0.0002%）；
   但 LP 普遍退化（46.9% 的解向量不同），逐时段策略不唯一。
3. ~~现行模型未做**独立复算**~~ —— **已在阶段C″7 补齐（2026-09-12）**：
   `scripts/verify_q3_multistage.py` 不 import 模型，三条路径（结算 / 实时层与储能轨迹 /
   八组合与 Shapley）从原始附件重算，偏差 0.000e+00（Shapley ≤ 5.821e-11）。
   仍未覆盖：LP 未重解、预测子模型、SAA 情景生成、`λ`、锁定区间规则、`result3.xlsx` 未直接重算。

**其余未决项**：

- 第四问：① 与第三问的 +5.20% 未拆分"波动风险"与"预测误差信息缺失"两项
  （需"完美预见电价"对照回测，见 C9）；② C4 第 6 项"第三问各预报时刻价值是否改变"
  经用户决定移出本次范围（见 C4）。
- 第三问：~~终端储能价值系数（常数水价 `λ = 0.478`）未做全年敏感性扫描~~
  —— **已完成（2026-09-12，见 C″8）**：0—3 倍 λ 内交付期费用极差 97,158.1623 元（0.74%），
  但费用对 λ 非单调，λ ≈ 0.36—0.48 之间有储能运行机制切换；
  仍未测「八组合节省是否随 λ 变化」，λ 也仍未内生。
  ~~其余 6 套策略的储能轨迹唯一性未检验~~ —— 该表述属旧模型 B5.2。现行模型已完成
  退化普查（C″6）：LP 确实普遍存在多个最优解，故**储能轨迹不唯一是可确认的结论**，
  但年度费用不受影响。
