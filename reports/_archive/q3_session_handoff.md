> 🔒 **已封存（2026-09-12）** —— 本文件描述的是**已被取代**的第三问模型
> `src/q3_solver.py`，其全部数字与结论**均已失效，不得再被引用**。
>
> **失效原因**：第三问在阶段B 之后整体更换为 `src/q3_multistage.py`
> （模板行框、0:00/6:00/12:00/18:00 四阶段、题面结算口径）。
> 新旧模型的时间口径与结算口径都不同，**两边的数字不可相减、不可混引**。
>
> **现行权威来源**：`docs/q3_multistage_model.md`、`reports/q3_report.md`、
> `reports/q3_paper_handoff.md`。
>
> 本文件仅作**审计轨迹**保留，不再更新。

**本文件承载的已失效内容**：旧 `src/q3_solver.py` 的断点续跑改造与 10 套策略回测交接记录。

---

# 第三问修正会话交接总结

更新时间：2026-09-12  
工作目录：`C:\Users\admin\Desktop\zz`

## 一、当前结论

本次已完成第三问模型、推导、求解器和验收脚本的实质修改，但**全年10套策略回测被中断，未生成新版最终结果**。当前 `outputs/q3/` 中的 `summary.json`、`solver_payload.json`、`result3.xlsx` 等文件时间早于本次重跑，属于旧模型产物，**不得作为修正后答案引用**。

被中断的 Python 进程已经结束，不存在后台继续计算。

## 二、已完成的实质修改

### 1. 消除情景提前预知

`src/q3_solver.py` 已将情景模型改为共同储能轨迹：同一发布时刻的全部净负荷情景共享购电量、充电量、放电量和SOC；只有紧急购电与弃电为情景补救变量。由此不再允许不同未来情景在当前时刻选择不同储能动作。

### 2. 统一优化与实际执行

第三问不再调用第二问的贪心 `causal_dispatch` 覆盖优化结果。每次发布后，下一6小时严格执行求解器给出的共同充放电轨迹；实际净负荷出现后，只据能量缺口计算紧急购电或弃电。

因此，新结果中可能出现“紧急购电同时充电”。这不表示执行器自相矛盾，而表示发布时已锁定的储能轨迹在实际情景下仍被执行。需在报告中说明其因果含义。

### 3. 修正调整费用口径

主模型采用较保守、也更贴近题面“总费用包括计划购电费用和调整相关费用”的解释：

- 初始计划购电费用保留；
- 每次调整相对当前生效合同计算；
- 上调部分按 `1.5p` 计费；
- 下调部分按 `0.5p` 计违约费；
- 先取消后恢复会产生两次调整费用。

同时保留另一种“允许退款、只按最终净额结算”的重新优化结果和固定电量重结算敏感性。两种合同解释均标为“需人工确认”，不再把 `pA+0.5p|A-G|` 写成唯一等价公式。

### 4. 增加预报价值对照

求解器已配置以下10套独立全年策略：

1. 8种新预报组合：`none`、`6`、`12`、`18`、`6+12`、`6+18`、`12+18`、`6+12+18`；
2. `6+12+18_frozen0`：仍在6/12/18时重优化，但冻结0:00光伏预报；
3. `6+12+18_refund`：按退款/最终净额口径重新优化。

每套策略独立维护SOC、跨日计划量和跨日最终合同量，不能用同一主策略事后删减预报点代替。

### 5. 补充审计产物

新版求解器计划生成：

- `policy_comparison_daily.csv`：10套策略的334天逐日费用和SOC；
- `release_log.json`：每次发布的完整合同向量、共同充放电向量、信息截止时刻、情景数和逐次调整费用；
- `baseline_none_interval_detail.csv`：仅0:00基线的48,096条明细；
- `baseline_frozen0_interval_detail.csv`：冻结预报基线的48,096条明细；
- 主策略 `interval_detail.csv`、`daily_metrics.csv`、`summary.json` 和 `solver_payload.json`。

### 6. 修正文档与验收

以下文件已经改写或更新：

- `src/q3_solver.py`
- `docs/q3_model.md`
- `docs/q3_derivation.md`
- `scripts/validate_q3.py`
- `README.md`

`reports/q3_review.md` 未修改，继续作为审查依据。`scripts/build_result3.mjs` 沿用官方模板写入流程。

另已修复模板行费用边界：自然日调整费用与模板行调整费用分别保存，避免漏掉本行末列“次日00:00”的调整费用。

## 三、本次实际运行到哪里

本次最后一次重跑依次完成或基本完成了：

- `none`
- `6`
- `12`
- `18`
- `6+12`
- `6+18`
- `12+18` 已显示运行到第300/365天，随后进程在进入下一阶段前后被中断。

由于当前程序只在全部10套策略结束后统一写文件，上述内存结果没有落盘，不能恢复，也不能作为正式数值。`6+12+18`、冻结0:00预报和退款口径均未完成。

## 四、当前未完成事项

1. 尚未完成10套全年策略回测。
2. 尚未生成修正后的 `summary.json`、CSV明细和 `solver_payload.json`。
3. 尚未用新版载荷生成 `result3.xlsx`。
4. 尚未运行新版 `scripts/validate_q3.py`。
5. 尚未检查工作簿四张表的渲染图和公式错误扫描。
6. 尚未得到可报告的第三问最终费用、紧急购电量、8种组合比较及四个指定日期数值。
7. 终端价值敏感性仍未实现；当前固定为 `0.9*min(p)`，报告中只能列为限制。
8. 0:00计划仍未通过完整情景树联合定价未来调整机会，因此只能称因果滚动SAA/MPC，不能称严格多阶段随机最优。

## 五、下次续跑前的必要改进

直接再次运行会从头计算，耗时且容易再次丢失。建议先给 `src/q3_solver.py` 增加**按策略检查点与断点续跑**：

- 每完成一套策略，立即将该策略的逐日结果和发布日志写入独立文件；
- 启动时校验模型版本、输入文件SHA256和参数签名；
- 签名一致时跳过已完成策略；
- 全部策略结束后再汇总生成主结果和工作簿载荷。

完成检查点功能后，依次执行：

```powershell
.\.venv\Scripts\python.exe -m py_compile .\src\q3_solver.py
.\.venv\Scripts\python.exe .\src\q3_solver.py --data-dir .\problem\data --output-dir .\outputs\q3 --report .\reports\q3_report.md
node .\scripts\build_result3.mjs
.\.venv\Scripts\python.exe .\scripts\validate_q3.py
```

也可在检查点完成后运行：

```powershell
.\scripts\run_q3.ps1
```

## 六、最终验收标准

- `summary.json` 明确 `strict_multistage_optimal=false`；
- 10套策略各有334天结果，共3340条策略日记录；
- 主策略有48,096条自然日10分钟记录；
- 每次发布均保存完整合同与共同储能向量；
- 能量平衡和SOC递推最大残差小于 `1e-7 kWh`；
- SOC始终位于1200—10800 kWh；
- 单时段充放电量不超过833.3333 kWh；
- 无同时充放电；
- 跨日SOC连续；
- 主口径费用可由“计划费＋逐次调整费＋紧急购电费”独立复算；
- 退款口径只作为敏感性，不与主口径混算；
- `result3.xlsx` 与载荷逐项一致，四张工作表均完成渲染检查；
- 四个指定日期的计划、最终合同、6个四小时充放电段和紧急购电均从新版结果提取。

## 七、仓库状态说明

当前工作树包含此前第二问修改以及第三问新增文件，均未提交。本次没有删除或覆盖 `reports/q3_review.md`，也没有执行Git提交或远程推送。继续工作时应只暂存确认属于本任务的文件，避免覆盖用户已有修改。
