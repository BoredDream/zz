# 1、2、3、4 敏感度分析符号统一表（配套《1，2，3，4符号统一对照表》）

> **本文件的定位**：主表《`1，2，3，4符号统一对照表.md`》统一的是**模型里的量**（\(x,q,z,c,g,y,S,\eta,\lambda,\alpha,K,\mathcal M_{\text{on}}\)……）；
> 本文件统一的是**敏感度分析里的量**——被扰动的参数、实验设计、结果指标、结论强度判定。
>
> **与主表的关系（硬约束）**：
>
> 1. 本文件**只新增符号，不改动主表任何既有符号**。凡主表已定义的量（如 \(\lambda,\alpha,\eta,K,\mathcal M_{\text{on}},S_0,F_d,Q_d\) 等），一律沿用主表写法；本文件新增的 \(\eta_c,\eta_d,\eta_{rt},\Theta,\Xi,\mathcal I,\text{alg},\jmath,L_b,B,n_b,\beta,\zeta,\xi,\mathcal E,\Psi\) 等一律不与主表冲突。
> 2. 若本文件与主表冲突，**以主表为准**。
> 3. 主表 §1 的五组分组（索引与集合 / 参数 / 决策变量 / 状态量 / 派生量）**不适用于本文件**；本文件按"自变量 / 实验设计 / 因变量 / 判定"四组重排。
>
> **核查对象**：`BoredDream/zz` 仓库 **`q3-hybrid-mpc` 分支、提交 `5611746`**（2026-09-12 21:30，含前一笔 `dff5ed8`）。
> 结论由**逐文件读源码与产物 JSON** 得出：`scripts/` 下 14 个敏感度/对照脚本、`outputs/` 下 30 余个敏感度 JSON、`reports/` 下 12 份相关报告、`docs/` 下 6 份数学模型文档。
>
> **本文件不含任何新的数值结论**：所有数值均照抄仓库产物，**只用于标注符号的取值、网格与量级**，不作为论文正文数字；
> 论文正文数字仍应从主表指定的来源文件与仓库 `summary*.json` 取。
>
> **仓库代码不改**：全部敏感度实验都是**替换模块命名空间里的名字**实现的（`src/` 未被敏感度脚本修改），
> 这一事实本身要在论文"模型检验"一节声明，见 §六 护栏 9。

---

## 〇、速览：仓库 Q1–Q4 敏感度分析全清单（26 项，其中 3 项 ⊘）

**读法**：先看"扰动的符号"一列——**论文写"我们做了敏感性分析"时必须能填出这一列**；
再看它标的是 \(\text{R}_1/\text{R}_2/\text{R}_3\) 哪一档（定义见 §1.4），这决定你**能写多强的话**。

| 编号 | 问 | 敏感度 / 对照 | 扰动的符号 | 扫描或对照范围 | 代表量级（元） | 档 |
|---|---|---|---|---|---|---|---|
| **S1.1** | 一 | 储能效率口径 | \(\eta_c,\eta_d,\eta_{rt}\) | 两端各 0.9（81%）↔ 往返 0.9（各 \(\sqrt{0.9}\)） | 费用 \(-3.773\%\) | R₂ |
| **S1.2** | 一 | 起点储电量 | \(S_0\) | 固定 \(S_0=6000\) ↔ 起点自由（仅要求 \(S_{144}=S_0\)） | 35,126.85 ↔ 35,118.60 | R₂ |
| **S1.3** | 一 | 时间口径（标签解释） | \(\Theta\) | 交付口径 ↔ 反事实（整体错移 10 min） | 35,126.85 ↔ 35,126.95 | R₂ |
| **S1.4** | 一 | 独立复算与求解器 | \(\text{alg}\) | 主 LP ↔ 二元变量显式互斥 + 内点法重解 | 逐项一致 | R₁ |
| **S1.5** | 一 | 无储能对照 | —（策略基线） | 配储 ↔ 不配储 | 对照，非扰动 | R₁ |
| **S2.1** | 二 | 储能效率口径 | \(\eta_c,\eta_d,\eta_{rt}\) | 同上 | 费用 \(-2.739\%\) | R₂ |
| **S2.2** | 二 | 终端价值倍数 | \(\lambda\) | \(0,1,2\) 倍 × 4 个指定日期 | 当日费用最大 \(+9.5\%\) | R₂ |
| **S2.3** | 二 | 预测器权重与残差窗口 | \(w_d,\kappa_d\) | \(w\in\{0,0.25,0.5,0.75,1\}\)；\(\kappa_d\in\{7,14\}\) | 滚动验证择优选参 | R₁ |
| **S2.4** | 二 | 无储能基线 | —（策略基线） | 同历史情景、逐时段最小化 | 对照，非扰动 | R₁ |
| **S2.5** | 二 | 吞吐惩罚项存在性 | \(\varepsilon\) | 仓库只有 \(\varepsilon=10^{-7}\) 一个取值（**无消融**） | **未做（⊘）** | ⊘ |
| **S3.1** | 三 | 报表发布组合（8 种） | \(\mathcal M_{\text{on}}\) | \(\{0\}\) 到 \(\{0,1,2,3\}\) 的全部子集 | 13,785,061.76 → 13,162,682.89 | R₁ |
| **S3.2** | 三 | 发布时刻条件边际价值 | \(\mathcal M_{\text{on}}\) | 每个时刻的 4 种上下文 + Shapley | 6:00 130,907.90；12:00 186,175.29；18:00 305,295.68 | R₂ |
| **S3.3** | 三 | 终端储能价值 \(\lambda\) 全年扫描 | \(\lambda\) | \(0,\ 0.5,\ 0.75,\ 1,\ 1.5,\ 2,\ 3\) 倍（0—1.434） | 极差 97,158.16（0.738%） | R₂ |
| **S3.4** | 三 | 求解器退化与配置 | \(\text{alg},\jmath\) | 换算法 + 1,316 个 LP 级普查（42 抽样日） | 目标差 \(\le 6.17\times10^{-14}\) | R₁ |
| **S3.5** | 三 | 独立复算（第二套实现） | —（实现） | 从原始附件重算结算/轨迹/八组合 | 逐项一致 | R₁ |
| **S3.6** | 三 | 储能效率口径（含 8 组合排名） | \(\eta_c,\eta_d,\eta_{rt}\) | 同上，八组合各跑一遍 | 费用 \(-3.152\%\)；最优组合不变 | R₁ |
| **S3.7** | 三 | 配对块 Bootstrap 与月度分层 | \(L_b,B\) | 7 日循环移动块 × 10,000 次 | \(\Psi=622,378.88\)，CI \([381,459.48,\ 904,660.84]\) | R₂ |
| **S3.8** | 三 | 情景数 \(K\) 收敛 | \(K\) | 设计网格 \(\{10,20,30,50,80\}\) | **未完成（⊘）** | ⊘ |
| **S4.1** | 四 | 同预测器严格控制变量对照 | \(\mathcal M_{\text{on}}\) | \(\{0\}\) ↔ \(\{0,1,2,3\}\)，同一 `q4_solver` | 省 647,557.39（4.467%） | R₁ |
| **S4.2** | 四 | 终端储能价值 \(\lambda\) 全年扫描 | \(\lambda\) | 两变体各 7 点，按各自 \(\lambda_0\) 取 0—3 倍 | 4-2 极差 133,400.36（0.878%）；4-3 极差 124,538.50（0.899%） | R₂ |
| **S4.3** | 四 | 完美预见电价对照（信息分解） | \(\mathcal I\) | 滚动预测 ↔ 当日实际电价（其余一字不动） | \(\Delta F^{\text{risk}}=567,796.36\)；\(\Delta F^{\text{info}}=117,315.70\) | R₂ |
| **S4.4** | 四 | 求解器退化与配置 | \(\text{alg},\jmath\) | 整年换算法 + 1,284 个 LP 级普查（17 抽样日） | 整年差 \(-1.86\times10^{-9}\) 元 | R₁ |
| **S4.5** | 四 | 滚动情景样本外校准 | \(\zeta,\beta\) | 阶段 0—3 × 净负荷/价格 | 净负荷 CRPS 28.630 → 18.474；90% 区间经验覆盖仅 0.831—0.835 | R₃ |
| **S4.6** | 四 | 储能效率口径 | \(\eta_c,\eta_d,\eta_{rt}\) | 同上 | 费用 \(-2.693\%\) / \(-3.181\%\) | R₂ |
| **S4.7** | 四 | 配对块 Bootstrap | \(L_b,B\) | 7 日循环移动块 × 10,000 次 | \(\Psi=647,557.39\)，CI \([383,099.35,\ 943,529.95]\) | R₂ |
| **S4.8** | 四 | 情景数 \(K\) 收敛 | \(K\) | 同 S3.8 | **未完成（⊘）** | ⊘ |

> **三项 ⊘ 的直接影响**：`reports/q1_q4_paper_master_handoff.md` §7 明文写
> 「在完整 \(K=\{10,20,30,50,80\}\) 结果汇总前，**不得声称 K=30 已收敛**」；
> 而 **S2.5（\(\varepsilon\) 消融）在仓库里根本没有第二个取值**——`src/q2_solver.py` 的
> `throughput_penalty: float = 1e-7` 是唯一取值，也没有任何脚本把它设为 0 重跑。
> 三项都只能写成"列入进一步研究"，**不得**引用具体数值或声称已检验。

---

## 一、统一写法

### 1.1 敏感度自变量（被扰动或被切换的量）

**这一类是"敏感性分析的 \(x\) 轴"**。主表 §1.2 已定义的参数，本表不重复定义，只补"扫描时怎么标"。

| 统一写法 | 含义 | 单位 | 基准值（论文主口径） | 仓库扫描范围 | 适用 |
|---|---|---|---|---|---|
| \(\eta_c,\eta_d\) | 单向充电 / 放电效率（**主表只有合并的 \(\eta\)，敏感度必须拆开**） | — | \(0.9,\ 0.9\) | 0.9 / \(\sqrt{0.9}=0.948683\) | 一~四 |
| \(\eta_{rt}\) | 往返效率 \(\eta_c\eta_d\)（**情景标签就用它**） | — | \(0.81\) | \(0.81\) / \(0.90\) | 一~四 |
| \(\lambda\) | 终端储电量价值（水价，主表 §1.2 / §3.4） | 见下注（**两种量纲不得混写**） | 三问与 4-3：\(0.478\) 元/kWh；二问与 4-2：\(0.33417\) 元/**内部** kWh | \(0,\ 0.5,\ 0.75,\ 1,\ 1.5,\ 2,\ 3\) 倍 | 二、三、四 |
| \(\lambda_0\) | 该问题的**基准** \(\lambda\)（扫描的 1 倍点） | 同上 | 0.478 元/kWh（三、4-3）/ 0.33417 元/内部 kWh（二、4-2） | 同上 | 二、三、四 |
| \(K\) | 情景数上限（主表 §1.1） | 个 | 30 | 设计网格 \(\{10,20,30,50,80\}\)（⊘ 未跑） | 二、三、四 |
| \(K^{\text{eff}}_d\) | **第 \(d\) 日实际可用**情景数 | 个 | \(\le K\) | 年初历史不足时 \(K^{\text{eff}}_d<K\) | 二、三、四 |
| \(\mathcal M_{\text{on}}\) | 实际启用的发布时刻集合（主表 §1.1） | 时 | \(\{0,1,2,3\}\) | 全部 \(2^4\) 子集（八组合） | 三、四 |
| \(\alpha\) | 紧急购电电价倍数（主表 §1.2） | — | 5 | **未扫描（⊘）** | 二、三、四 |
| \(\varepsilon\) | 吞吐惩罚系数（主表 §1.2） | 元/kWh | \(10^{-7}\) | 仅"有/无"二点 | 二、4-2 |
| \(\Theta\) | **时间口径标记** \(\in\{\text{终},\text{起}\}\)：附件行标签解释为区间**终点**/区间**起点** | — | 终（见 §六 护栏 1） | 二点 | 一 |
| \(\Xi\) | **结算口径标记** \(\in\{\text{主},\text{退}\}\)：逐次提交不退款 / 退款（最终净额） | — | 主 | 现行模型**未重做**（⊘，仅旧模型有） | 三、四 |
| \(\mathcal I\) | **信息集标记** \(\in\{\text{滚动},\text{冻结0},\text{完美预见}\}\) | — | 滚动 | 完美预见：仅第四问；冻结0：仅**旧模型**有（⊘） | 三、四 |
| \(\text{alg}\) | 求解算法 \(\in\{\text{highs-ds},\text{highs-ipm},\text{highs}\}\) | — | `highs` | 换算法整年重解 | 一~四 |
| \(\jmath\) | 人为抖动幅度（去 LP 多解退化用，相对量） | — | 0 | \(10^{-7}\) | 三、四 |
| \(S_0\) | 日循环锚点 / 初始储电量（主表 §1.2、§3.8） | kWh | 6000 | 固定 ↔ 起点自由 | 一 |

> ⚠️ **\(\eta\) 与 \(\eta_c,\eta_d,\eta_{rt}\) 的关系必须写清楚**：主表 §2.3 定 \(\eta\) 为"充、放电效率（各 0.9）"。
> 敏感度分析一旦把两端拆开，**\(\eta\) 就不再够用**——所以本表新增 \(\eta_c,\eta_d\) 两个符号，
> 并规定：**情景一律用 \(\eta_{rt}\) 命名**（"往返 81% 主口径 / 往返 90% 敏感性对照"），
> 正文出现 \(\eta\) 时默认指 \(\eta_c=\eta_d\) 的主口径值。
>
> ⚠️ **\(\lambda\) 的量纲必须分开写**：主表 §3.4 已声明「元/内部 kWh」与「元/kWh」是两个口径、相差一个 \(\eta\)。
> 仓库里二问与 4-2 的终端价值是 \(\eta\min_tp_t=0.33417\)**元/内部 kWh**
> （`outputs/q2/summary.json` 的字段名就是 `terminal_value_yuan_per_internal_kwh`），
> 三问与 4-3 是 \(0.478\)**元/kWh**。本表的扫描倍率 \(\theta_j\) 对两种量纲各自成立，
> 但**符号表与正文必须标对单位**，不得把 0.33417 写成"元/kWh"。

### 1.2 实验设计符号（基准、情景、网格、算法、重采样）

| 统一写法 | 含义 | 单位 | 适用 |
|---|---|---|---|
| \((\cdot)^{0}\) | **基准**取值（= 论文主口径、= 交付答案） | — | 一~四 |
| \((\cdot)^{s}\) | **敏感性情景**取值，\(s\) 为情景标签（如 \(s=\text{rt90}\)、\(s=\text{free}\)） | — | 一~四 |
| \((\cdot)_j\) | 第 \(j\) 个**扫描点**取值 | — | 二~四 |
| \(\mathcal G\) | 扫描网格（点集），\(J=\lvert\mathcal G\rvert\) 为点数 | — | 二~四 |
| \(g_0\) | 网格的基准点，满足 \(F(g_0)=F^{0}\)（须逐位复现交付答案） | — | 二~四 |
| \(\theta_j\) | 扫描倍率 \(\theta_j=g_j/g_0\) | — | 二~四 |
| \(\lambda^{\star}\) | 费用对 \(\lambda\) 的**体制切换点**（非单调时的转折） | 元/kWh | 三、四 |
| \(L_b\) | Bootstrap **块长**（循环移动块） | 天 | 三、四 |
| \(B\) | Bootstrap **重采样次数** | 次 | 三、四 |
| \(n_b\) | 循环移动块的**块数** \(n_b=\lceil\lvert\mathcal D^{\text{del}}\rvert/L_b\rceil\)（本仓库 \(=48\)） | 个 | 三、四 |
| \(\beta\) | **名义**置信水平（取 0.95）/ 名义覆盖率（取 0.5、0.8、0.9） | — | 三、四 |
| \(\zeta\) | **分位数水平** \(\in\{0.1,0.5,0.9\}\) | — | 四 |
| \(\mathcal D^{\text{del}}\) | 交付期日集合，\(\lvert\mathcal D^{\text{del}}\rvert=334\) | — | 二~四 |
| \(\mathcal D^{\text{ext}}\) | **极端日**集合（实际日峰值净负荷前 10%） | — | 四 |
| \(\mathcal D^{\text{samp}}\) | 求解器普查的**抽样日**集合 | — | 三、四 |
| \(\mathcal T_m\) | 阶段 \(m\) 的**评分窗口**（阶段 0：\(h=1,\dots,144\)；阶段 \(m\ge1\)：\(t=T^{\text{stg}}_m,\dots,143\)） | — | 四 |
| \(\xi_{k,d,t}\) | 第 \(k\) 个情景在 \((d,t)\) 的取值；情景集记 \(\mathcal E_{d,t}=\{\xi_{1,d,t},\dots,\xi_{K,d,t}\}\) | 与变量同 | 二~四 |
| \(\mathrm{Quant}_\zeta(\mathcal E_{d,t})\) | 情景集 \(\mathcal E_{d,t}\) 的 \(\zeta\) 分位数算子 | 与变量同 | 四 |
| \(n_{\text{LP}}\) | 普查的 LP 总数 | 个 | 三、四 |

> ⚠️ \(\zeta\) 是本次新增，**不要用 \(\tau\)**：主表 §1.1 已把 \(\tau\) 定为"时段终点时刻标签"。
> ⚠️ \(\beta\) 与主表无冲突（主表未用 \(\beta\)）；**不要用 \(\alpha\) 表示显著性水平**——\(\alpha\) 是紧急电价倍数。
> ⚠️ 块数用 \(n_b\)，**不要用 \(m\)**：主表 §1.1 已把 \(m\) 定为决策阶段编号（0/1/2/3）。

### 1.3 结果指标（敏感度因变量）

| 统一写法 | 含义 | 单位 | 定义式 / 来源 |
|---|---|---|---|
| \(F^{\text{tot}}_d\) | 第 \(d\) 日**总费用**（三分项之和） | 元 | \(F^{\text{tot}}_d=F^{\text{plan}}_d+F^{\text{adj}}_d+F^{\text{emg}}_d\) |
| \(F^{\text{del}}\) | **交付期总费用** | 元 | \(F^{\text{del}}=\sum_{d\in\mathcal D^{\text{del}}}F^{\text{tot}}_d\)（**不是** \(\sum_dF_d\)，见下注） |
| \(F^{\text{plan}},F^{\text{adj}},F^{\text{emg}}\) | 计划购电费 / 调整相关费 / 紧急购电费（三个分项） | 元 | \(F^{\text{plan}}_d=\sum_tp_t\min(x_t,q_t)\)；\(F^{\text{adj}}_d=\sum_t\bigl[1.5p_t(q_t-x_t)^{+}+0.5p_t(x_t-q_t)^{+}\bigr]\)；\(F^{\text{emg}}_d=\sum_t\alpha\,p_tz_t\)；三者之和 \(\equiv F^{\text{tot}}_d\)（逐日核对） |
| \(F(\mathcal C)\) | 启用集合 \(\mathcal M_{\text{on}}=\mathcal C\) 下的 \(F^{\text{del}}\) | 元 | 用于 §四 式(5)；\(\mathcal C\supseteq\{0\}\) |
| \(F^{\text{PF}}\) | 完美预见信息集 \(\mathcal I=\text{完美预见}\) 下的 \(F^{\text{del}}\) | 元 | 用于 §四 式(10)；其余设置同 Q4-3 |
| \(Q^{\text{plan}},Q^{\text{emg}},Y^{\text{tot}}\) | 计划购电量 / 紧急购电量 / 弃电量合计 | kWh | 主表 §1.5 的合计式 |
| \(\Delta F\) | 绝对费用差 \(F^{s}-F^{0}\) | 元 | — |
| \(\delta F\) | 相对费用变化率 | % | \(\delta F=(F^{s}-F^{0})/F^{0}\times100\) |
| \(R_{\text{scan}}\) | 扫描**极差** \(\max_jF_j-\min_jF_j\) | 元 | — |
| \(\rho_{\text{scan}}\) | 相对极差 \(R_{\text{scan}}/F^{0}\) | % | — |
| \(\text{mono}\) | 费用对扫描量是否单调 \(\in\{\text{是},\text{否}\}\) | — | — |
| \(\Psi\) | **节省额**（费用降低为正） \(\Psi=F^{0}-F^{s}\) | 元 | — |
| \(\psi\) | 相对节省率 \(\Psi/F^{0}\) | % | — |
| \(\Psi_d\) | 第 \(d\) 日节省 | 元 | \(\Psi_d=F^{\text{tot},0}_d-F^{\text{tot},s}_d\) |
| \(\Psi_{\min}\) | 最差单日节省（可为负） | 元 | — |
| \(\Psi_{\max}\) | 最好单日节省 | 元 | — |
| \(\Psi^{\text{marg}}_r(\mathcal C)\) | 发布时刻 \(r\) 在上下文 \(\mathcal C\) 下的**条件边际节省** | 元 | 见 §3.3 S3.2 |
| \(\Psi^{\text{Shap}}_r\) | 发布时刻 \(r\) 的 **Shapley 分摊节省** | 元 | \(\sum_r\Psi^{\text{Shap}}_r=\Psi\)（全组合 vs 仅 0:00） |
| \(\Psi^{-},\Psi^{+}\) | Bootstrap 区间**下/上界** | 元 | 区间记 \([\Psi^{-},\Psi^{+}]\) |
| \(P^{+}\) | 重采样中节省为正的比例 | — | — |
| \(n^{+},n^{-}\) | 全年节省为正 / 为负的**天数** | 天 | \(n^{+}+n^{-}\le\lvert\mathcal D^{\text{del}}\rvert\)（恰为 0 的日子不计入） |
| CRPS | 连续排序概率分（标量预报的分布评分） | 与变量同 | §四 式(6) |
| ES | 能量分数（向量预报的分布评分） | 与变量同 | §四 式(7) |
| \(\hat\beta^{(m)}\) | 阶段 \(m\) 在名义水平 \(\beta\) 下的**逐时段边际**经验覆盖率 | — | §四 式(8)（先对时段、后对日期取平均） |
| \(\hat\Phi^{(m)}_\zeta\) | 阶段 \(m\)、分位数水平 \(\zeta\) 的**经验频率**（理想值 \(=\zeta\)） | — | §四 式(8) |
| \(\text{MAE},\text{MAPE},\text{RMSE}\) | 点预测误差指标（主表 §1.5 已定 MAE） | kWh 或 % | — |
| \(\Delta F^{\text{risk}}\) | 完美预见分解中的**价格波动风险成本** | 元 | \(\Delta F^{\text{risk}}=F^{\text{PF}}-F^{\text{Q3}}\) |
| \(\Delta F^{\text{info}}\) | 同上，**预测误差信息缺失成本** | 元 | \(\Delta F^{\text{info}}=F^{\text{Q4-3}}-F^{\text{PF}}\) |
| \(\delta^{\text{obj}}\) | 跨算法的最大相对最优值差 | — | — |
| \(\chi\) | 跨算法**解向量不同**的 (LP,算法) 对占比 | % | — |
| \(z_{\zeta},z_{\max}\) | **按日汇总**量的 \(\zeta\) 分位数 / 最大值（尾部风险） | kWh | \(\zeta\in\{0.90,0.95,0.99\}\)，按 334 天取值 |
| \(\text{TS}_{5\%}\) | 尾部占比（最高 5% 日的量占全年比） | % | — |

> ⚠️ **\(\Psi\) 不是求和号**：求和仍写 \(\sum\)。选 \(\Psi\) 是为了避开主表已占用的 \(G,W,U,S,P,F,v,\Sigma\) 家族。
> ⚠️ **节省的正负方向必须一次说清**：本表一律 \(\Psi=F^{0}-F^{s}>0\) 表示"敏感性情景更省"。
> 仓库产物里存在两种写法（`saving_vs_0_only_yuan` 是"基准−滚动"，`delta_vs_baseline_yuan` 是"扫描点−基准"，**符号相反**），见 §二。
> ⚠️ **\(F^{\text{del}}\) 不等于"主表 §1.5 的全天购电费之和"**：主表 \(F_d=\sum_tp_tx_{d,t}\) **只含计划购电**，
> 而三、四问的日总费用还含调整费与紧急购电费。仅当 \(q\equiv x\)（第一、二问）时才有 \(F^{\text{plan}}_d=F_d\)；
> **不得**把 \(F_d\) 当作三、四问的 \(F^{\text{tot}}_d\)。这是本表最容易写错的一处，故 \(F^{\text{del}}\) 的定义式写在 \(F^{\text{tot}}_d\) 之上。

### 1.4 结论强度判定符号（**论文措辞的总开关**）

| 判定 | 含义 | 论文允许写 | 论文禁止写 |
|---|---|---|---|
| \(\text{R}_1\) **排序稳健** | 被扰动的量变了，**策略排序/方向不变** | "主要策略结论对该口径稳健" | "模型对此参数不敏感"（数值其实变了） |
| \(\text{R}_2\) **数值敏感** | 数值变、量级不变，**结论未反转** | "该口径会改变费用与能量流的精确数值，但不改变结论" | "结果稳健"而不给扫描范围 |
| \(\text{R}_3\) **结论脆弱/边界** | 出现**反向证据**（如区间欠覆盖、非单调、排序反转） | 必须写明反向证据与适用边界 | 只报有利的一半 |
| ⊘ | **未完成 / 未做**（计划内未跑，或按设计不做） | 只能写成**建模假设**或"列入进一步研究" | 引用任何具体数值或声称已检验 |

---

## 二、仓库现写法 → 论文统一写法（逐条替换）

**读法**：左栏是仓库里的字段名 / 变量名 / 报告用语；右栏是论文该写的符号。**论文里不出现左栏任何字符串。**

### 2.1 被扰动参数

| 量 | 仓库现写法 | 取值 / 网格 | **论文统一写法** |
|---|---|---|---|
| 单向效率 | `eta_charge`、`eta_discharge` | 0.9 / \(\sqrt{0.9}\) | \(\eta_c,\eta_d\) |
| 往返效率 | `roundtrip_efficiency`、`roundtrip` | 0.81 / 0.90 | \(\eta_{rt}\) |
| 效率情景名 | `oneway90`（**主口径**）、`roundtrip90`（**对照**） | 二点 | 情景标签按**往返效率**命名：\(s=\text{rt81}\)（= 仓库 `oneway90`，主口径）/ \(s=\text{rt90}\)（= 仓库 `roundtrip90`，对照） |
| 终端水价 | `LAM`、`lam`、`terminal`、`terminal_value`、`terminal_value_yuan_per_internal_kwh`、`terminal_value_factor` | 0.478 元/kWh；0.33417 元/内部 kWh；倍数 \(0,1,2\) | \(\lambda\)，基准记 \(\lambda_0\)；倍数情景记 \(\theta_j\) |
| 扫描点 | `lam0`、`lam0p239`、`lam0p3585`、`lam0p478`、`lam0p717`、`lam0p956`、`lam1p434` | 七点 | \(\lambda_j,\ j=1,\dots,7\) |
| 扫描倍率 | `lam_over_baseline` | \(0,0.5,0.75,1,1.5,2,3\) | \(\theta_j\) |
| 情景数 | `K`、`K_max` | 30 | \(K\)（上限）；当日实际 \(K^{\text{eff}}_d\) |
| 紧急电价倍数 | `ALPHA`、`alpha`、**结算式里的裸 `5`** | 5 | \(\alpha\)（**公式里不得出现裸 5**，主表 §3.6） |
| 吞吐惩罚 | `epsilon`、`throughput_penalty` | \(10^{-7}\) | \(\varepsilon\) |
| 发布组合 | `stages`、`policy`、`tags`：`0`,`01`,`02`,`03`,`012`,`013`,`023`,`0123`、`0-only`、`0+6+12+18` | 八种 | \(\mathcal M_{\text{on}}\subseteq\{0,1,2,3\}\)（时刻写法 \(\{0,6,12,18\}\)） |
| 阶段起始 | `TSTAGE` | \([0,35,71,107,144]\) | \(T^{\text{stg}}\)（主表 §1.1） |
| 求解算法 | `method`：`highs`、`highs-ds`、`highs-ipm` | 三种 | \(\text{alg}\) |
| 抖动 | `jitter_rel` | \(10^{-7}\) | \(\jmath\) |
| 变体 | `variant`：`"2"`,`"3"`、`q4-2`,`q4-3` | 两个 | 4-2 / 4-3（正文用题面语言"对应问题2/问题3"） |

> ⚠️ **最高危的一处（同名不同版）**：`dff5ed8`（17:52）时**主口径是 `roundtrip90`（往返 90%）**、对照是两端各 0.9；
> `5611746`（21:30）把两者**整体对调**，主口径改为 `oneway90`（两端各 0.9、往返 81%）。
> 两笔提交之间 **JSON 文件名不变**，变的只有 `comparison.json` 里的
> `primary_scenario`（现为 `oneway90`）/`sensitivity_scenario`（现为 `roundtrip90`）与报告措辞。
> ⇒ **引用仓库报告与产物前必须确认是哪一笔提交的版本**；论文一律以"两端各 0.9（往返 81%）为主口径、往返 90% 为敏感性对照"表述。

### 2.2 实验设计与统计

| 量 | 仓库现写法 | **论文统一写法** |
|---|---|---|
| 基准复现校验 | `baseline_reproduction_check`、`rerun_matches_delivered`、`abs_gap_yuan` | \(g_0\) 点须逐位复现 \(F^{0}\)，偏差记 \(\lvert\Delta F\rvert=0\) |
| 扫描容器 | `scan`、`points`、`point_files` | \(\mathcal G,\ J\) |
| 极差 | `spread_yuan`、`spread_pct_of_baseline` | \(R_{\text{scan}},\rho_{\text{scan}}\) |
| 单调性 | `costs_monotone_in_lam`、`nets_monotone_in_lam` | \(\text{mono}\) |
| 区间 | `ci95_yuan` | \([\Psi^{-},\Psi^{+}]\)，\(\beta=0.95\) |
| 块与次数 | `block_days`、`replicates`、`method: "paired circular moving-block bootstrap"` | \(L_b=7\) 天、\(B=10{,}000\)、**配对循环移动块自助法** |
| 为正比例 | `bootstrap_positive_probability` | \(P^{+}\) |
| 天数统计 | `positive_days`、`loss_days`、`worst_day_yuan`、`best_day_yuan` | \(n^{+}\)、\(n^{-}\)、\(\Psi_{\min}\)、\(\Psi_{\max}\) |
| 月度分层 | `months` | 按自然月分层的 \(\Psi_d\) 汇总 |
| 校准 | `n_vectors`、`n_points`、`mean_crps`、`mean_energy_score`、`central_interval_coverage`、`quantile_calibration` | CRPS、ES、\(\hat\beta^{(m)}\)、\(\hat\Phi^{(m)}_\zeta\) |
| 极端日 | `extreme_day_stage0`、`peak_90pct_coverage`、`top 10% actual daily peak net load` | \(\mathcal D^{\text{ext}}\)、\(\hat\beta^{(0)}_{0.9}\)（峰值口径） |
| 尾部 | `p90_kwh`、`p95_kwh`、`p99_kwh`、`max_kwh`、`top5pct_share` | \(z_{0.90},z_{0.95},z_{0.99},z_{\max},\text{TS}_{5\%}\) |
| 普查 | `lps`、`sampled_days`、`overall`、`by_stage`、`n_lp_method_pairs*` | \(n_{\text{LP}}\)、\(\mathcal D^{\text{samp}}\)、\(\delta^{\text{obj}}\)、\(\chi\) |
| 增量/边际 | `conditional_marginal_savings`、`shapley_savings_vs_0_only_for_full_policy_yuan` | \(\Psi^{\text{marg}}_r(\mathcal C)\)、\(\Psi^{\text{Shap}}_r\) |

### 2.3 输出指标

| 量 | 仓库现写法 | **论文统一写法** |
|---|---|---|
| 总费用 | `total_cost_yuan`、`reference_total_cost_yuan`、`alt_total_cost_yuan` | \(F^{0}\)、\(F^{s}\) |
| 分项 | `plan_cost_yuan`、`adjust_cost_yuan`、`emergency_cost_yuan` | \(F^{\text{plan}},F^{\text{adj}},F^{\text{emg}}\) |
| 电量 | `planned_purchase_kwh`、`emergency_kwh`、`charge_kwh`、`discharge_kwh`、`curtail_kwh`、`adjust_up_kwh`、`adjust_down_kwh` | \(Q^{\text{plan}},Q^{\text{emg}},\sum c,\sum g,Y^{\text{tot}}\)、净上调/下调量 |
| 费用差 | `delta_vs_baseline_yuan`、`delta_pct`（**扫描点 − 基准**） | \(\Delta F,\ \delta F\) |
| 节省 | `saving_vs_0_only_yuan`、`saving_vs_0_only_pct`、`annual_saving_yuan`、`cost_saving_pct`（**基准 − 情景**） | \(\Psi,\ \psi\) |
| 信息分解 | `a_price_volatility_risk_cost_yuan`、`b_forecast_error_information_loss_yuan`、`identity_residual_yuan` | \(\Delta F^{\text{risk}},\Delta F^{\text{info}}\)；恒等式残差须为 0 |
| 预测精度 | `MAPE`、`MAE`、`RMSE` | 同（大写正体） |
| SOC 边界 | `soc_end_mean_daily_kwh`、`soc_band_hits_delivery`、`soc_net_change_kwh` | \(S_t\) 的统计量（**不进符号表，正文用文字**） |
| 净值口径 | `cost_net_of_storage_change_yuan`、`delta_net_vs_baseline_yuan` | **期末存量转移扣除后的费用及其相对基准的差**，正文必须显式声明口径 |

> ⚠️ **`delta_*` 与 `saving_*` 符号相反**，这是把仓库数字搬错方向的最常见来源。
> 写论文时**统一用 \(\Psi=F^{0}-F^{s}\)**，并在表注写明"正=敏感性情景更省"。

---

## 三、逐问敏感度清单与可写边界

### 3.1 第一问（5 项）

| 编号 | 分析 | 符号 | 数值（照抄仓库） | 可写边界 |
|---|---|---|---|---|
| S1.1 | 效率口径 | \(\eta_{rt}\) | 费用 \(-3.773\%\)、购电量 \(-3.289\%\)、充电 \(-4.329\%\)、放电 \(+6.301\%\) | 可写"效率解释改变数值、不改变结论"；**不得**称效率越高全部指标都改善 |
| S1.2 | 起点储电量 | \(S_0\) | 固定 6000：35,126.8486 元；起点自由：\(S_0=8{,}550.0000\) kWh、35,118.5986 元 | 必须写清"主模型把 \(S_0\) 当**硬锚点**"（主表 §3.8），并说明该对照**不是**放松了题面要求 |
| S1.3 | 时间口径 | \(\Theta\) | 交付 35,126.8486 ↔ 反事实 35,126.9486（差约 0.1 元） | ⚠️ **命名与主表相反，见 §六 护栏 1**；必须写"指定时间段的购电量会整体错移 10 分钟"，不能只比全天总额 |
| S1.4 | 独立复算与求解器 | \(\text{alg}\) | 逐项一致；对照标准用二元变量显式互斥 | 可写"两套编码 + 内点法重解一致" |
| S1.5 | 无储能对照 | — | 有对照值 | 属**对照**而非扰动，不能记入"敏感性分析"清单 |

### 3.2 第二问（5 项，1 项 ⊘）

| 编号 | 分析 | 符号 | 数值（照抄仓库） | 可写边界 |
|---|---|---|---|---|
| S2.1 | 效率口径 | \(\eta_{rt}\) | 费用 \(-2.739\%\)、紧急购电 \(+2.967\%\)、弃电 \(+1.373\%\) | 同 S1.1 |
| S2.2 | 终端价值倍数 | \(\lambda\) | 4 个指定日期 × \(\{0,1,2\}\) 倍，共 12 点；当日费用与 24:00 SOC 同步变化 | **只能写"四个指定日期的检验"**——仓库明文："终端敏感性仅覆盖四个指定日期，不能替代更大范围的鲁棒性研究" |
| S2.3 | 权重与残差窗口 | \(w_d,\kappa_d\) | 权重候选 \(\{0,0.25,0.5,0.75,1\}\)、窗口 \(\{7,14\}\) 按过去 28 日 MAE / 7 条完整轮廓的**能量评分**滚动择优 | 属**参数选择证据**，不是单因素敏感性；不得写成"我们做了权重敏感性分析" |
| S2.4 | 无储能基线 | — | 同历史情景逐时段最小化 | 对照 |
| S2.5 | 吞吐惩罚项 | \(\varepsilon\) | 仓库只有 \(\varepsilon=10^{-7}\)（**无消融**） | 必须逐问声明有无（主表 §3.3）：**二问与 4-2 有，一、三问无**；但仓库**未跑**消融，不得写成"已检验惩罚项影响" |

### 3.3 第三问（8 项，1 项 ⊘）

| 编号 | 分析 | 符号 | 数值（照抄仓库） | 可写边界 |
|---|---|---|---|---|
| S3.1 | 八种发布组合 | \(\mathcal M_{\text{on}}\) | 仅 0:00 13,785,061.76 → 全时点 13,162,682.89；\(\Psi=622,378.88\)（4.5149%）；排名：\(0123<013<023<03<012<02<01<0\) | 可写"全时点最优"；**不得**称 \(0{+}6{+}18\) 与 \(0{+}12{+}18\) 有显著优劣（仅差 1,542.24 元，CI 跨 0） |
| S3.2 | 条件边际与 Shapley | \(\Psi^{\text{marg}}_r,\Psi^{\text{Shap}}_r\) | Shapley：6:00 130,907.90；12:00 186,175.29；18:00 305,295.68，**三者之和 \(=622{,}378.88=\Psi\)**（即"仅 0:00 → 全时点"的总节省）。条件边际则随上下文变（6:00 在 \(\mathcal C=\{0\}\) 下为 160,598.08、在 \(\{0,12\}\) 下为 123,860.34），**四组上下文的值不可相加** | 可写"价值排序：18:00 > 12:00 > 6:00，且存在替代性"；必须写明"边际值依赖上下文、只有 Shapley 可加" |
| S3.3 | \(\lambda\) 全年扫描 | \(\lambda\) | 七点 0—1.434；最小 13,154,552.99（\(\lambda=0.3585\)）；最大 13,251,711.15（\(\lambda=1.434\)）；\(R_{\text{scan}}=97,158.16\)、\(\rho_{\text{scan}}=0.738\%\)；\(\text{mono}=\)否；切换点 \(\lambda^{\star}\approx0.36\!-\!0.48\)，交付值 0.478 落在切换点右侧 | 可写"主答案量级不是 \(\lambda\) 的伪影"；**必须同时写非单调与切换**；不得写"\(\lambda\) 已被标定正确" |
| S3.4 | 求解器与配置 | \(\text{alg},\jmath\) | 1,316 个 LP / 42 抽样日：\(\delta^{\text{obj}}\le6.17\times10^{-14}\)，无一超 \(10^{-9}\)；\(\chi=1234/2632=46.9\%\)；\(\jmath=10^{-7}\) 时最大相对目标差 \(1.21\times10^{-5}\) | 可写"费用面唯一、**策略面不唯一**"；不得由解向量差异推断"模型不稳定" |
| S3.5 | 独立复算 | — | 第二套实现，不 import 模型，从原始附件重算 | 可写"独立复算一致" |
| S3.6 | 效率口径 | \(\eta_{rt}\) | 费用 \(-3.152\%\)；两口径下最优组合都是 \(0123\)、排名完全一致 | 可写"排序对效率口径稳健" |
| S3.7 | Bootstrap 与月度 | \(L_b,B\) | \(L_b=7,B=10{,}000\)；\(\Psi=622,378.88\)，\([\Psi^-,\Psi^+]=[381{,}459.48,\ 904{,}660.84]\)，\(P^{+}=100\%\)，\(n^{+}=222/334\)；**2025-05 月度节省为负** | 可写"全年与 Bootstrap 层面稳健"；**不得**写"每月都节省"；必须声明该区间**只反映本年度逐日费用差的时间块重采样，不代表跨年度结构不确定性** |
| S3.8 | 情景数收敛 | \(K\) | **⊘ 未完成**（设计网格 \(\{10,20,30,50,80\}\)，仅 Q4 跑了 \(K=30\) 的两点） | 只能写"列入进一步研究" |

### 3.4 第四问（8 项，1 项 ⊘）

| 编号 | 分析 | 符号 | 数值（照抄仓库） | 可写边界 |
|---|---|---|---|---|
| S4.1 | 同预测器严格控制变量 | \(\mathcal M_{\text{on}}\) | 仅 0:00 14,495,352.33 → 滚动 13,847,794.94；\(\Psi=647,557.39\)（4.467%）；紧急购电 145,699.46 → 41,691.45 | 可写"同一预测体系下信息更新与调整权的**联合**价值"；**不得**再分解为纯信息价值/纯调整权价值 |
| S4.2 | \(\lambda\) 全年扫描 | \(\lambda\) | 4-2（\(\lambda_0=0.33417\)）\(R_{\text{scan}}=133{,}400.36\)、\(\rho_{\text{scan}}=0.878\%\)；4-3（\(\lambda_0=0.478\)）\(R_{\text{scan}}=124{,}538.50\)、\(\rho_{\text{scan}}=0.899\%\)；均 \(\text{mono}=\)否 | 必须写"两个变体都在用**附件1 的价格水平**给附件4 的储能量定价"这一口径不一致，且它**已被度量但未被修正** |
| S4.3 | 完美预见分解 | \(\mathcal I\) | \(F^{\text{Q3}}=13{,}162{,}682.89\)；\(F^{\text{PF}}=13{,}730{,}479.24\)；\(F^{\text{Q4-3}}=13{,}847{,}794.94\)；\(\Delta F^{\text{risk}}=567{,}796.36\)（\(+4.314\%\)）；\(\Delta F^{\text{info}}=117{,}315.70\)（\(+0.891\%\)）；合计 685,112.05（\(+5.205\%\)），恒等式残差 0 | 可写分解；**必须同时写**：① 完美预见只是**事后下界参照**、不是可执行策略；② \(F^{\text{Q3}}\) 与 \(F^{\text{PF}}\) **跨两套求解器实现**，可比性来自"附件1 电价 ≈ 附件4 逐时段均值（最大差 \(5.15\times10^{-5}\)）"这条论据 |
| S4.4 | 求解器与配置 | \(\text{alg},\jmath\) | 4-2 与 4-3 整年换 `highs-ds` 与主答案逐位相同；4-3 换 `highs-ipm` 差 \(-1.86\times10^{-9}\) 元；1,284 个 LP / 17 抽样日：\(\delta^{\text{obj}}\le1.011\times10^{-8}\)，无一超 \(10^{-6}\)；\(\chi=1145/2568=44.6\%\) | 同 S3.4 |
| S4.5 | 样本外校准 | \(\zeta,\beta\) | 净负荷 CRPS 阶段 0→3：28.630 → 18.474；价格 CRPS：0.0579 / 0.0400 / 0.0591 / 0.0573；\(\hat\beta^{(m)}_{0.9}\)：净负荷 0.835（阶段 0）→ 0.834（阶段 3）、价格 0.853 → 0.843；极端日 \(\lvert\mathcal D^{\text{ext}}\rvert=34\)、峰值 90% 覆盖 0.6471；紧急购电尾部 4-2 \(z_{0.99}=10{,}599.77\)、\(\text{TS}_{5\%}=48.46\%\)；4-3 \(z_{0.99}=1{,}474.27\)、\(\text{TS}_{5\%}=47.13\%\) | **\(\text{R}_3\) 档**：可写"滚动更新改善点预测/分布评分，但当前经验残差场景**欠覆盖**"；**不得**写"90% 情景区间已校准" |
| S4.6 | 效率口径 | \(\eta_{rt}\) | 4-2 \(-2.693\%\)；4-3 \(-3.181\%\)；两口径下滚动均优于仅 0:00 | 可写"方向未反转" |
| S4.7 | Bootstrap | \(L_b,B\) | \(\Psi=647{,}557.39\)，\([383{,}099.35,\ 943{,}529.95]\)，\(P^{+}=100\%\)，\(n^{+}=222/334\) | 同 S3.7（含"不代表跨年度结构不确定性"的声明） |
| S4.8 | 情景数收敛 | \(K\) | **⊘ 未完成** | 只能写"列入进一步研究" |

### 3.5 未做清单（⊘：论文只能写"假设"或"进一步研究"）

> 本清单含两类：**按设计未做**（S2.5 的 \(\varepsilon\) 消融、\(\alpha\)、现行模型的退款口径与冻结预报、
> 48 小时复位、CVaR 与连续效率扫描）与**计划内未完成**（S3.8、S4.8 两项 \(K\) 网格）。
> 两类在论文里的处理相同——**不得引用具体数值、不得声称已检验**。

| 未做项 | 仓库证据 | 论文里禁止的写法 |
|---|---|---|
| \(K\in\{10,20,30,50,80\}\) 收敛网格 | `scripts/assemble_k_convergence.py` 的 `KS=(10,20,30,50,80)`、`TAGS` 八组合；产物只有 Q4 的 \(K=30\) 两点 | 不得声称 \(K=30\) 理论充分/已收敛 |
| \(\alpha\)（紧急电价倍数）扫描 | 全仓库无 \(\alpha\) 扫描脚本 | 必须写成**假设**（题面未给倍数）并只给方向性讨论 |
| \(\varepsilon\) 消融 | `src/q2_solver.py` 只有 `throughput_penalty: float = 1e-7` 一个取值，CLI 无相关开关，`scripts/` 下无脚本改过它 | 不得称"已检验惩罚项影响"；只能声明"本问含/不含该惩罚项" |
| **现行**多阶段模型的 \(\Xi\) 结算口径（退款）对照 | `scripts/verify_q3_refund.py`、`reports/_archive/q3_refund_verification.md` 全部针对**旧模型**；现行模型只有 `docs/q3_multistage_model.md` 一句口径说明 | 不得把旧模型的退款口径数字当现行结果（主表 §五 + master handoff §8 第 7 条：不得引用归档旧 Q3 结果） |
| **现行**模型的 \(\mathcal I=\text{冻结0}\) 基线 | 仅旧模型有 `frozen0`（`outputs/q3/`、归档报告） | 同上；**不得**用旧模型的"预报价值几乎全来自光伏更新"去论证现行多阶段模型 |
| 48 小时终端复位口径对照 | 二问已**删除**该口径 | 不得把 48 小时复位写成题面要求 |
| CVaR / 鲁棒优化、连续 \(\eta_{rt}\in[0.81,0.90]\) 扫描 | master handoff §9：未完成 | 只能列入进一步研究；**不得**写成已验证结果 |

---

## 四、论文"模型检验与敏感性"一章的符号骨架（可直接抄）

> 以下公式是**写作骨架**：符号全部取自本表与主表，数值取自 §三。
> 式(6)(7)(8)(9) 已**逐行对照仓库实现**核定（`scripts/analyze_scenario_calibration.py` 的 `crps` / `energy_score` / `update`，
> `scripts/bootstrap_monthly_analysis.py` 的 `compare`）：其中式(6)(7) 为标准集成定义，
> 式(8)(9) 的"先时段后日期的双重平均""循环移动块取模与尾部截断"等细节与代码逐字一致。

**(1) 效率情景**

\[
\eta_{rt}=\eta_c\eta_d,\qquad
(\eta_c,\eta_d)=
\begin{cases}
(0.9,\ 0.9), & \eta_{rt}=0.81\ \text{（论文主口径）}\\[2pt]
(\sqrt{0.9},\ \sqrt{0.9}), & \eta_{rt}=0.90\ \text{（敏感性对照）}
\end{cases}
\]

**(2) 相对变化率与极差**

\[
\delta F=\frac{F^{s}-F^{0}}{F^{0}}\times100\%,\qquad
R_{\text{scan}}=\max_{j}F_j-\min_{j}F_j,\qquad
\rho_{\text{scan}}=\frac{R_{\text{scan}}}{F^{0}}\times100\%
\]

**(3) 终端价值的扫描（含基准复现校验）**

\[
\lambda_j=\theta_j\lambda_0,\qquad
\theta_j\in\{0,\ 0.5,\ 0.75,\ 1,\ 1.5,\ 2,\ 3\},\qquad
\theta_j=1\ \Longrightarrow\ F_j=F^{0}\ (\text{逐位相同})
\]

**(4) 节省额（**方向唯一定义**）**

\[
\Psi=\sum_{d\in\mathcal D^{\text{del}}}\Psi_d=F^{0}-F^{s},\qquad
\Psi_d=F^{\text{tot},0}_d-F^{\text{tot},s}_d,\qquad
\psi=\frac{\Psi}{F^{0}}\times100\%
\]

> **配对**的含义：\(\Psi_d\) 是**同一天**两个策略的费用差，先逐日相减、再求和——**不是**先求年度和再相减的另一种算法
> （两者数值相同，但配对是 Bootstrap 能对时间块重采样的前提）。

**(5) 发布时刻的条件边际与 Shapley 分摊**

\[
\Psi^{\text{marg}}_r(\mathcal C)=F(\mathcal C)-F(\mathcal C\cup\{r\}),
\qquad \mathcal C\supseteq\{0\},\quad r\notin\mathcal C
\]

其中 \(F(\mathcal C)\) 是启用集合 \(\mathcal C\) 下的交付期总费用（§1.3）；
每个 \(r\in\{6,12,18\}\) 都有 \(2^{2}=4\) 个上下文（= 另外两个非零时刻的任意子集），
**上下文不同则值不同，不可把不同上下文的值相加**。Shapley 分摊才是唯一可加的口径：

\[
\sum_{r\in\{6,12,18\}}\Psi^{\text{Shap}}_r
=F\bigl(\{0\}\bigr)-F\bigl(\{0,1,2,3\}\bigr)=\Psi
\]

**(6) CRPS（标量预报分布评分，集成式）**

\[
\mathrm{CRPS}_{d,t}=\frac1K\sum_{k=1}^{K}\lvert \xi_{k,d,t}-y_{d,t}\rvert-\frac{1}{2K^{2}}\sum_{i=1}^{K}\sum_{j=1}^{K}\lvert \xi_{i,d,t}-\xi_{j,d,t}\rvert
\]

其中 \(y_{d,t}\) 是第 \(d\) 日第 \(t\) 时段的**实际值**，\(\xi_{k,d,t}\) 是该时段**第 \(k\) 个情景**的取值，
情景集记 \(\mathcal E_{d,t}=\{\xi_{1,d,t},\dots,\xi_{K,d,t}\}\)。

> **符号说明（防撞名）**：情景值**不用 \(x\)**——主表 §1.3 已把 \(x_t\) 定为"计划购电量"；
> 分位数**不用 \(q\)**——主表已把 \(q_t\) 定为"最终生效合约量"。分位数一律写成算子 \(\mathrm{Quant}_\zeta(\mathcal E_{d,t})\)。
>
> **报告值不是上式本身**，而是"先对时段、后对日期"的两次平均（与仓库 `update()` 逐行一致）：

\[
\overline{\mathrm{CRPS}}=\frac{1}{\lvert\mathcal D^{\text{del}}\rvert}\sum_{d\in\mathcal D^{\text{del}}}\Bigl(\frac{1}{\lvert\mathcal T_m\rvert}\sum_{t\in\mathcal T_m}\mathrm{CRPS}_{d,t}\Bigr)
\]

\(\mathcal T_m\) 为阶段 \(m\) 的**评分窗口**：阶段 0 用 145 段窗口去掉已锁定的 \(h=0\) 后的 \(h=1,\dots,144\)，
阶段 \(m\ge1\) 用 \(t=T^{\text{stg}}_m,\dots,143\) —— 四个阶段的窗口长度与时段构成都不同，
**跨阶段的 CRPS 不可直接横比**。

**(7) 能量分数（向量预报）**

\[
\mathrm{ES}_{d,t}=\frac1K\sum_{k=1}^{K}\lVert \boldsymbol\xi_{k,d,t}-\boldsymbol y_{d,t}\rVert-\frac{1}{2K^{2}}\sum_{i=1}^{K}\sum_{j=1}^{K}\lVert \boldsymbol\xi_{i,d,t}-\boldsymbol\xi_{j,d,t}\rVert
\]

与 CRPS 的唯一区别是范数作用在**向量**上：本仓库中该"向量"= 该日该阶段评分窗口上的**整条路径**，
故 ES 衡量**整条路径**的分布质量，而 CRPS 是逐时段评分的汇总。
由此两点差别必须写清：**①** ES 每个（日，阶段）只产生**一个标量**，报告值只有"对日期"的一次平均
（\(\overline{\mathrm{ES}}=\frac{1}{\lvert\mathcal D^{\text{del}}\rvert}\sum_d\mathrm{ES}_d\)），**没有**对时段的第二次平均；
**②** ES 的量纲随窗口长度增长，与 CRPS 不同量纲，**两者不可互相换算**。

**(8) 经验覆盖率与分位数校准**

\[
\hat\beta^{(m)}=\frac{1}{\lvert\mathcal D^{\text{del}}\rvert}\sum_{d\in\mathcal D^{\text{del}}}
\Biggl(\frac{1}{\lvert\mathcal T_m\rvert}\sum_{t\in\mathcal T_m}
\mathbf 1\bigl\{y_{d,t}\in[\,\mathrm{Quant}_{\frac{1-\beta}{2}}(\mathcal E_{d,t}),\ \mathrm{Quant}_{\frac{1+\beta}{2}}(\mathcal E_{d,t})\,]\bigr\}\Biggr)
\]

\[
\hat\Phi^{(m)}_\zeta=\frac{1}{\lvert\mathcal D^{\text{del}}\rvert}\sum_{d\in\mathcal D^{\text{del}}}
\Biggl(\frac{1}{\lvert\mathcal T_m\rvert}\sum_{t\in\mathcal T_m}\mathbf 1\{y_{d,t}\le \mathrm{Quant}_\zeta(\mathcal E_{d,t})\}\Biggr)
\]

三点必须写进正文，否则会被质疑**口径偷换**：

1. \(\mathrm{Quant}_\zeta(\mathcal E_{d,t})\) 是**该日该时段**情景集的分位数，不是全天分位数；
2. 因此这是**逐时段边际覆盖**（先对时段、后对日期两次平均），**不等于**"全天 144 段同时落入区间"的联合覆盖——后者必然更低；
3. 极端日那个 64.7% 是**第三种口径**：只取阶段 0 的**日峰值**情景分布，看实际日峰值是否落入其 90% 区间，
   与上式不同，**不可互相解释**。

**(9) 配对块自助法与区间**

把交付期 334 天按 \(i=1,\dots,n\)（\(n=\lvert\mathcal D^{\text{del}}\rvert=334\)）顺序编号，
逐日节省 \(\Psi_i\) 即 §1.3 的 \(\Psi_d\)（由式(4)给出）。
每次重采样独立抽取 \(n_b=\lceil n/L_b\rceil=48\) 个**块起点**（允许重复、允许环绕）：

\[
\Psi^{(b)}=\sum_{j=1}^{n_b}\ \sum_{i=0}^{L_b-1}\Psi_{\,((s^{(b)}_j-1+i)\bmod n)+1},
\qquad s^{(b)}_j\stackrel{\text{iid}}{\sim}\mathrm{Unif}\{1,\dots,n\},\qquad b=1,\dots,B
\]

\[
[\Psi^{-},\Psi^{+}]=\bigl[\hat Q_{0.025}\bigl(\Psi^{(1)},\dots,\Psi^{(B)}\bigr),\ \hat Q_{0.975}\bigl(\Psi^{(1)},\dots,\Psi^{(B)}\bigr)\bigr],
\qquad
P^{+}=\frac1B\sum_{b=1}^{B}\mathbf 1\bigl\{\Psi^{(b)}>0\bigr\}
\]

四点说明（与实现逐字对应）：

1. **循环移动块**：块内是 \(L_b=7\) 个**连续**日历日，跨年末时按 \(\bmod\ n\) 绕回年初——不是独立逐日抽样；
2. **尾部截断**：\(n_bL_b=48\times7=336>334\)，实现是拼成长度 336 的序列后**只取前 334 天**求和；
3. **配对**：两个策略用的是**同一组块起点**（同一天的费用差），这正是"配对"的含义，也是区间比独立抽样更窄的原因；
4. \(\hat Q\) 为经验分位数（线性插值），非顺序统计量索引；仓库校验脚本会断言 \(\Psi^{-}\le\Psi\le\Psi^{+}\)。

**(10) 完美预见分解（恒等式）**

三个费用点先定义清楚：

\[
F^{\text{Q3}}:\ \text{附件1 确定性电价，且已知}\ ;\qquad
F^{\text{PF}}:\ \text{附件4 实际波动电价，且已知}\ ;\qquad
F^{\text{Q4-3}}:\ \text{附件4 实际波动电价，但只能预测}
\]

\[
\underbrace{F^{\text{PF}}-F^{\text{Q3}}}_{\Delta F^{\text{risk}}}
+\underbrace{F^{\text{Q4-3}}-F^{\text{PF}}}_{\Delta F^{\text{info}}}
=F^{\text{Q4-3}}-F^{\text{Q3}},
\qquad \text{恒等式残差}=0
\]

> ⚠️ **这一条的前提必须写全，否则跨模型相减不成立**：
> ① \(F^{\text{PF}}\) 与 \(F^{\text{Q4-3}}\) **同属第四问求解器**（`src/q4_solver.py`），
> 只把 `price_hat` 换成当日实际电价，负荷/光伏预测、情景、\(\lambda\)、实时层与滚动时域全部不动 —— 这两点相减是**严格对照**；
> ② 但 \(F^{\text{Q3}}\) 来自**第三问的正式模型**（`src/q3_multistage.py`），**不是同一个求解器**。
> 仓库给出的可比性论据是：**附件1 的分时电价恰是附件4 的逐时段均值**，
> 逐时段最大差 \(5.15\times10^{-5}\) 元/kWh（属表格取整）。论文必须写明这条论据，
> 否则"风险"与"信息"两项的归属会被质疑。
> ③ \(F^{\text{PF}}\) 是**事后下界参照**，不是可执行策略，**不得**写进策略比较表。

---

## 五、给论文符号表追加的行（只列主表未定义的新增符号）

> 交付包规格要求论文符号表**只列一次**（主表 §3.1）。下表是**追加块**，接在主表 §1 五组之后；
> 主表已定义的（\(\eta,\lambda,\alpha,\varepsilon,K,\mathcal M_{\text{on}},S_0,F_d,Q_d\) 等）**不重复列入**。

| 符号 | 含义 | 单位 | 取值或来源 |
|---|---|---|---|
| \(\eta_c,\eta_d,\eta_{rt}\) | 单向充/放电效率与往返效率 | — | \(0.9,0.9,0.81\)（主口径）；对照 \(0.90\) |
| \(\lambda_0,\lambda_j,\theta_j,\lambda^{\star}\) | 基准水价 / 扫描点 / 倍率 / 体制切换点 | 元/kWh **或** 元/内部 kWh（按问标注） | 见 §三 S3.3、S4.2 |
| \(\mathcal G,J,g_0\) | 扫描网格 / 点数 / 基准点（\(F(g_0)=F^{0}\)） | — | 见 §三 |
| \(\Theta,\Xi,\mathcal I\) | 时间口径 / 结算口径 / 信息集标记 | — | \(\Theta\)：终/起；\(\Xi\)：主/退；\(\mathcal I\)：滚动/冻结0/完美预见 |
| \(\text{alg},\jmath\) | 求解算法 / 抖动幅度 | — | `highs-ds`、`highs-ipm`；\(10^{-7}\) |
| \(L_b,B,n_b,\beta,\zeta\) | 块长 / 重采样次数 / 块数 / 名义水平 / 分位数水平 | 天,次,个,—,— | 7、10,000、48、0.95（区间）与 \(\{0.5,0.8,0.9\}\)（覆盖率）、\(\{0.1,0.5,0.9\}\) |
| \(\mathcal D^{\text{del}},\mathcal D^{\text{ext}},\mathcal D^{\text{samp}},\mathcal T_m,n_{\text{LP}}\) | 交付期 / 极端日 / 抽样日集合、阶段 \(m\) 的评分窗口、LP 数 | — | 334、34、42 或 17、按阶段、1,316 或 1,284 |
| \(K^{\text{eff}}_d\) | 第 \(d\) 日实际可用情景数 | 个 | \(\le K\) |
| \(F^{\text{tot}}_d,F^{\text{del}},F^{\text{plan}},F^{\text{adj}},F^{\text{emg}},F(\mathcal C),F^{\text{PF}}\) | 日总费用、交付期总费用、三个分项、启用集合下的费用、完美预见费用 | 元 | 见 §1.3（**\(F^{\text{del}}\ne\sum_dF_d\)**） |
| \(\Delta F,\delta F,R_{\text{scan}},\rho_{\text{scan}},\text{mono}\) | 绝对/相对差、极差、相对极差、单调性 | 元,%,元,%,— | 见 §四 式(2) |
| \(\Psi,\psi,\Psi_d,\Psi_{\min},\Psi_{\max},n^{+},n^{-},\Psi^{-},\Psi^{+},P^{+}\) | 节省额、相对节省、单日/最差/最好单日、为正/为负天数、区间上下界、为正比例 | 元,%,元,元,元,天,天,元,元,— | 见 §四 式(4)(9) |
| \(\Psi^{\text{marg}}_r(\mathcal C),\Psi^{\text{Shap}}_r\) | 条件边际节省 / Shapley 分摊节省 | 元 | 见 §四 式(5) |
| \(\mathcal T_m\) | 阶段 \(m\) 的评分窗口 | — | 见 §1.2、§四 式(6)(8) |
| \(\xi_{k,d,t},\mathcal E_{d,t},\mathrm{Quant}_\zeta(\mathcal E_{d,t})\) | 情景值 / 情景集 / 情景集分位数算子 | 与变量同 | 见 §1.2、§四 式(6)(7)(8) |
| CRPS, ES | 分布评分（逐时段标量 / 整条路径向量） | 与变量同 | 见 §四 式(6)(7) |
| \(\hat\beta^{(m)},\hat\Phi^{(m)}_\zeta\) | 逐时段边际经验覆盖率 / 经验分位频率 | — | 见 §四 式(8) |
| \(z_{\zeta},z_{\max},\text{TS}_{5\%}\) | 尾部风险分位数、最大值、尾部占比 | kWh,% | 见 §三 S4.5 |
| \(\Delta F^{\text{risk}},\Delta F^{\text{info}}\) | 价格波动风险成本 / 预测误差信息缺失成本 | 元 | 见 §四 式(10) |
| \(\delta^{\text{obj}},\chi\) | 跨算法最大相对目标差 / 解向量差异占比 | —,% | 见 §三 S3.4、S4.4 |
| \(\text{R}_1,\text{R}_2,\text{R}_3,\oslash\) | 结论强度判定：排序稳健 / 数值敏感 / 结论脆弱 / 未完成 | — | 见 §1.4 |

---

## 六、护栏（敏感度部分的 13 条）

1. **时间口径的命名与主表 §1.1.1 相反，必须先对齐再写。**
   仓库 `reports/q1_report.md` §7 把交付口径命名为「**区间起点**」（行标即区间起点），把「行标为区间终点」当作**反事实**；
   主表 §1.1.1 用三处独立证据（表1 的 `B61/B73/…`、表2 的 \(\{144\}\cup\{1,\dots,23\}\)、附件1 首末行标签）核定
   **附件1 的行标签就是区间终点**，并明确"论文不要引用'起点口径'这个词"。
   ⇒ **交付数值两边一致（35,126.8486 元）**，只是名字相反。
   **建议**：按主表改写为「数据行标签 = 区间终点（交付口径）」与「整体错移 10 分钟（反事实）」，并把仓库 §7 表格的两行标题互换。**待你拍板**。
2. **写"稳健"必须同时给出三件事**：扫的是哪个符号、扫描范围、\(\text{R}\) 档。缺一项就是空话。
3. **\(\eta\) 的"各"字不能省**：\(\eta=0.9\) 必须写成"充、放电**各** 0.9（往返 81%）"，否则与往返 90% 的敏感性对照自相矛盾。
4. **\(\alpha\) 未扫描**：只能写成假设（题面未给紧急电价倍数），并说明其方向性影响；公式里**不得出现裸 5**（主表 §3.6）。
5. **\(K\) 是上限不是实际值**：正文出现 \(K=30\) 时必须说明 \(K^{\text{eff}}_d\le K\)（年初历史不足），且 \(K\) 网格 ⊘ 未完成。
6. **校准结论只能写"欠覆盖"**：\(\hat\beta^{(m)}_{0.9}\approx0.83\)（各阶段）、极端日峰值口径 0.647 ⇒ 不得写"90% 情景区间已校准"，也不得只强调 CRPS 下降。
7. **排序不变 \(\ne\) 数值不变**：所有 R₂ 档分析必须两句话都写（数值变了 + 结论没反转），并给 \(\delta F\) / \(\rho_{\text{scan}}\)。
8. **第一问的单日数字不得与二~四问的 334 天数字并列**（主表 §一 + master handoff §1）：\(\mathcal D^{\text{del}}\) 只覆盖二~四问。
9. **"不修改 `src/`"要写成方法学优点而非省略**：全部敏感度实验以替换模块命名空间中的名字实现，
   论文应声明"敏感性实验未改动模型实现，基准点逐位复现交付值"（\(g_0\) 校验、\(\lvert\Delta F\rvert=0\)）。
10. **归档数字一律不引**：旧 Q3 模型的 \(\mathcal I=\text{冻结0}\) 与 \(\Xi=\text{退}\) 结果**只存在于 `outputs/q3/` 与 `reports/_archive/`**，
    与现行多阶段模型的口径不同；现行模型的这两项是 ⊘。
11. **Bootstrap 的适用范围要写全**：区间只反映**本年度逐日费用差的时间块重采样**，
    **不代表跨年度结构不确定性**（仓库 `reports/bootstrap_monthly.md` 末行原话）——
    不得由"\(P^{+}=100\%\)"推出"该策略在任何年份都更省"。
12. **覆盖率是"逐时段边际覆盖"，不是"全天联合覆盖"**：先对时段、后对日期两次平均（§四 式(8)）；
    而极端日的 64.7% 用的是"日峰值落入阶段 0 峰值情景 90% 区间"这**第三种**口径。
    三种口径不得互相换算、也不得用一个去解释另一个。
13. **费用口径不得串**：\(F^{\text{del}}=\sum_d F^{\text{tot}}_d\)，其中 \(F^{\text{plan}}_d=\sum_t p_t\min(x_t,q_t)\)；
    主表 §1.5 的 \(F_d=\sum_t p_t x_{d,t}\) **只在 \(q\equiv x\)（第一、二问）时**等于 \(F^{\text{plan}}_d\)，
    不得当作三、四问的日总费用。同理 \(F^{\text{PF}}\) 是事后下界参照，不得进策略比较表。

---

## 七、自检：我做了什么、发现了什么、待你拍板

**做过**（全部在 `q3-hybrid-mpc@5611746` 的工作树上逐文件核对，未改仓库任何文件）：

- 读了 `scripts/` 下的敏感度与对照脚本：`efficiency_sensitivity.py`、`assemble_efficiency_sensitivity.py`、
  `q3_lambda_sensitivity.py`、`q4_lambda_sensitivity.py`、`q3_solver_uniqueness_check.py`、`q4_solver_uniqueness_check.py`、
  `q4_perfect_foresight.py`、`run_saa_experiment.py`、`assemble_k_convergence.py`、`assemble_q4_controlled.py`、
  `analyze_scenario_calibration.py`、`bootstrap_monthly_analysis.py`、`validate_paper_analysis.py`、`assemble_q3_stage_comparison.py`；
- 读了产物：`outputs/efficiency_sensitivity/*.json`、`outputs/q3_multistage/{lambda_sensitivity_K30,solver_sensitivity_*,solver_degeneracy_census,q3_stage_comparison}.json`、
  `outputs/q4/{lambda_sensitivity_q4_K30,solver_sensitivity_*,perfect_foresight_q4_K30}.json`、
  `outputs/paper_analysis/{saa,calibration,bootstrap,q4_controlled}/*.json`、`outputs/q1..q4/summary*.json`；
- 读了报告：`reports/{q1_report,q2_report,q2_review,q3_report,q4_report,efficiency_sensitivity,paper_handoff_efficiency_sensitivity,scenario_calibration,bootstrap_monthly,q4_controlled_comparison,q1_q4_paper_master_handoff,project_task_reference}.md`；
- 读了模型文档：`docs/{q2_model,q2_derivation,q3_model,q3_derivation,q3_multistage_model,q4_model}.md`。

**第二轮复核（专查公式逻辑，逐条回读实现）**：

- 把 §四 的 10 条公式**逐条对照源码**：CRPS / ES / 覆盖率 / 分位数校准回到 `scripts/analyze_scenario_calibration.py`
  的 `crps()`、`energy_score()`、`update()`；Bootstrap 回到 `scripts/bootstrap_monthly_analysis.py` 的 `compare()`；
  条件边际与 Shapley 回到 `outputs/q3_multistage/q3_stage_comparison.json` 的数值反推。
  据此**改写/修正了 9 处**：
  1. 式(3) 的基准复现校验原本写成残缺的绝对值式（`\bigl|F_j\bigr|…`），已改为 \(\theta_j=1\Rightarrow F_j=F^0\)；
  2. 式(5) 的条件边际漏了 \(\mathcal C\supseteq\{0\}\)、\(r\notin\mathcal C\) 的前提，且未说明"不同上下文不可相加"，已补；
  3. 式(6)(7) 原来把 CRPS/ES 写成单一标量式 —— **与实现的"按（日，阶段）先算标量再对日期平均"不符**，已拆成
     \(\mathrm{CRPS}_{d,t}\) + 双重平均，并指明 ES **只有一次平均**、与 CRPS 量纲不同；
  4. 式(8) 原来只对日期求和（隐含"每天一个标量"）—— 实际是**先对时段、后对日期**两次平均，已改为双重求和，
     并补上"边际覆盖 ≠ 联合覆盖""极端日 64.7% 是第三种口径"两条；
  5. 式(9) 原来写成"逐日随机移位"（逻辑上不成立：块必须整体搬移），已改为**块起点 + 块内连续 + 取模环绕 + 尾部截断**的完整形式；
  6. §1.3 的 \(F^{\text{del}}\) 原写成 \(\sum_d F_d\)（\(F_d\) 取自主表 §1.5，只含计划购电）——
     对三、四问**漏掉了调整费与紧急购电费**，已改为 \(\sum_d F^{\text{tot}}_d\) 并列出三分项定义式。
  7. 式(10) 原写"**同一个求解器**、只换电价信息集"——这与实现不符：\(F^{\text{Q3}}\) 来自第三问的
     `src/q3_multistage.py`，\(F^{\text{PF}}\) 与 \(F^{\text{Q4-3}}\) 来自第四问的 `src/q4_solver.py`，
     **跨了两套实现**。已改为"PF 与 Q4-3 严格同源；Q3 与 PF 的可比性依赖仓库给出的
     '附件1 电价 = 附件4 逐时段均值（最大差 \(5.15\times10^{-5}\)）'这条论据"。
  8. §四 式中原本用 \(x_{k,d,t}\) 表示**情景值**、\(q^{(\cdot)}_{d,t}\) 表示**分位数**——
     **与主表 §1.3 的 \(x_t\)（计划购电量）、\(q_t\)（最终生效合约量）直接撞名**（违反本文件"不改主表、不与主表冲突"的承诺）。
     已改用 \(\xi_{k,d,t}\) 表示情景值、\(\mathcal E_{d,t}\) 表示情景集、\(\mathrm{Quant}_\zeta(\cdot)\) 表示分位数算子，
     并在 §1.2、§五 补入定义。
  9. §1.2 原缺 \(\mathcal T_m\)（阶段评分窗口）——它被式(6)(8) 直接引用却未定义，已补入 §1.2 与 §五。
- 逐字核对了本文引用的 **71 个仓库字段名/变量名**是否真实存在（脚本扫描 `scripts/ src/ docs/ reports/` 与轻量 JSON）：
  其中 70 个命中；**唯一一处不存在的 `terminal_water_value` 已改为真实字段** `terminal_value` / `terminal_value_yuan_per_internal_kwh`。
- 复核了量纲：\(\lambda\) 在二问/4-2 是**元/内部 kWh**、在三问/4-3 是**元/kWh**（主表 §3.4 警告差一个 \(\eta\)），
  本文原表头把两者都写成"元/kWh"，已改。
- 复核了计数自洽：速览列 S1.1–S4.8 共 **26 项**，其中 ⊘ **3 项**（S2.5、S3.8、S4.8）；
  故第二问小节是"5 项，**1 项** ⊘"、第三问"8 项，**1 项** ⊘"（原误写 2 项）、第四问"8 项，1 项 ⊘"，与速览一致。
- 复核了仓库**是否真有** \(\varepsilon\) 消融：`src/q2_solver.py` 中 `throughput_penalty: float = 1e-7` 是唯一取值，
  CLI 无相关开关，`scripts/` 下也无任何脚本改过它 ⇒ S2.5 由原写的"有（\(10^{-7}\)）↔ 无"（暗示做过消融）
  改为 **⊘ 未做**，这是本次唯一一处**把"看起来做过"改回"没做过"**的更正。
- 修了 1 处 **Markdown 表格语法错误**：速览表的表头有 7 列而分隔行只有 5 段，部分渲染器会整表失效，已补齐为 7 段。

**仓库自身的问题（都不是数值问题，但论文不能照抄）**：

1. `docs/q3_multistage_model.md` **同一文件内 \(\lambda\) 两义**：§8「参数汇总」与目标函数处 \(\lambda=0.478\) 元/kWh（终端水价），
   而 §5 预测子模型的指数衰减权重也写作 `λ=0.8` ⇒ 论文统一后衰减权重必须写主表的 \(\omega_j\)。
2. `docs/q2_model.md` **同一文件内 \(\varepsilon\) 两义**：残差轮廓向量 \(\boldsymbol\varepsilon_j\) 与吞吐惩罚 \(\epsilon\sum(\cdot)\)。
   ⇒ 论文按主表 §1.4 写 \(\varepsilon^{\text{res},k}_t\) 与 \(\varepsilon\) 两个符号，并在符号表加脚注区分。
3. `docs/q2_model.md` 的目标函数用 \(k\) 作**时段**索引（\(\sum_{k=1}^{144}p_kG_{d,k}\)），与三、四问的 \(k=\)**情景**索引撞名
   ⇒ 一律改 \(t\)（主表已定）。
4. \(\eta\) **文档落后于代码**：`docs/*.md` 仍写单一 \(\eta=0.9\)，而 `src/efficiency.py` 与 `summary*.json` 已是
   `eta_charge` / `eta_discharge` / `roundtrip_efficiency` 三件套 ⇒ 本表 §2.1 已给出对照。
5. `figures/paper/README.md` 列了 `fig_k_convergence`，但该图**在树里不存在**（\(K\) 网格未跑完），与 §3.5 ⊘ 一致 ⇒ 论文暂不可引用该图。
6. 提交 `5611746` 的 message **只有标题无正文**（与前两笔的详细正文风格不一致），且 `figures/paper/README.md` 与产物不完全对应 ⇒ 引用时以产物 JSON 为准。
7. **终端价值的量纲跨问不同、且报告里被简称掩盖**：二问 `outputs/q2/summary.json` 的字段名写明了
   `terminal_value_yuan_per_internal_kwh = 0.33417`（**元/内部 kWh**），三、四问是 `LAM = 0.478`（**元/kWh**）；
   主表 §3.4 已说明两者相差一个 \(\eta\)。仓库报告里两处常简称"水价/元/kWh"，
   **引用时必须自行补准单位**，否则会写出差一个 \(\eta\) 的对比。

**待你拍板**（都会影响论文正文，但**都不改任何数值**）：

1. **§六 护栏 1**：时间口径的命名按主表改（推荐），还是保留仓库的"起点口径"字样？——推荐按主表，并把仓库 §7 的两行标题互换。
2. **§1.3 \(\Psi\) 的符号选择**：接受用 \(\Psi/\psi\) 表示节省额与相对节省率（避开主表已占用的字母），还是你另有偏好？
3. **§1.4 三档判定** \(\text{R}_1/\text{R}_2/\text{R}_3\)：是否同意把它作为全文"稳健"措辞的唯一开关？
4. **S3.2 的 Shapley**：三个时刻的 Shapley 之和恰等于全组合节省（622,378.88），
   但**条件边际不可相加**（4 个上下文下的值各不相同，其和 \(\ne\Psi\)）——
   正文是只报 Shapley（推荐），还是两组都报并解释差异？
5. **S4.2**：两个变体的 \(\lambda_0\) 不同且**量纲不同**（0.33417 元/内部 kWh / 0.478 元/kWh），
   正文是写成一个"\(\lambda\) 敏感性"节，还是拆成两节分别报、并各自标注单位？（仓库是拆成 §9.1 / §9.2。）
6. 是否要我把本文件 §五 的**追加块**直接并入主表 §1（做成一张总符号表），还是保持两文件分册？
